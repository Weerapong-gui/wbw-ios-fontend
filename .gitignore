# Xcode / build
build/
DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/

# XcodeGen output (project.yml is the source of truth)
WBW.xcodeproj/

# Secrets
WBW/GoogleService-Info.plist

# macOS
.DS_Store

# mockup ระหว่าง brainstorm (ของชั่วคราว — SDD ledger ใน .superpowers ที่เหลือยัง commit ได้)
.superpowers/brainstorm/

# repo อื่นที่วางคร่อมอยู่ในโฟลเดอร์นี้ ไม่ใช่ของ repo นี้ — กันไม่ให้ `git add` เผลอดูดเข้ามา
claude-code-server-side-swift-skills/
