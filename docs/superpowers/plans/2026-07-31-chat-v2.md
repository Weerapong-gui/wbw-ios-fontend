# Group Chat v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebuild WBW group chat so a joiner sees only messages sent after they joined, messages carry times and day separators, delivery is sub-second, senders see "read by N", members away from the chat screen get a push, and the list scrolls like iMessage.

**Architecture:** A new `group_chat_state` table (one row per user per group) holds both the join cut-off (`since_id`) and the read cursor (`last_read_id`). A new additive endpoint `GET /groups/:id/chat/sync` returns messages above the cut-off plus every member's read cursor in one response, and parks the request for up to 25 s (long-poll) until a Postgres `LISTEN/NOTIFY` event wakes it. The old `GET/POST /groups/:id/messages` endpoints are left byte-identical so the shipped Android app keeps working. On iOS the chat engine moves out of the chat screen into an app-lifetime `ChatSession` owned by `MainTabView`, so unread badges and pushes work while the chat is closed. Day separators and message grouping are computed by a pure, unit-tested function.

**Tech Stack:** Node 20 + Express 4 + `pg` 8 + firebase-admin 14 (backend); SwiftUI + SwiftData + XCTest, iOS 18 deployment target, XcodeGen (iOS).

**Spec:** `docs/superpowers/specs/2026-07-31-chat-system-design.md`

## Global Constraints

- **Two repos.** iOS is `/Users/park/wbw-ios-fontend`. Backend is `/Users/park/Desktop/WBW/backend` (a separate git repo — commit there separately). **Never modify `/Users/park/Student-Union-Server` (SUS)** — a teammate owns it.
- **Do not change `GET /groups/:groupId/messages` or `POST /groups/:groupId/messages`** — request and response shapes must stay byte-identical. The shipped Android app uses them. All chat v2 work is additive.
- Scope is group chat only. Do not touch Map, SU RUN, Ranking, Ticket, Settings, Staff, or the notification/announcement feature except where this plan names the exact file and line.
- iOS deployment target is **18.0**. Code must compile against it; no `if #available` is needed for any API used here (`defaultScrollAnchor`, `scrollDismissesKeyboard`, `onScrollGeometryChange`, `sensoryFeedback` are all iOS 17/18).
- Colors come only from `WBW/Config.swift`: `wbwCream`, `wbwInk`, `wbwGold`, `wbwGreen`, plus the local cream background `Color(red: 250/255, green: 247/255, blue: 240/255)` already used in the chat files. Do not invent named colors.
- New `.swift` files under `WBW/` (including subfolders) are picked up by XcodeGen's `sources: - WBW`. **Run `xcodegen generate` after creating any new file**, before building.
- `WBW.xcodeproj/` is **gitignored** (`project.yml` is the source of truth). Never `git add` it — the command fails on an ignored path. Stage only source, test, and config files by explicit path.
- Both repos are on branch `feature/chat-v2`. The backend repo root is `/Users/park/Desktop/WBW` (a monorepo also holding `android_native/` and `web/`), and it has **unrelated uncommitted changes under `android_native/`** — never `git add -A` or `git commit -a` there; stage only the explicit paths each task names.
- iOS build: `cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild -scheme WBW -destination 'generic/platform=iOS Simulator' build`
- iOS test: `cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'`
- Xcode builds take minutes. Use a Bash timeout of up to `600000` ms.
- A plain `xcodebuild build` does **not** default to the Debug configuration, so `#if DEBUG` code — including the `-uitest*` launch hooks and the chat instrumentation — compiles out silently. Pass `-configuration Debug` explicitly whenever you build something you intend to drive from the simulator.
- To exercise the app headlessly there is no tap tooling; use the DEBUG launch hooks: `xcrun simctl launch booted th.ac.mfu.wbwSwift -uitestToken <jwt> -uitestUser <username> -uitestRole participant -uitestTab 3 -uitestChat YES`, plus `-uitestChatCloseAfter <seconds>` to close the chat overlay through the same path a user's tap takes. `Config.backend` has a `.nodeLocal` case pointing at `http://localhost:4000` for this; the committed default stays `.prodNode`.
- Backend has **no unit-test framework installed and none is being added.** Backend verification is `api/scripts/chat-smoke.mjs`, an integration script run against a live API, which this plan grows task by task. It reads the database directly for assertions that no HTTP route can observe, so `DATABASE_URL` is a **required** input alongside the API credentials — the script fails loudly when any of them is missing, and never skips a check. The standard command is:

  ```bash
  export API_BASE=http://localhost:4000 U1=6931000001 P1=chatv2test U2=6931000002 P2=chatv2test GROUP_ID=1
  export DATABASE_URL=postgres://wbw:wbw_secret@localhost:5433/wbw
  cd /Users/park/Desktop/WBW/backend && docker compose restart api \
    && until curl -sf "$API_BASE/health" >/dev/null; do sleep 1; done \
    && node api/scripts/chat-smoke.mjs
  ```

  Postgres is published on **5433**, not 5432 — the teammate's SUS Postgres already holds 5432 on this machine, so `backend/docker-compose.override.yml` (untracked, never commit it) remaps the port. `api/src` is bind-mounted read-only into the container, so `docker compose restart api` is required after editing anything under it.
- Backend SQL migrations are plain files in `db/migrations/`, applied by hand, and written **idempotent** (`IF NOT EXISTS`, `ON CONFLICT DO NOTHING`) — follow `db/migrations/002_notification_preset.sql`.
- Backend code comments in this repo are Thai. Match that. Swift comments in the iOS repo are Thai. Match that too.
- Postgres returns `BIGINT` as a **JavaScript string**. Convert with `Number(...)` before putting an id in a JSON response field that iOS decodes as `Int64` (`since_id`, `last_read_id`, `member_count`). Leave `message.id` exactly as it is today — iOS decodes it through `@FlexibleString`.

---

## Environment setup (do this once, before Task 1)

The backend smoke script needs a running API, a database with the schema, and two participant logins in a group with free seats.

- [ ] **Step A: Start the local stack**

```bash
cd /Users/park/Desktop/WBW/backend
docker compose --profile localdb up -d db api
docker compose ps
```

Expected: `db` and `api` both `running`. If the API points at the NAS database instead, that is fine too — the smoke script only needs `API_BASE` to be reachable.

- [ ] **Step B: Confirm the API answers**

```bash
curl -s http://localhost:4000/health
```

Expected: `{"ok":true,"service":"wbw-api",...}`

- [ ] **Step C: Record two participant logins and a group id**

Pick two existing participant accounts (or create them the way the project normally does) and a group with at least 2 free seats. Export them for every later step:

```bash
export API_BASE=http://localhost:4000
export U1=<username1> P1=<password1>
export U2=<username2> P2=<password2>
export GROUP_ID=<group id with 2+ free seats>
export DATABASE_URL=postgres://wbw:wbw_secret@localhost:5433/wbw
```

`DATABASE_URL` is required, not optional: some smoke assertions read `group_chat_state`
directly because no HTTP route can observe it (see Task 1). The script must fail loudly
when it is absent rather than skipping those checks.

Expected: `curl -s -X POST $API_BASE/auth/login -H 'content-type: application/json' -d "{\"username\":\"$U1\",\"password\":\"$P1\"}"` returns a `token`.

---

## File structure

**Backend — `/Users/park/Desktop/WBW/backend`**

| File | Responsibility |
|---|---|
| `db/migrations/003_chat_v2.sql` | new — `group_chat_state` table, index, backfill |
| `api/src/chatEvents.js` | new — `LISTEN/NOTIFY` bridge only: `startChatEvents`, `notifyGroup`, `waitForGroup` |
| `api/src/routes/chatRoutes.js` | new — `GET /sync`, `POST /read`, mounted under `/groups/:groupId/chat` |
| `api/src/routes/groupRoutes.js` | edit — join/leave maintain `group_chat_state`; mount chat routes; notify + push after message insert |
| `api/src/push.js` | edit — add `sendChatPush` |
| `api/src/index.js` | edit — call `startChatEvents()` at boot |
| `api/scripts/chat-smoke.mjs` | new — integration checks, grown across tasks |

**iOS — `/Users/park/wbw-ios-fontend`**

| File | Responsibility |
|---|---|
| `WBW/Chat/ChatDTOs.swift` | new — `ChatSyncResponse`, `ReadCursor` |
| `WBW/APIClient+Chat.swift` | new — `chatSync`, `chatRead` |
| `WBW/Chat/ChatRow.swift` | new — pure row building, day labels, time formatting |
| `WBW/Chat/ChatSession.swift` | new (replaces `WBW/ChatStore.swift`) — sync loop, outbox, cursors, unread |
| `WBW/Chat/ChatBubble.swift` | new — bubble, tail shape, day pill, unread divider, read status |
| `WBW/Chat/ChatToast.swift` | new — in-app banner |
| `WBW/GroupChatView.swift` | edit — screen scaffold, scrolling, input bar |
| `WBW/MainTabView.swift` | edit — owns `ChatSession`, tab badge, toast, push routing |
| `WBW/AppDelegate.swift` | edit — route `type == "chat"` separately from announcements |
| `WBW/Models.swift` | edit — add `AppError.notInGroup` |
| `WBW/ChatStore.swift` | deleted in Task 5 |
| `WBWTests/ChatDTOTests.swift` | new — decoding |
| `WBWTests/ChatRowTests.swift` | new — row building, day labels |
| `WBWTests/ChatSessionTests.swift` | new — unread/read-count/purge logic |

---

### Task 1: Cut-off state and the `/chat/sync` endpoint

Creates the table, makes join/leave maintain it, and adds `GET /groups/:id/chat/sync` returning only messages above the caller's cut-off. No long-poll yet (`wait` is accepted and ignored).

**Files:**
- Create: `/Users/park/Desktop/WBW/backend/db/migrations/003_chat_v2.sql`
- Create: `/Users/park/Desktop/WBW/backend/api/src/routes/chatRoutes.js`
- Create: `/Users/park/Desktop/WBW/backend/api/scripts/chat-smoke.mjs`
- Modify: `/Users/park/Desktop/WBW/backend/api/src/routes/groupRoutes.js` (join handler, leave handler, mount)

**Interfaces:**
- Consumes: `query`, `withTransaction` from `../db.js`; `requireAuth` from `../auth.js`; the existing `isMember(userId, groupId)` helper at the top of `groupRoutes.js`.
- Produces: `GET /groups/:groupId/chat/sync?after=<id>&wait=<0..25>` →
  `{since_id: number, member_count: number, messages: Message[], cursors: [{user_id: string, last_read_id: number}]}`;
  `chatRoutes.js` default-exports an Express `Router({ mergeParams: true })`.

- [ ] **Step 1: Write the failing smoke test**

Create `/Users/park/Desktop/WBW/backend/api/scripts/chat-smoke.mjs`:

```js
// สคริปต์ตรวจแชท v2 แบบ integration — ยิงใส่ API ที่รันอยู่จริง
// ใช้: API_BASE=... U1=.. P1=.. U2=.. P2=.. GROUP_ID=.. DATABASE_URL=.. node api/scripts/chat-smoke.mjs
// DATABASE_URL จำเป็น — บาง assertion อ่าน group_chat_state ตรงๆ เพราะไม่มี route ไหนมองเห็น
const BASE = process.env.API_BASE || 'http://localhost:4000';
const { U1, P1, U2, P2 } = process.env;
const GROUP_ID = Number(process.env.GROUP_ID);

let failures = 0;
function check(name, ok, detail = '') {
  console.log(`${ok ? 'PASS' : 'FAIL'}  ${name}${ok || !detail ? '' : ` — ${detail}`}`);
  if (!ok) failures += 1;
}

async function login(username, password) {
  const r = await fetch(`${BASE}/auth/login`, {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify({ username, password }),
  });
  if (!r.ok) throw new Error(`login ${username} ล้มเหลว: ${r.status} ${await r.text()}`);
  return (await r.json()).token;
}

const api = (token) => async (method, path, body) => {
  const r = await fetch(`${BASE}${path}`, {
    method,
    headers: {
      authorization: `Bearer ${token}`,
      ...(body ? { 'content-type': 'application/json' } : {}),
    },
    ...(body ? { body: JSON.stringify(body) } : {}),
  });
  const text = await r.text();
  let json = null;
  try { json = text ? JSON.parse(text) : null; } catch { /* ไม่ใช่ JSON */ }
  return { status: r.status, body: json, text };
};

async function main() {
  if (!U1 || !P1 || !U2 || !P2 || !Number.isInteger(GROUP_ID)) {
    throw new Error('ต้องตั้ง env: U1 P1 U2 P2 GROUP_ID');
  }
  const a = api(await login(U1, P1));
  const b = api(await login(U2, P2));

  // เคลียร์สถานะ: ทั้งคู่ออกจากกลุ่มก่อน แล้ว A เข้ากลุ่มเป้าหมาย
  await a('POST', '/groups/leave');
  await b('POST', '/groups/leave');
  const joinA = await a('POST', `/groups/${GROUP_ID}/join`);
  check('A เข้ากลุ่มได้', joinA.status === 200, `status ${joinA.status} ${joinA.text}`);

  // A ส่ง 3 ข้อความ "ก่อน" B เข้ากลุ่ม
  for (const body of ['ก่อนเข้า 1', 'ก่อนเข้า 2', 'ก่อนเข้า 3']) {
    const r = await a('POST', `/groups/${GROUP_ID}/messages`, {
      client_id: crypto.randomUUID(), body, device_time: new Date().toISOString(),
    });
    if (r.status !== 201) throw new Error(`ส่งข้อความล้มเหลว: ${r.status} ${r.text}`);
  }

  // B เข้ากลุ่มทีหลัง → ต้องไม่เห็นอะไรเลย
  const joinB = await b('POST', `/groups/${GROUP_ID}/join`);
  check('B เข้ากลุ่มได้', joinB.status === 200, `status ${joinB.status} ${joinB.text}`);

  const s1 = await b('GET', `/groups/${GROUP_ID}/chat/sync?wait=0`);
  check('sync ตอบ 200', s1.status === 200, `status ${s1.status} ${s1.text}`);
  check('B ไม่เห็นข้อความก่อนเข้ากลุ่ม', s1.body?.messages?.length === 0,
        `เห็น ${s1.body?.messages?.length} ข้อความ`);
  check('sync มี since_id > 0', Number(s1.body?.since_id) > 0, `since_id=${s1.body?.since_id}`);
  check('sync มี member_count', Number(s1.body?.member_count) >= 2, `member_count=${s1.body?.member_count}`);
  check('sync มี cursors ของคนอื่น', Array.isArray(s1.body?.cursors) && s1.body.cursors.length >= 1,
        `cursors=${JSON.stringify(s1.body?.cursors)}`);

  // A ส่งอีก 1 ข้อความ "หลัง" B เข้ากลุ่ม → B ต้องเห็นอันนี้อันเดียว
  const after = await a('POST', `/groups/${GROUP_ID}/messages`, {
    client_id: crypto.randomUUID(), body: 'หลังเข้า', device_time: new Date().toISOString(),
  });
  check('A ส่งข้อความหลัง B เข้ากลุ่ม', after.status === 201, `status ${after.status}`);

  const s2 = await b('GET', `/groups/${GROUP_ID}/chat/sync?wait=0`);
  check('B เห็นเฉพาะข้อความหลังเข้ากลุ่ม', s2.body?.messages?.length === 1,
        `เห็น ${s2.body?.messages?.length} ข้อความ`);
  check('เนื้อความถูกต้อง', s2.body?.messages?.[0]?.body === 'หลังเข้า',
        JSON.stringify(s2.body?.messages?.[0]?.body));

  // เข้ากลุ่มเดิมซ้ำ → ตัดใหม่ ต้องกลับไปเห็น 0
  await b('POST', `/groups/${GROUP_ID}/join`);
  const s3 = await b('GET', `/groups/${GROUP_ID}/chat/sync?wait=0`);
  check('เข้ากลุ่มเดิมซ้ำ = ตัดประวัติใหม่', s3.body?.messages?.length === 0,
        `เห็น ${s3.body?.messages?.length} ข้อความ`);

  // endpoint เดิมต้องไม่เปลี่ยนพฤติกรรม (Android ยังใช้อยู่)
  const legacy = await b('GET', `/groups/${GROUP_ID}/messages?limit=50`);
  check('endpoint เดิมยังคืน array เหมือนเดิม', Array.isArray(legacy.body),
        `ได้ ${typeof legacy.body}`);
  check('endpoint เดิมยังเห็นประวัติทั้งหมด', legacy.body?.length >= 4,
        `เห็น ${legacy.body?.length} ข้อความ`);

  console.log(failures ? `\n${failures} ข้อไม่ผ่าน` : '\nผ่านทั้งหมด');
  process.exit(failures ? 1 : 0);
}

main().catch((e) => { console.error(e); process.exit(1); });
```

- [ ] **Step 2: Run it to verify it fails**

```bash
cd /Users/park/Desktop/WBW/backend && node api/scripts/chat-smoke.mjs
```

Expected: the `sync ตอบ 200` check FAILS with status 404 (`ไม่พบ endpoint นี้`), and the checks after it fail too.

- [ ] **Step 3: Write the migration**

Create `/Users/park/Desktop/WBW/backend/db/migrations/003_chat_v2.sql`:

```sql
-- แชท v2: ตัดประวัติตอนเข้ากลุ่ม (since_id) + อ่านถึงไหน (last_read_id) — idempotent
CREATE TABLE IF NOT EXISTS group_chat_state (
  user_id      UUID NOT NULL REFERENCES app_user(user_id) ON DELETE CASCADE,
  group_id     INT  NOT NULL REFERENCES participant_group(group_id) ON DELETE CASCADE,
  since_id     BIGINT      NOT NULL DEFAULT 0,   -- เห็นเฉพาะ group_message.id > ค่านี้
  last_read_id BIGINT      NOT NULL DEFAULT 0,   -- อ่านถึง id ไหนแล้ว
  read_at      TIMESTAMPTZ NOT NULL DEFAULT now(),  -- ใช้เป็นสัญญาณ "กำลังดูจอแชทอยู่"
  PRIMARY KEY (user_id, group_id)
);

CREATE INDEX IF NOT EXISTS idx_chat_state_group ON group_chat_state(group_id);

-- คนที่อยู่ในกลุ่มอยู่ก่อนฟีเจอร์นี้ → since_id = 0 คือประวัติเดิมไม่หาย
INSERT INTO group_chat_state (user_id, group_id)
SELECT user_id, group_id FROM participant_profile WHERE group_id IS NOT NULL
ON CONFLICT (user_id, group_id) DO NOTHING;
```

- [ ] **Step 4: Apply the migration**

```bash
cd /Users/park/Desktop/WBW/backend
docker compose --profile localdb exec -T db psql -U wbw -d wbw -f - < db/migrations/003_chat_v2.sql
```

If the API points at an external database instead, run `psql "$DATABASE_URL" -f db/migrations/003_chat_v2.sql`.

Expected: `CREATE TABLE`, `CREATE INDEX`, `INSERT 0 <n>`.

Verify:

```bash
docker compose --profile localdb exec -T db psql -U wbw -d wbw -c '\d group_chat_state'
```

Expected: five columns, primary key `(user_id, group_id)`.

- [ ] **Step 5: Create `api/src/routes/chatRoutes.js`**

```js
import { Router } from 'express';
import { query } from '../db.js';
import { requireAuth } from '../auth.js';

// mergeParams: true — ต้องอ่าน :groupId จาก router แม่ (groupRoutes)
const router = Router({ mergeParams: true });

const MSG_COLS = `m.id, m.group_id, m.sender_id, m.client_id, m.body,
                  m.device_time, m.created_at, p.first_name, p.last_name`;

// อยู่ในกลุ่มนี้จริงมั้ย — แหล่งความจริงเดียวกับ groupRoutes
async function isMember(userId, groupId) {
  const { rows } = await query(
    `SELECT 1 FROM participant_profile WHERE user_id = $1 AND group_id = $2`,
    [userId, groupId]
  );
  return rows.length > 0;
}

// จุดตัดประวัติของคนนี้ — ไม่มีแถว (เช่น admin ย้ายกลุ่มให้ตรงๆ) = 0 คือเห็นทั้งหมดแบบเดิม
async function loadSinceId(userId, groupId) {
  const { rows } = await query(
    `SELECT since_id FROM group_chat_state WHERE user_id = $1 AND group_id = $2`,
    [userId, groupId]
  );
  return rows[0]?.since_id ?? '0';
}

async function fetchMessages(groupId, afterId, sinceId, limit) {
  const { rows } = await query(
    `SELECT ${MSG_COLS} FROM group_message m
     LEFT JOIN participant_profile p ON p.user_id = m.sender_id
     WHERE m.group_id = $1 AND m.id > GREATEST($2::bigint, $3::bigint)
     ORDER BY m.id ASC LIMIT $4`,
    [groupId, afterId, sinceId, limit]
  );
  return rows;
}

// cursor ของสมาชิกปัจจุบัน ไม่รวมตัวเอง — JOIN participant_profile ตัดคนที่ออกกลุ่มไปแล้วทิ้ง
async function loadCursors(groupId, exceptUserId) {
  const { rows } = await query(
    `SELECT s.user_id, s.last_read_id
     FROM group_chat_state s
     JOIN participant_profile p ON p.user_id = s.user_id AND p.group_id = s.group_id
     WHERE s.group_id = $1 AND s.user_id <> $2`,
    [groupId, exceptUserId]
  );
  return rows.map((r) => ({ user_id: r.user_id, last_read_id: Number(r.last_read_id) }));
}

async function loadMemberCount(groupId) {
  const { rows } = await query(
    `SELECT member_count FROM participant_group WHERE group_id = $1`,
    [groupId]
  );
  return Number(rows[0]?.member_count ?? 0);
}

// ดึงข้อความ + cursor ในคำขอเดียว (wait = long-poll, ใส่จริงใน task ถัดไป)
router.get('/sync', requireAuth, async (req, res) => {
  const groupId = Number(req.params.groupId);
  if (!Number.isInteger(groupId)) return res.status(400).json({ error: 'กลุ่มไม่ถูกต้อง' });
  const userId = req.user.sub;
  if (!(await isMember(userId, groupId))) return res.status(403).json({ error: 'ต้องอยู่ในกลุ่มนี้ก่อน' });

  // ต้อง reuse ค่าที่ default แล้ว — อ้าง req.query.after ตรงๆ ซ้ำจะได้ "undefined" (string) ตอนไม่ส่ง after มา
  const afterRaw = String(req.query.after ?? '0');
  const afterId = /^\d+$/.test(afterRaw) ? afterRaw : '0';
  const sinceId = await loadSinceId(userId, groupId);
  const messages = await fetchMessages(groupId, afterId, sinceId, 50);

  res.json({
    since_id: Number(sinceId),
    member_count: await loadMemberCount(groupId),
    messages,
    cursors: await loadCursors(groupId, userId),
  });
});

export default router;
```

- [ ] **Step 6: Mount chat routes and maintain state on join/leave**

In `/Users/park/Desktop/WBW/backend/api/src/routes/groupRoutes.js`:

Add the import next to the existing ones at the top:

```js
import chatRoutes from './chatRoutes.js';
```

Mount it immediately after `const router = Router();`:

```js
// แชท v2 (เพิ่มใหม่) — endpoint เดิม /:groupId/messages ยังอยู่ครบ ไม่แตะ
router.use('/:groupId/chat', chatRoutes);
```

In the `POST /:groupId/join` handler, inside `withTransaction`, right after the
`UPDATE participant_profile SET group_id = $1 ...` query and before the
`return { status: 200, ... }`:

```js
      // แชท v2 — ย้ายกลุ่ม = ทิ้ง state กลุ่มเก่า
      await c.query(`DELETE FROM group_chat_state WHERE user_id = $1 AND group_id <> $2`, [userId, groupId]);
      // เข้ากลุ่มครั้งไหนก็ตัดประวัติใหม่ทุกครั้ง (เห็นเฉพาะข้อความหลังจากนี้)
      const mx = await c.query(
        `SELECT COALESCE(MAX(id), 0) AS max_id FROM group_message WHERE group_id = $1`,
        [groupId]
      );
      await c.query(
        `INSERT INTO group_chat_state (user_id, group_id, since_id, last_read_id, read_at)
         VALUES ($1, $2, $3, $3, now())
         ON CONFLICT (user_id, group_id) DO UPDATE
           SET since_id = EXCLUDED.since_id, last_read_id = EXCLUDED.since_id, read_at = now()`,
        [userId, groupId, mx.rows[0].max_id]
      );
```

Replace the whole `POST /leave` handler with:

```js
// ออกจากกลุ่ม
router.post('/leave', requireAuth, async (req, res) => {
  await query(
    `UPDATE participant_profile SET group_id = NULL, updated_at = now() WHERE user_id = $1`,
    [req.user.sub]
  );
  // แชท v2 — คนที่ออกไปแล้วต้องไม่ถูกนับใน "อ่านแล้ว N"
  await query(`DELETE FROM group_chat_state WHERE user_id = $1`, [req.user.sub]);
  res.json({ ok: true });
});
```

- [ ] **Step 7: Restart the API and run the smoke test**

```bash
cd /Users/park/Desktop/WBW/backend
docker compose restart api && until curl -sf "$API_BASE/health" >/dev/null; do sleep 1; done
node api/scripts/chat-smoke.mjs
```

Expected: every check PASSes, ending with `ผ่านทั้งหมด`.

- [ ] **Step 8: Commit (backend repo)**

```bash
cd /Users/park/Desktop/WBW/backend
git add db/migrations/003_chat_v2.sql api/src/routes/chatRoutes.js api/src/routes/groupRoutes.js api/scripts/chat-smoke.mjs
git commit -m "feat(chat): ตัดประวัติแชทตอนเข้ากลุ่ม + endpoint /chat/sync"
```

---

### Task 2: Long-poll via Postgres LISTEN/NOTIFY

Makes `/chat/sync?wait=25` park the request until a message lands in that group, so receive latency drops from 5–8 s to well under a second.

**Files:**
- Create: `/Users/park/Desktop/WBW/backend/api/src/chatEvents.js`
- Modify: `/Users/park/Desktop/WBW/backend/api/src/index.js` (boot hook)
- Modify: `/Users/park/Desktop/WBW/backend/api/src/routes/chatRoutes.js` (honour `wait`)
- Modify: `/Users/park/Desktop/WBW/backend/api/src/routes/groupRoutes.js` (notify after insert)
- Modify: `/Users/park/Desktop/WBW/backend/api/scripts/chat-smoke.mjs`

**Interfaces:**
- Produces: `startChatEvents(): void`, `notifyGroup(groupId: number, actor: string|null): Promise<void>`, `waitForGroup(groupId: number, ms: number, exceptActor: string|null): Promise<boolean>` from `api/src/chatEvents.js`.
- Consumes: `query` from `../db.js`, `process.env.DATABASE_URL`.

- [ ] **Step 1: Add the failing latency check to the smoke script**

In `api/scripts/chat-smoke.mjs`, insert this block immediately before the
`// endpoint เดิมต้องไม่เปลี่ยนพฤติกรรม` comment:

```js
  // long-poll: B ค้างรออยู่ → A ส่ง → B ต้องได้ภายใน 1 วิ (ไม่ใช่รอครบ wait)
  {
    await b('POST', `/groups/${GROUP_ID}/join`);         // reset cut-off ให้ B
    const t0 = Date.now();
    const parked = b('GET', `/groups/${GROUP_ID}/chat/sync?wait=10`);
    await new Promise((r) => setTimeout(r, 500));        // ให้ request ไปค้างที่ server ก่อน
    await a('POST', `/groups/${GROUP_ID}/messages`, {
      client_id: crypto.randomUUID(), body: 'ปลุก long-poll', device_time: new Date().toISOString(),
    });
    const got = await parked;
    const ms = Date.now() - t0;
    check('long-poll ตื่นเมื่อมีข้อความ', got.body?.messages?.length === 1,
          `ได้ ${got.body?.messages?.length} ข้อความ`);
    check('long-poll ตื่นภายใน 2 วิ', ms < 2000, `ใช้ ${ms}ms`);
  }

  // long-poll ไม่มีอะไรเข้า → ต้องหมดเวลาแล้วตอบ 200 พร้อม messages ว่าง
  {
    // reset cut-off ให้ B ก่อน — ไม่งั้น sync (ไม่มี after ขยับ) จะยังเห็นข้อความ "ปลุก long-poll"
    // จากบล็อกก่อนหน้าเป็นของค้างไม่ได้อ่าน แล้วตอบทันทีโดยไม่ได้ค้างรอจริง
    await b('POST', `/groups/${GROUP_ID}/join`);
    const t0 = Date.now();
    const idle = await b('GET', `/groups/${GROUP_ID}/chat/sync?wait=3`);
    const ms = Date.now() - t0;
    check('หมดเวลาแล้วตอบ 200 ว่าง', idle.status === 200 && idle.body?.messages?.length === 0,
          `status ${idle.status}, ${idle.body?.messages?.length} ข้อความ`);
    check('ค้างครบเวลาจริง (>= 2.5 วิ)', ms >= 2500, `ใช้ ${ms}ms`);
  }
```

- [ ] **Step 2: Run it to verify it fails**

```bash
cd /Users/park/Desktop/WBW/backend && node api/scripts/chat-smoke.mjs
```

Expected: `ค้างครบเวลาจริง (>= 2.5 วิ)` FAILS — `wait` is currently ignored so the request returns instantly.

- [ ] **Step 3: Create `api/src/chatEvents.js`**

```js
// สะพาน Postgres LISTEN/NOTIFY → EventEmitter ในเครื่อง
// cluster.js fork หลาย worker → emitter ในโปรเซสเดียวไม่ถึงกัน ต้องผ่าน PG
import pg from 'pg';
import { EventEmitter } from 'node:events';
import { query } from './db.js';

const CHANNEL = 'chat_event';

const bus = new EventEmitter();
bus.setMaxListeners(0);   // ผู้รอ = 1 ต่อ request ที่ค้างอยู่ อาจหลายพัน ไม่ใช่ leak

let client = null;
let retryMs = 1000;

function scheduleRetry() {
  if (client) {
    try { client.end(); } catch { /* ปิดไม่ได้ก็ช่าง */ }
    client = null;
  }
  setTimeout(connect, retryMs);
  retryMs = Math.min(retryMs * 2, 30000);
}

function connect() {
  // ต้องเป็น connection เฉพาะ ห้ามยืมจาก pool — LISTEN ผูกกับ connection ตลอดอายุ
  const c = new pg.Client({ connectionString: process.env.DATABASE_URL });
  client = c;
  c.on('error', (e) => {
    console.error('[chat] LISTEN error:', e.message);
    if (client === c) scheduleRetry();
  });
  c.on('notification', (msg) => {
    try { bus.emit('event', JSON.parse(msg.payload)); }
    catch { console.error('[chat] payload เพี้ยน:', msg.payload); }
  });
  c.connect()
    .then(() => c.query(`LISTEN ${CHANNEL}`))
    .then(() => { retryMs = 1000; console.log('[chat] LISTEN chat_event พร้อม'); })
    .catch((e) => {
      console.error('[chat] LISTEN connect ล้มเหลว:', e.message);
      if (client === c) scheduleRetry();
    });
}

/** เริ่มฟัง — เรียกครั้งเดียวตอน boot ของแต่ละ worker */
export function startChatEvents() {
  if (!client) connect();
}

/** ประกาศว่ากลุ่มนี้มีอะไรเปลี่ยน — ถึงทุก worker. actor = คนก่อเหตุ (null = ปลุกทุกคน) */
export async function notifyGroup(groupId, actor = null) {
  try {
    await query(`SELECT pg_notify($1, $2)`, [
      CHANNEL,
      JSON.stringify({ group_id: Number(groupId), actor: actor || null }),
    ]);
  } catch (e) {
    console.error('[chat] notify ล้มเหลว:', e.message);
  }
}

/** รอเหตุการณ์ของกลุ่มนี้ — true = มีเหตุการณ์, false = หมดเวลาหรือถูกยกเลิก
 *  exceptActor: ข้ามเหตุการณ์ที่ตัวเองก่อ (เช่น read ของตัวเอง ไม่ต้องปลุก poll ตัวเอง)
 *  signal: AbortSignal ทางเลือก — ให้ผู้เรียกปล่อย wait ก่อนครบเวลาได้ (เช่น client หลุดกลางคัน)
 *          ฟังก์ชันนี้ยังเป็นแค่ pub/sub ล้วนๆ ไม่รู้จัก request/response ใดๆ รับแค่ signal มาตรฐานของ JS */
export function waitForGroup(groupId, ms, exceptActor = null, signal) {
  const gid = Number(groupId);
  return new Promise((resolve) => {
    let done = false;
    const finish = (v) => {
      if (done) return;
      done = true;
      bus.off('event', onEvent);
      clearTimeout(timer);
      if (signal) signal.removeEventListener('abort', onAbort);
      resolve(v);
    };
    const onEvent = (e) => {
      if (e.group_id === gid && (!exceptActor || e.actor !== exceptActor)) finish(true);
    };
    const onAbort = () => finish(false);
    const timer = setTimeout(() => finish(false), ms);
    bus.on('event', onEvent);
    if (signal) {
      if (signal.aborted) { finish(false); return; }   // เผื่อหลุดไปแล้วตั้งแต่ก่อนเรียก (กันไว้ ปกติไม่เจอ)
      signal.addEventListener('abort', onAbort);
    }
  });
}
```

- [ ] **Step 4: Start it at boot**

In `/Users/park/Desktop/WBW/backend/api/src/index.js`, add the import with the others:

```js
import { startChatEvents } from './chatEvents.js';
```

and call it just above `app.listen(...)`:

```js
startChatEvents();   // แชท v2 — ฟัง NOTIFY สำหรับ long-poll
```

- [ ] **Step 5: Honour `wait` in `/chat/sync`**

In `api/src/routes/chatRoutes.js`, add the import:

```js
import { waitForGroup } from '../chatEvents.js';
```

and replace the body of the `/sync` handler from `const afterId = ...` to the
`res.json({...})` call with:

```js
  // ต้อง reuse ค่าที่ default แล้ว — อ้าง req.query.after ตรงๆ ซ้ำจะได้ "undefined" (string) ตอนไม่ส่ง after มา
  const afterRaw = String(req.query.after ?? '0');
  const afterId = /^\d+$/.test(afterRaw) ? afterRaw : '0';
  const sinceId = await loadSinceId(userId, groupId);
  const wait = Math.min(Math.max(Number(req.query.wait) || 0, 0), 25);

  let messages = await fetchMessages(groupId, afterId, sinceId, 50);
  if (!messages.length && wait > 0) {
    // ค้าง request ไว้จนมีเหตุการณ์ของกลุ่มนี้ หรือหมดเวลา — หมดเวลา = ตอบ 200 ว่าง (ปกติ ไม่ใช่ error)
    // client หลุดกลางคัน (แอปไปพัก/หลุดเน็ต) → ปล่อย listener/timer ทันที ไม่ query/ตอบต่อ (ไม่มีใครรอรับแล้ว)
    const ac = new AbortController();
    let clientGone = false;
    const onClose = () => { clientGone = true; ac.abort(); };
    req.on('close', onClose);
    await waitForGroup(groupId, wait * 1000, userId, ac.signal);
    req.off('close', onClose);   // ถอดทุกทางออก กัน listener ค้างบน keep-alive socket
    if (clientGone) { console.log('[chat] sync: client หลุดกลางคัน ปล่อย wait ไม่ตอบต่อ'); return; }
    messages = await fetchMessages(groupId, afterId, sinceId, 50);
  }

  // โหลด cursor หลังตื่น เพื่อให้สถานะ "อ่านแล้ว" สดที่สุด
  res.json({
    since_id: Number(sinceId),
    member_count: await loadMemberCount(groupId),
    messages,
    cursors: await loadCursors(groupId, userId),
  });
```

- [ ] **Step 6: Notify after a message is inserted**

In `api/src/routes/groupRoutes.js`, add the import:

```js
import { notifyGroup } from '../chatEvents.js';
```

In the `POST /:groupId/messages` handler, replace `res.status(201).json(rows[0]);` with:

```js
    res.status(201).json(rows[0]);
    // ปลุก long-poll ของทุกคนในกลุ่ม (actor = null เพื่อให้เครื่องอื่นของคนส่งตื่นด้วย)
    notifyGroup(groupId, null);
```

- [ ] **Step 7: Restart and run the smoke test**

```bash
cd /Users/park/Desktop/WBW/backend
docker compose restart api && until curl -sf "$API_BASE/health" >/dev/null; do sleep 1; done
node api/scripts/chat-smoke.mjs
```

Expected: all checks PASS, including `long-poll ตื่นภายใน 2 วิ` and `ค้างครบเวลาจริง (>= 2.5 วิ)`.

- [ ] **Step 8: Commit (backend repo)**

```bash
cd /Users/park/Desktop/WBW/backend
git add api/src/chatEvents.js api/src/index.js api/src/routes/chatRoutes.js api/src/routes/groupRoutes.js api/scripts/chat-smoke.mjs
git commit -m "feat(chat): long-poll ผ่าน PG LISTEN/NOTIFY"
```

---

### Task 3: iOS chat DTOs and API calls

Adds the decodable types and the two networking calls, with a decoding test.

**Files:**
- Create: `/Users/park/wbw-ios-fontend/WBW/Chat/ChatDTOs.swift`
- Create: `/Users/park/wbw-ios-fontend/WBW/APIClient+Chat.swift`
- Create: `/Users/park/wbw-ios-fontend/WBWTests/ChatDTOTests.swift`
- Modify: `/Users/park/wbw-ios-fontend/WBW/Models.swift` (the `AppError` enum at the bottom)

**Interfaces:**
- Consumes: `Config.apiBase`, `APIClient.send(_:)`, `MessageDTO`, `AppError` (all existing).
- Produces: `struct ChatSyncResponse { sinceId: Int64; memberCount: Int; messages: [MessageDTO]; cursors: [ReadCursor] }`; `struct ReadCursor { userId: String; lastReadId: Int64 }`; `AppError.notInGroup`; `APIClient.chatSync(token:groupId:after:wait:) async throws -> ChatSyncResponse`; `APIClient.chatRead(token:groupId:lastReadId:) async` (non-throwing on purpose — see below).

- [ ] **Step 1: Write the failing decode test**

Create `/Users/park/wbw-ios-fontend/WBWTests/ChatDTOTests.swift`:

```swift
import XCTest
@testable import WBW

final class ChatDTOTests: XCTestCase {
    private func decode(_ json: String) throws -> ChatSyncResponse {
        let dec = JSONDecoder()
        dec.keyDecodingStrategy = .convertFromSnakeCase
        return try dec.decode(ChatSyncResponse.self, from: json.data(using: .utf8)!)
    }

    func testDecodesFullResponse() throws {
        let r = try decode("""
        {"since_id": 42, "member_count": 7,
         "messages": [{"id": 43, "group_id": 3, "sender_id": "u1", "client_id": "c1",
                       "body": "ไง", "device_time": "2026-07-31T10:00:00.000Z",
                       "created_at": "2026-07-31T10:00:01.000Z",
                       "first_name": "ปาร์ค", "last_name": "ก"}],
         "cursors": [{"user_id": "u2", "last_read_id": 43}]}
        """)
        XCTAssertEqual(r.sinceId, 42)
        XCTAssertEqual(r.memberCount, 7)
        XCTAssertEqual(r.messages.first?.id, "43")
        XCTAssertEqual(r.messages.first?.senderName, "ปาร์ค ก")
        XCTAssertEqual(r.cursors.first?.userId, "u2")
        XCTAssertEqual(r.cursors.first?.lastReadId, 43)
    }

    func testDecodesEmptyTimeoutResponse() throws {
        let r = try decode("""
        {"since_id": 0, "member_count": 2, "messages": [], "cursors": []}
        """)
        XCTAssertTrue(r.messages.isEmpty)
        XCTAssertTrue(r.cursors.isEmpty)
        XCTAssertEqual(r.sinceId, 0)
    }
}
```

- [ ] **Step 2: Run it to verify it fails**

```bash
cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'
```

Expected: compile failure — `cannot find type 'ChatSyncResponse' in scope`.

- [ ] **Step 3: Create `WBW/Chat/ChatDTOs.swift`**

```swift
import Foundation

/// คำตอบของ GET /groups/:id/chat/sync — ข้อความ + สถานะอ่าน ในคำขอเดียว
struct ChatSyncResponse: Codable {
    /// จุดตัดประวัติของเรา — ข้อความ id <= ค่านี้ห้ามเห็น (ใช้ล้าง cache เครื่อง)
    let sinceId: Int64
    let memberCount: Int
    let messages: [MessageDTO]
    /// อ่านถึงไหน ของสมาชิกคนอื่น (server ตัดตัวเราออกให้แล้ว)
    let cursors: [ReadCursor]
}

struct ReadCursor: Codable, Equatable {
    let userId: String
    let lastReadId: Int64
}
```

- [ ] **Step 4: Add the error case**

In `/Users/park/wbw-ios-fontend/WBW/Models.swift`, change the `AppError` enum to:

```swift
enum AppError: LocalizedError {
    case message(String)
    case groupFull(String)
    case offline
    case notInGroup
    var errorDescription: String? {
        switch self {
        case let .message(m): return m
        case let .groupFull(m): return m
        case .offline: return "เชื่อมต่อเซิร์ฟเวอร์ไม่ได้"
        case .notInGroup: return "ไม่ได้อยู่ในกลุ่มนี้แล้ว"
        }
    }
}
```

- [ ] **Step 5: Create `WBW/APIClient+Chat.swift`**

```swift
import Foundation

extension APIClient {
    /// long-poll ข้อความ — wait = วินาทีที่ยอมให้ server ค้าง request ไว้ (0 = ตอบทันที)
    /// คืน messages ว่าง = หมดเวลา ไม่ใช่ error
    func chatSync(token: String, groupId: Int, after: Int64, wait: Int) async throws -> ChatSyncResponse {
        var path = "\(Config.apiBase)/groups/\(groupId)/chat/sync?wait=\(wait)"
        if after > 0 { path += "&after=\(after)" }
        guard let url = URL(string: path) else { throw AppError.message("URL ไม่ถูกต้อง") }
        var req = URLRequest(url: url)
        req.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
        req.timeoutInterval = TimeInterval(wait) + 10   // ต้องมากกว่า wait ไม่งั้น client ตัดเอง
        let (data, resp): (Data, URLResponse)
        do { (data, resp) = try await Self.send(req) }
        catch { throw AppError.offline }
        guard let http = resp as? HTTPURLResponse else { throw AppError.message("ผิดพลาด") }
        if http.statusCode == 403 { throw AppError.notInGroup }
        guard http.statusCode == 200 else { throw AppError.message("โหลดข้อความไม่สำเร็จ") }
        let dec = JSONDecoder(); dec.keyDecodingStrategy = .convertFromSnakeCase
        return try dec.decode(ChatSyncResponse.self, from: data)
    }

    /// อ่านถึง id ไหน — ใช้เป็น heartbeat "กำลังดูจอแชทอยู่" ด้วย จึงยิงซ้ำค่าเดิมได้
    /// ไม่ throw — ผู้เรียกทำอะไรกับความล้มเหลวไม่ได้อยู่แล้ว (ตัวถัดไปจะยิงซ้ำค่าเดิม/ค่าใหม่กว่าเอง)
    /// พังแล้ว log ไว้เฉยๆ ให้ตามรอยได้ ไม่ใช่หายเงียบ
    func chatRead(token: String, groupId: Int, lastReadId: Int64) async {
        guard let url = URL(string: "\(Config.apiBase)/groups/\(groupId)/chat/read") else {
            NSLog("[chat] chatRead: URL ไม่ถูกต้อง (group \(groupId))")
            return
        }
        guard let body = try? JSONSerialization.data(withJSONObject: ["last_read_id": Int(lastReadId)]) else {
            NSLog("[chat] chatRead: เตรียม body ไม่สำเร็จ (group \(groupId))")
            return
        }
        var req = URLRequest(url: url)
        req.httpMethod = "POST"
        req.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
        req.setValue("application/json", forHTTPHeaderField: "Content-Type")
        req.httpBody = body
        do {
            let (_, resp) = try await Self.send(req)
            let status = (resp as? HTTPURLResponse)?.statusCode ?? -1
            if status != 200 { NSLog("[chat] chatRead: server ตอบ \(status) (group \(groupId))") }
        } catch {
            NSLog("[chat] chatRead: ส่งไม่สำเร็จ (group \(groupId)): \(error)")
        }
    }
}
```

- [ ] **Step 6: Run the tests to verify they pass**

```bash
cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'
```

Expected: `ChatDTOTests` both PASS; existing `FlexibleStringTests` and `QRCodeTests` still PASS.

- [ ] **Step 7: Commit (iOS repo)**

```bash
cd /Users/park/wbw-ios-fontend
git add WBW/Chat/ChatDTOs.swift WBW/APIClient+Chat.swift WBW/Models.swift WBWTests/ChatDTOTests.swift project.yml
git commit -m "feat(chat): DTO + API call สำหรับ chat sync/read"
```

---

### Task 4: Row building — day separators, grouping, unread mark

The pure function that turns a message list into display rows. No view, no network, fully unit-tested.

**Files:**
- Create: `/Users/park/wbw-ios-fontend/WBW/Chat/ChatRow.swift`
- Create: `/Users/park/wbw-ios-fontend/WBWTests/ChatRowTests.swift`

**Interfaces:**
- Consumes: `ChatMessage` (SwiftData model, existing — instantiable in tests without a `ModelContext`).
- Produces: `extension ChatMessage { var displayTime: Date }`; `enum ChatRow` with `case day(Date)`, `case unreadMark`, `case message(ChatMessage, ChatRow.Layout)` and `ChatRow.Layout { isFirstInGroup, isLastInGroup, showTime }`; `enum ChatRowBuilder { static let groupingWindow: TimeInterval; static func build(_:myLastReadId:myId:calendar:) -> [ChatRow] }`; `enum ChatFormat { static func dayLabel(for:now:calendar:) -> String; static func time(_:) -> String }`.

- [ ] **Step 1: Write the failing tests**

Create `/Users/park/wbw-ios-fontend/WBWTests/ChatRowTests.swift`:

```swift
import XCTest
@testable import WBW

final class ChatRowTests: XCTestCase {
    private let me = "me"
    private var cal: Calendar = {
        var c = Calendar(identifier: .gregorian)
        c.timeZone = TimeZone(identifier: "Asia/Bangkok")!
        return c
    }()

    /// ข้อความปลอม — serverId ใส่เองเพื่อคุมสถานะอ่าน
    private func msg(_ id: Int64?, _ sender: String, _ at: Date, _ body: String = "x") -> ChatMessage {
        ChatMessage(clientId: "c\(id ?? -1)-\(sender)-\(at.timeIntervalSince1970)",
                    serverId: id, groupId: 1, senderId: sender, body: body,
                    deviceTime: at, createdAt: at, senderName: sender,
                    state: id == nil ? .pending : .sent)
    }

    private func date(_ iso: String) -> Date {
        let f = ISO8601DateFormatter()
        f.timeZone = TimeZone(identifier: "Asia/Bangkok")
        return f.date(from: iso)!
    }

    func testEmptyListProducesNoRows() {
        XCTAssertTrue(ChatRowBuilder.build([], myLastReadId: 0, myId: me, calendar: cal).isEmpty)
    }

    func testInsertsOneDayPillPerDay() {
        let rows = ChatRowBuilder.build([
            msg(1, "a", date("2026-07-30T23:50:00+07:00")),
            msg(2, "a", date("2026-07-31T00:05:00+07:00")),
        ], myLastReadId: 99, myId: me, calendar: cal)
        let days = rows.filter { if case .day = $0 { return true } else { return false } }
        XCTAssertEqual(days.count, 2)
    }

    func testSameDayProducesOneDayPill() {
        let rows = ChatRowBuilder.build([
            msg(1, "a", date("2026-07-31T09:00:00+07:00")),
            msg(2, "a", date("2026-07-31T09:01:00+07:00")),
        ], myLastReadId: 99, myId: me, calendar: cal)
        let days = rows.filter { if case .day = $0 { return true } else { return false } }
        XCTAssertEqual(days.count, 1)
    }

    func testGroupsSameSenderWithinFiveMinutes() {
        let rows = ChatRowBuilder.build([
            msg(1, "a", date("2026-07-31T09:00:00+07:00")),
            msg(2, "a", date("2026-07-31T09:04:00+07:00")),
        ], myLastReadId: 99, myId: me, calendar: cal)
        let layouts = rows.compactMap { if case let .message(_, l) = $0 { return l } else { return nil } }
        XCTAssertEqual(layouts.map(\.isFirstInGroup), [true, false])
        XCTAssertEqual(layouts.map(\.isLastInGroup), [false, true])
        XCTAssertEqual(layouts.map(\.showTime), [false, true])
    }

    func testBreaksGroupAfterFiveMinutes() {
        let rows = ChatRowBuilder.build([
            msg(1, "a", date("2026-07-31T09:00:00+07:00")),
            msg(2, "a", date("2026-07-31T09:06:00+07:00")),
        ], myLastReadId: 99, myId: me, calendar: cal)
        let layouts = rows.compactMap { if case let .message(_, l) = $0 { return l } else { return nil } }
        XCTAssertEqual(layouts.map(\.isFirstInGroup), [true, true])
    }

    func testBreaksGroupOnSenderChange() {
        let rows = ChatRowBuilder.build([
            msg(1, "a", date("2026-07-31T09:00:00+07:00")),
            msg(2, "b", date("2026-07-31T09:00:30+07:00")),
        ], myLastReadId: 99, myId: me, calendar: cal)
        let layouts = rows.compactMap { if case let .message(_, l) = $0 { return l } else { return nil } }
        XCTAssertEqual(layouts.map(\.isFirstInGroup), [true, true])
    }

    func testBreaksGroupOnDayChangeEvenWithinFiveMinutes() {
        // ห่างกันแค่ 3 นาที (ในหน้าต่างจับกลุ่ม) แต่ข้ามเที่ยงคืน — ต้องเริ่มชุดใหม่เพราะเงื่อนไข sameDay
        let rows = ChatRowBuilder.build([
            msg(1, "a", date("2026-07-30T23:58:00+07:00")),
            msg(2, "a", date("2026-07-31T00:01:00+07:00")),
        ], myLastReadId: 99, myId: me, calendar: cal)
        let layouts = rows.compactMap { if case let .message(_, l) = $0 { return l } else { return nil } }
        XCTAssertEqual(layouts.map(\.isFirstInGroup), [true, true])
    }

    func testGroupsAtExactlyFiveMinuteBoundary() {
        // ห่างกันพอดี 300 วินาที — เงื่อนไข <= ต้องยังนับว่าจับกลุ่มกัน
        let rows = ChatRowBuilder.build([
            msg(1, "a", date("2026-07-31T09:00:00+07:00")),
            msg(2, "a", date("2026-07-31T09:05:00+07:00")),
        ], myLastReadId: 99, myId: me, calendar: cal)
        let layouts = rows.compactMap { if case let .message(_, l) = $0 { return l } else { return nil } }
        XCTAssertEqual(layouts.map(\.isFirstInGroup), [true, false])
    }

    func testUnreadMarkSitsBeforeFirstUnreadFromOthers() {
        let rows = ChatRowBuilder.build([
            msg(1, "a", date("2026-07-31T09:00:00+07:00")),
            msg(2, "a", date("2026-07-31T09:10:00+07:00")),
        ], myLastReadId: 1, myId: me, calendar: cal)
        guard let markIndex = rows.firstIndex(where: { if case .unreadMark = $0 { return true } else { return false } })
        else { return XCTFail("ไม่มีเส้นข้อความใหม่") }
        // แถวถัดจากเส้นต้องเป็นข้อความ id 2
        guard case let .message(m, _) = rows[markIndex + 1] else { return XCTFail("แถวถัดไปไม่ใช่ข้อความ") }
        XCTAssertEqual(m.serverId, 2)
    }

    func testNoUnreadMarkWhenEverythingRead() {
        let rows = ChatRowBuilder.build([
            msg(1, "a", date("2026-07-31T09:00:00+07:00")),
        ], myLastReadId: 5, myId: me, calendar: cal)
        XCTAssertFalse(rows.contains { if case .unreadMark = $0 { return true } else { return false } })
    }

    func testMyOwnUnreadMessagesDoNotTriggerTheMark() {
        let rows = ChatRowBuilder.build([
            msg(9, me, date("2026-07-31T09:00:00+07:00")),
        ], myLastReadId: 0, myId: me, calendar: cal)
        XCTAssertFalse(rows.contains { if case .unreadMark = $0 { return true } else { return false } })
    }

    func testPendingMessageStillRenders() {
        let rows = ChatRowBuilder.build([
            msg(nil, me, date("2026-07-31T09:00:00+07:00")),
        ], myLastReadId: 0, myId: me, calendar: cal)
        XCTAssertEqual(rows.compactMap { if case .message = $0 { return true } else { return nil } }.count, 1)
    }

    // ===== ป้ายวัน / เวลา =====

    func testDayLabelToday() {
        let now = date("2026-07-31T18:00:00+07:00")
        XCTAssertEqual(ChatFormat.dayLabel(for: date("2026-07-31T09:00:00+07:00"), now: now, calendar: cal), "วันนี้")
    }

    func testDayLabelYesterday() {
        let now = date("2026-07-31T18:00:00+07:00")
        XCTAssertEqual(ChatFormat.dayLabel(for: date("2026-07-30T09:00:00+07:00"), now: now, calendar: cal), "เมื่อวาน")
    }

    func testDayLabelOlderThisYearHasNoYear() {
        let now = date("2026-07-31T18:00:00+07:00")
        let label = ChatFormat.dayLabel(for: date("2026-03-02T09:00:00+07:00"), now: now, calendar: cal)
        XCTAssertFalse(label.contains("25"), "ปีนี้ไม่ต้องมีปี: \(label)")
    }

    func testDayLabelPreviousYearHasBuddhistYear() {
        let now = date("2026-07-31T18:00:00+07:00")
        let label = ChatFormat.dayLabel(for: date("2025-12-31T09:00:00+07:00"), now: now, calendar: cal)
        XCTAssertTrue(label.contains("2568"), "ปีก่อนต้องมี พ.ศ.: \(label)")
    }

    func testTimeIs24Hour() {
        XCTAssertEqual(ChatFormat.time(date("2026-07-31T21:07:00+07:00"),
                                       timeZone: TimeZone(identifier: "Asia/Bangkok")!), "21:07")
    }
}
```

- [ ] **Step 2: Run the tests to verify they fail**

```bash
cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'
```

Expected: compile failure — `cannot find 'ChatRowBuilder' in scope`.

- [ ] **Step 3: Create `WBW/Chat/ChatRow.swift`**

```swift
import Foundation

extension ChatMessage {
    /// เวลาที่ใช้แสดง/จัดกลุ่ม — เวลา server ถ้ามี ไม่งั้นนาฬิกาเครื่อง (ข้อความที่ยังไม่ส่ง)
    var displayTime: Date { createdAt ?? deviceTime }
}

/// แถวในลิสต์แชท — ข้อความ, ป้ายคั่นวัน, เส้น "ข้อความใหม่"
enum ChatRow: Identifiable {
    struct Layout: Equatable {
        var isFirstInGroup: Bool   // ฟองแรกของชุด → โชว์ avatar + ชื่อ
        var isLastInGroup: Bool    // ฟองสุดท้ายของชุด → มีหาง
        var showTime: Bool         // โชว์เวลาใต้ฟอง
    }

    case day(Date)
    case unreadMark
    case message(ChatMessage, Layout)

    var id: String {
        switch self {
        case let .day(d):        return "day-\(Int(d.timeIntervalSince1970))"
        case .unreadMark:        return "unread-mark"
        case let .message(m, _): return m.clientId
        }
    }
}

enum ChatRowBuilder {
    /// ข้อความคนเดียวกันห่างไม่เกินนี้ = ชุดเดียวกัน
    static let groupingWindow: TimeInterval = 300

    /// สร้างแถวสำหรับแสดงผล — บริสุทธิ์ ไม่มี view/network เทสได้ตรงๆ
    /// - Parameters:
    ///   - msgs: เรียงจากเก่าไปใหม่แล้ว (ChatSession จัดให้)
    ///   - myLastReadId: อ่านถึง id ไหน — ใช้วางเส้น "ข้อความใหม่"
    ///   - myId: userId ของเรา — ข้อความตัวเองไม่ทำให้เกิดเส้นข้อความใหม่
    static func build(_ msgs: [ChatMessage],
                      myLastReadId: Int64,
                      myId: String,
                      calendar: Calendar = .current) -> [ChatRow] {
        guard !msgs.isEmpty else { return [] }

        // รอบแรก: ข้อความไหนเริ่มชุดใหม่
        var starts = [Bool](repeating: true, count: msgs.count)
        for i in 1..<msgs.count {
            let prev = msgs[i - 1], cur = msgs[i]
            let sameSender = prev.senderId == cur.senderId
            let close = cur.displayTime.timeIntervalSince(prev.displayTime) <= groupingWindow
            let sameDay = calendar.isDate(prev.displayTime, inSameDayAs: cur.displayTime)
            starts[i] = !(sameSender && close && sameDay)
        }

        // ข้อความแรกที่ยังไม่อ่านและไม่ใช่ของเรา
        // serverId เป็น nil ได้เฉพาะข้อความที่เรายังไม่ส่งเอง (ChatStore การันตี) — ข้อความของคนอื่น
        // มี serverId เสมอ ดังนั้น ?? 0 ตรงนี้ไม่มีทางทำให้ข้อความคนอื่นถูกนับว่ายังไม่อ่านผิดๆ
        let firstUnread = msgs.firstIndex {
            $0.senderId != myId && ($0.serverId ?? 0) > myLastReadId
        }

        var rows: [ChatRow] = []
        var lastDay: Date?
        for i in msgs.indices {
            let m = msgs[i]
            let day = calendar.startOfDay(for: m.displayTime)
            if lastDay != day {
                rows.append(.day(day))
                lastDay = day
            }
            if i == firstUnread { rows.append(.unreadMark) }
            let isLast = (i == msgs.count - 1) || starts[i + 1]
            rows.append(.message(m, .init(isFirstInGroup: starts[i],
                                          isLastInGroup: isLast,
                                          showTime: isLast)))
        }
        return rows
    }
}

/// ข้อความบนป้ายวัน + เวลาใต้ฟอง
enum ChatFormat {
    static func dayLabel(for day: Date, now: Date = Date(), calendar: Calendar = .current) -> String {
        if calendar.isDate(day, inSameDayAs: now) { return "วันนี้" }
        if let yesterday = calendar.date(byAdding: .day, value: -1, to: now),
           calendar.isDate(day, inSameDayAs: yesterday) { return "เมื่อวาน" }
        let sameYear = calendar.component(.year, from: day) == calendar.component(.year, from: now)
        let f = DateFormatter()
        f.locale = Locale(identifier: "th_TH")   // ได้ พ.ศ. อัตโนมัติ
        f.timeZone = calendar.timeZone
        f.dateFormat = sameYear ? "d MMM" : "d MMM yyyy"
        return f.string(from: day)
    }

    /// 24 ชั่วโมงเสมอ ไม่ตามการตั้งค่าเครื่อง (en_US_POSIX กัน AM/PM)
    static func time(_ date: Date, timeZone: TimeZone = .current) -> String {
        let f = DateFormatter()
        f.locale = Locale(identifier: "en_US_POSIX")
        f.timeZone = timeZone
        f.dateFormat = "HH:mm"
        return f.string(from: date)
    }
}
```

- [ ] **Step 4: Run the tests to verify they pass**

```bash
cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'
```

Expected: all `ChatRowTests` PASS.

- [ ] **Step 5: Commit**

```bash
cd /Users/park/wbw-ios-fontend
git add WBW/Chat/ChatRow.swift WBWTests/ChatRowTests.swift
git commit -m "feat(chat): สร้างแถวแชท — ป้ายคั่นวัน จับกลุ่ม เส้นข้อความใหม่"
```

---

### Task 5: `ChatSession` — app-lifetime engine with long-poll

Replaces `ChatStore`. Moves ownership to `MainTabView` so the engine keeps running with the chat screen closed, switches polling to long-poll, purges cached messages below the cut-off, and tracks cursors and unread count.

**Files:**
- Create: `/Users/park/wbw-ios-fontend/WBW/Chat/ChatSession.swift`
- Delete: `/Users/park/wbw-ios-fontend/WBW/ChatStore.swift`
- Create: `/Users/park/wbw-ios-fontend/WBWTests/ChatSessionTests.swift`
- Modify: `/Users/park/wbw-ios-fontend/WBW/MainTabView.swift` (owns the session; chat overlay takes it as a parameter)
- Modify: `/Users/park/wbw-ios-fontend/WBW/GroupChatView.swift` (accept the session instead of building one)

**Interfaces:**
- Consumes: `ChatSyncResponse`, `ReadCursor`, `APIClient.chatSync`, `APIClient.chatRead`, `Connectivity`, `ChatMessage`, `MessageDTO`, `AppError.notInGroup`.
- Produces: `@MainActor final class ChatSession: ObservableObject` with published `messages: [ChatMessage]`, `cursors: [ReadCursor]`, `memberCount: Int`, `unreadCount: Int`, `myLastReadId: Int64`; methods `configure(groupId:token:myId:context:)`, `start()`, `stop()`, `send(_:senderName:)`, `retry(_:)`, `markRead()`, `setScreenVisible(_:)`, `isMine(_:)`, `readCount(for:) -> Int`; and `static func unreadCount(messages:myLastReadId:myId:) -> Int` plus `static func readCount(for:cursors:) -> Int` as testable pure helpers.

- [ ] **Step 1: Write the failing tests**

Create `/Users/park/wbw-ios-fontend/WBWTests/ChatSessionTests.swift`:

```swift
import XCTest
@testable import WBW

final class ChatSessionTests: XCTestCase {
    private func msg(_ id: Int64?, _ sender: String) -> ChatMessage {
        ChatMessage(clientId: "c\(id ?? -1)-\(sender)", serverId: id, groupId: 1,
                    senderId: sender, body: "x", deviceTime: Date(), createdAt: Date(),
                    senderName: sender, state: id == nil ? .pending : .sent)
    }

    func testUnreadCountsOnlyOthersMessagesAboveCursor() {
        let msgs = [msg(1, "a"), msg(2, "me"), msg(3, "b"), msg(nil, "me")]
        XCTAssertEqual(ChatSession.unreadCount(messages: msgs, myLastReadId: 1, myId: "me"), 1)
        XCTAssertEqual(ChatSession.unreadCount(messages: msgs, myLastReadId: 0, myId: "me"), 2)
        XCTAssertEqual(ChatSession.unreadCount(messages: msgs, myLastReadId: 9, myId: "me"), 0)
    }

    func testReadCountCountsCursorsAtOrAboveMessage() {
        let cursors = [ReadCursor(userId: "a", lastReadId: 5),
                       ReadCursor(userId: "b", lastReadId: 3),
                       ReadCursor(userId: "c", lastReadId: 0)]
        XCTAssertEqual(ChatSession.readCount(for: msg(3, "me"), cursors: cursors), 2)
        XCTAssertEqual(ChatSession.readCount(for: msg(5, "me"), cursors: cursors), 1)
        XCTAssertEqual(ChatSession.readCount(for: msg(6, "me"), cursors: cursors), 0)
    }

    func testReadCountIsZeroForUnsentMessage() {
        let cursors = [ReadCursor(userId: "a", lastReadId: 99)]
        XCTAssertEqual(ChatSession.readCount(for: msg(nil, "me"), cursors: cursors), 0)
    }

    func testPurgePredicateDropsSentMessagesAtOrBelowCutoffButKeepsPending() {
        let msgs = [msg(1, "a"), msg(5, "a"), msg(6, "a"), msg(nil, "me")]
        let kept = msgs.filter { ChatSession.survivesCutoff($0, sinceId: 5) }
        XCTAssertEqual(kept.compactMap(\.serverId), [6])
        XCTAssertEqual(kept.count, 2, "ข้อความที่ยังไม่ส่งต้องอยู่ต่อ")
    }
}
```

- [ ] **Step 2: Run the tests to verify they fail**

```bash
cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'
```

Expected: compile failure — `cannot find 'ChatSession' in scope`.

- [ ] **Step 3: Create `WBW/Chat/ChatSession.swift`**

```swift
import Foundation
import SwiftData

/// เครื่องยนต์แชท — อายุเท่าแอป (MainTabView ถือไว้) ไม่ใช่เท่าจอแชท
/// offline-first: cache + outbox (SwiftData), optimistic send, long-poll, flush ตอน reconnect
@MainActor
final class ChatSession: ObservableObject {
    @Published private(set) var messages: [ChatMessage] = []
    @Published private(set) var cursors: [ReadCursor] = []
    @Published private(set) var memberCount = 0
    @Published private(set) var unreadCount = 0
    @Published private(set) var myLastReadId: Int64 = 0
    /// ข้อความล่าสุดที่เพิ่งเข้ามาตอนไม่ได้เปิดจอแชท — ใช้เด้ง toast
    @Published var incoming: ChatMessage?

    let connectivity = Connectivity()

    private var context: ModelContext?
    private var groupId: Int?
    private var token = ""
    private var myId = ""
    private var cursor: Int64 = 0            // serverId สูงสุดที่มีแล้ว
    private var screenVisible = false
    private var syncTask: Task<Void, Never>?
    private var heartbeatTask: Task<Void, Never>?
    private var readDebounce: Task<Void, Never>?

    private var cursorKey: String { "chat.cursor.\(groupId ?? 0)" }
    private var readKey: String { "chat.read.\(groupId ?? 0)" }

    // ===== ค่าที่ view ใช้ =====

    func isMine(_ m: ChatMessage) -> Bool { m.senderId == myId }
    func readCount(for m: ChatMessage) -> Int { Self.readCount(for: m, cursors: cursors) }

    /// นับเฉพาะข้อความคนอื่นที่เกิน cursor เรา
    static func unreadCount(messages: [ChatMessage], myLastReadId: Int64, myId: String) -> Int {
        messages.filter { $0.senderId != myId && ($0.serverId ?? 0) > myLastReadId }.count
    }

    /// กี่คนอ่านข้อความนี้แล้ว — cursors ไม่รวมตัวเราอยู่แล้ว (server ตัดให้)
    static func readCount(for m: ChatMessage, cursors: [ReadCursor]) -> Int {
        guard let sid = m.serverId else { return 0 }   // ยังไม่ส่ง = ยังไม่มีใครอ่าน
        return cursors.filter { $0.lastReadId >= sid }.count
    }

    /// ข้อความนี้รอดจากจุดตัดประวัติมั้ย — ที่ยังไม่ส่ง (serverId = nil) รอดเสมอ
    static func survivesCutoff(_ m: ChatMessage, sinceId: Int64) -> Bool {
        guard let sid = m.serverId else { return true }
        return sid > sinceId
    }

    // ===== วงจรชีวิต =====

    /// ตั้งกลุ่ม/ผู้ใช้ — เรียกซ้ำได้ ถ้าค่าเดิมจะไม่รีสตาร์ต
    func configure(groupId: Int?, token: String, myId: String, context: ModelContext) {
        self.context = context
        self.token = token
        self.myId = myId
        guard self.groupId != groupId else { return }

        stop()
        self.groupId = groupId
        guard groupId != nil else {
            messages = []; cursors = []; memberCount = 0; unreadCount = 0; myLastReadId = 0
            return
        }
        cursor = Int64(UserDefaults.standard.integer(forKey: cursorKey))
        myLastReadId = Int64(UserDefaults.standard.integer(forKey: readKey))
        loadCache()
        connectivity.onReconnect = { [weak self] in
            guard let self else { return }
            Task { await self.flushOutbox(); self.start() }
        }
        start()
    }

    func start() {
        guard groupId != nil, syncTask == nil else { return }
        syncTask = Task { [weak self] in
            await self?.flushOutbox()
            await self?.syncLoop()
        }
    }

    func stop() {
        syncTask?.cancel(); syncTask = nil
        heartbeatTask?.cancel(); heartbeatTask = nil
        readDebounce?.cancel(); readDebounce = nil
    }

    /// จอแชทเปิด/ปิด — คุม heartbeat และการเด้ง toast
    func setScreenVisible(_ visible: Bool) {
        screenVisible = visible
        heartbeatTask?.cancel(); heartbeatTask = nil
        guard visible else { return }
        markRead()
        // heartbeat: บอก server ว่ายังจ้อจออยู่ ไม่งั้นโดน push ทั้งที่กำลังอ่าน
        heartbeatTask = Task { [weak self] in
            while !Task.isCancelled {
                try? await Task.sleep(nanoseconds: 10_000_000_000)
                guard let self, !Task.isCancelled else { return }
                await self.postRead(self.myLastReadId)
            }
        }
    }

    // ===== ส่งข้อความ (เหมือนเดิม) =====

    func send(_ text: String, senderName: String) {
        let t = text.trimmingCharacters(in: .whitespacesAndNewlines)
        guard !t.isEmpty, let gid = groupId, let context else { return }
        let msg = ChatMessage(clientId: UUID().uuidString, serverId: nil, groupId: gid,
                              senderId: myId, body: t, deviceTime: Date(), createdAt: nil,
                              senderName: senderName, state: .pending)
        context.insert(msg)
        try? context.save()
        messages = sorted(messages + [msg])
        Task { await flushOutbox() }
    }

    func retry(_ m: ChatMessage) {
        m.state = .pending
        try? context?.save()
        messages = sorted(messages)
        Task { await flushOutbox() }
    }

    // ===== สถานะอ่าน =====

    /// อ่านถึงข้อความล่าสุด — หน่วง 500ms กันยิงรัวตอนข้อความไหลเข้า
    func markRead() {
        let maxId = messages.compactMap(\.serverId).max() ?? 0
        guard maxId > myLastReadId else { return }
        myLastReadId = maxId
        UserDefaults.standard.set(Int(maxId), forKey: readKey)
        recomputeUnread()
        readDebounce?.cancel()
        readDebounce = Task { [weak self] in
            try? await Task.sleep(nanoseconds: 500_000_000)
            guard let self, !Task.isCancelled else { return }
            await self.postRead(self.myLastReadId)
        }
    }

    private func postRead(_ id: Int64) async {
        guard let gid = groupId, !token.isEmpty else { return }
        await APIClient.shared.chatRead(token: token, groupId: gid, lastReadId: id)   // ไม่ throw
    }

    // ===== เครื่องยนต์ =====

    private func loadCache() {
        guard let context, let gid = groupId else { return }
        let desc = FetchDescriptor<ChatMessage>(predicate: #Predicate { $0.groupId == gid })
        messages = sorted((try? context.fetch(desc)) ?? [])
        recomputeUnread()
    }

    private func syncLoop() async {
        var backoff: UInt64 = 1
        while !Task.isCancelled {
            guard let gid = groupId, !token.isEmpty else { return }
            guard connectivity.online else {
                try? await Task.sleep(nanoseconds: 2_000_000_000)
                continue
            }
            do {
                let r = try await APIClient.shared.chatSync(token: token, groupId: gid,
                                                            after: cursor, wait: 25)
                apply(r)
                if messages.contains(where: { $0.state == .pending }) { await flushOutbox() }
                backoff = 1                     // สำเร็จ = วนต่อทันที (หมดเวลา = messages ว่าง ไม่ใช่ error)
            } catch AppError.notInGroup {
                purgeAll()
                return                          // โดนเอาออกจากกลุ่ม — หยุด loop
            } catch {
                try? await Task.sleep(nanoseconds: backoff * 1_000_000_000)
                backoff = min(backoff * 2, 10)
            }
        }
    }

    private func apply(_ r: ChatSyncResponse) {
        if r.sinceId > 0 { purge(upTo: r.sinceId) }
        memberCount = r.memberCount
        cursors = r.cursors
        let fresh = merge(r.messages)
        recomputeUnread()
        if !screenVisible, let last = fresh.last(where: { $0.senderId != myId }) {
            incoming = last            // ให้ MainTabView เด้ง toast
        }
        if screenVisible { markRead() }
    }

    /// server บอกว่าเห็นได้แค่ > sinceId → ของเก่าในเครื่องลบทิ้ง (เข้ากลุ่มใหม่ = ตัดประวัติ)
    private func purge(upTo sinceId: Int64) {
        let stale = messages.filter { !Self.survivesCutoff($0, sinceId: sinceId) }
        guard !stale.isEmpty, let context else { return }
        for m in stale { context.delete(m) }
        try? context.save()
        messages = messages.filter { Self.survivesCutoff($0, sinceId: sinceId) }
    }

    private func purgeAll() {
        guard let context else { return }
        for m in messages { context.delete(m) }
        try? context.save()
        messages = []; cursors = []; unreadCount = 0
    }

    /// รวมข้อความจาก server — dedupe ตาม clientId. คืนเฉพาะอันที่เพิ่งเข้ามาใหม่จริงๆ
    @discardableResult
    private func merge(_ dtos: [MessageDTO]) -> [ChatMessage] {
        guard let context, let gid = groupId else { return [] }
        var fresh: [ChatMessage] = []
        for dto in dtos {
            guard let sid = Int64(dto.id) else { continue }
            if let existing = messages.first(where: { $0.clientId == dto.clientId }) {
                if existing.serverId == nil {
                    existing.serverId = sid
                    existing.createdAt = parseISO(dto.createdAt)
                    existing.state = .sent
                }
            } else if !messages.contains(where: { $0.serverId == sid }) {
                let m = ChatMessage(clientId: dto.clientId, serverId: sid, groupId: gid,
                                    senderId: dto.senderId, body: dto.body,
                                    deviceTime: parseISO(dto.deviceTime) ?? Date(),
                                    createdAt: parseISO(dto.createdAt),
                                    senderName: dto.senderName, state: .sent)
                context.insert(m)
                messages.append(m)
                fresh.append(m)
            }
            if sid > cursor { cursor = sid }
        }
        UserDefaults.standard.set(Int(cursor), forKey: cursorKey)
        try? context.save()
        messages = sorted(messages)
        return fresh
    }

    /// ส่ง pending ตามลำดับ — เน็ตล่ม=หยุด (retry รอบหน้า), 4xx=mark failed แล้วทำต่อ
    private func flushOutbox() async {
        guard let gid = groupId, !token.isEmpty, let context else { return }
        let pending = messages.filter { $0.state == .pending }.sorted { $0.deviceTime < $1.deviceTime }
        for m in pending {
            do {
                let dto = try await APIClient.shared.sendMessage(
                    token: token, groupId: gid, clientId: m.clientId,
                    body: m.body, deviceTime: iso(m.deviceTime))
                m.serverId = Int64(dto.id) ?? m.serverId
                m.createdAt = parseISO(dto.createdAt)
                m.state = .sent
                if let sid = m.serverId, sid > cursor {
                    cursor = sid
                    UserDefaults.standard.set(Int(cursor), forKey: cursorKey)
                }
                try? context.save()
                messages = sorted(messages)
            } catch AppError.offline {
                break
            } catch {
                m.state = .failed
                try? context.save()
                messages = sorted(messages)
            }
        }
    }

    private func recomputeUnread() {
        unreadCount = Self.unreadCount(messages: messages, myLastReadId: myLastReadId, myId: myId)
    }

    // เรียงตาม serverId (นาฬิกาเครื่องเพี้ยนได้ — pending ต่อท้าย)
    private func sorted(_ arr: [ChatMessage]) -> [ChatMessage] {
        arr.sorted { a, b in
            switch (a.serverId, b.serverId) {
            case let (x?, y?): return x < y
            case (_?, nil):    return true
            case (nil, _?):    return false
            case (nil, nil):   return a.deviceTime < b.deviceTime
            }
        }
    }

    private let isoFmt: ISO8601DateFormatter = {
        let f = ISO8601DateFormatter()
        f.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
        return f
    }()
    private func iso(_ d: Date) -> String { isoFmt.string(from: d) }
    private func parseISO(_ s: String?) -> Date? {
        guard let s else { return nil }
        return isoFmt.date(from: s) ?? ISO8601DateFormatter().date(from: s)
    }
}
```

- [ ] **Step 4: Delete the old store**

```bash
cd /Users/park/wbw-ios-fontend && git rm WBW/ChatStore.swift
```

- [ ] **Step 5: Hand ownership to `MainTabView`**

In `/Users/park/wbw-ios-fontend/WBW/MainTabView.swift`, add `@Environment(\.scenePhase)`
and the session next to the existing `@StateObject private var noti = NotiStore()`:

```swift
    @Environment(\.scenePhase) private var scenePhase
    @StateObject private var chat = ChatSession()
```

Replace the chat overlay block with one that passes the session in:

```swift
            // แชทกลุ่ม — overlay เลื่อนขึ้นจากล่าง (navbar หายแบบเด้งๆ)
            if chatOpen, profile.me?.groupId != nil {
                GroupChatView(store: chat, onClose: { chatOpen = false })
                    .transition(.move(edge: .bottom).combined(with: .opacity))
                    .zIndex(1)
            }
```

Extend the existing `.task { ... }` on the `TabView` so it configures the session
after the profile loads (add these lines at the end of the closure, before the
`#if DEBUG` block):

```swift
                chat.configure(groupId: profile.me?.groupId, token: session.token ?? "",
                               myId: profile.me?.userId ?? "", context: context)
```

and add these modifiers next to the existing `.onReceive(...)`:

```swift
            .onChange(of: profile.me?.groupId) { _, gid in
                chat.configure(groupId: gid, token: session.token ?? "",
                               myId: profile.me?.userId ?? "", context: context)
            }
            .onChange(of: scenePhase) { _, phase in
                // background = iOS แขวน connection อยู่ดี ปล่อยให้ push รับช่วง
                phase == .active ? chat.start() : chat.stop()
            }
```

- [ ] **Step 6: Make `GroupChatView` take the session**

In `/Users/park/wbw-ios-fontend/WBW/GroupChatView.swift`, replace the stored
properties and `init` (lines 11–21) with:

```swift
    @ObservedObject var store: ChatSession
    let onClose: () -> Void
    @State private var draft = ""
    @State private var members: [String: GroupMember] = [:]   // senderId → member (avatar)
```

In the same file, replace the `.task { ... }` / `.onDisappear { ... }` pair with:

```swift
        .task {
            store.setScreenVisible(true)
            let gid = profile.me?.groupId ?? 0
            let ms = await groups.members(groupId: gid, token: session.token ?? "")
            members = Dictionary(uniqueKeysWithValues: ms.map { ($0.userId, $0) })
        }
        .onDisappear { store.setScreenVisible(false) }
```

Also change the header's member count from `members.count` to `store.memberCount`:

```swift
                Text("\(store.memberCount) คน").font(.system(size: 12)).foregroundStyle(.secondary)
```

- [ ] **Step 7: Run the tests to verify they pass**

```bash
cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'
```

Expected: all `ChatSessionTests` PASS, everything else still PASSes, and the app builds.

- [ ] **Step 8: Verify against the live backend by hand**

With the backend from Tasks 1–2 running and `Config.backend = .prodNode` pointed
at it, run the app on the simulator, open the group chat, and send a message from
the smoke script's user A:

```bash
cd /Users/park/Desktop/WBW/backend && node -e "
const BASE=process.env.API_BASE; const U=process.env.U1, P=process.env.P1, G=process.env.GROUP_ID;
const r = await (await fetch(BASE+'/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:U,password:P})})).json();
await fetch(BASE+'/groups/'+G+'/messages',{method:'POST',headers:{authorization:'Bearer '+r.token,'content-type':'application/json'},body:JSON.stringify({client_id:crypto.randomUUID(),body:'ทดสอบ long-poll',device_time:new Date().toISOString()})});
console.log('ส่งแล้ว');
"
```

Expected: the message appears in the simulator in under a second.

- [ ] **Step 9: Commit**

```bash
cd /Users/park/wbw-ios-fontend
git add WBW/Chat/ChatSession.swift WBW/MainTabView.swift WBW/GroupChatView.swift WBWTests/ChatSessionTests.swift
git commit -m "feat(chat): ChatSession อายุเท่าแอป + long-poll + ล้าง cache ตามจุดตัด"
```

---

**As built.** The shipped `ChatSession` differs from the snippet above in five reviewed ways, all in the code: the three static helpers are `nonisolated` (a `@MainActor` class propagates isolation to its statics, so the snippet as written could not compile against its own tests); `MainTabView` also carries `.onDisappear { chat.stop() }`, because `logout()` never changes `profile.me` and the sync `Task` retains `self` for the whole loop, so without it the loop is orphaned on a stale token; `purgeAll()` clears `memberCount`/`myLastReadId` and nils `syncTask` so the loop can restart; the group id is captured at request time and the response dropped if it no longer matches; and the routine per-cycle `NSLog` instrumentation is `#if DEBUG`-gated while the error paths stay unconditional.

Verified against the live backend, not just unit-tested: 29 ms from the server's `created_at` to the message rendering; fifteen consecutive 25-second timeouts re-issued with a 0–13 ms gap and no backoff ladder; a joiner seeing none of the 127 messages that preceded them; and the heartbeat ticking at 10.06–10.68 s while the chat is open, then silent for 104.6 s after it closes while four sync cycles keep running alongside — which is what rules out a dropped network as the explanation.

---

### Task 6: Chat UI — times, day pills, grouping, tails

Rewrites the message list to render `ChatRow`s: day pills, grouped bubbles with tails, and per-group times.

**Files:**
- Create: `/Users/park/wbw-ios-fontend/WBW/Chat/ChatBubble.swift`
- Modify: `/Users/park/wbw-ios-fontend/WBW/GroupChatView.swift` (`messageList`, and delete the private `MessageBubble` at the bottom)

**Interfaces:**
- Consumes: `ChatRow`, `ChatRowBuilder`, `ChatFormat`, `ChatSession`, `ProfileAvatar`, `Color.wbwGold`, `Color.wbwInk`.
- Produces: `struct BubbleShape: Shape` (init `(isMine: Bool, hasTail: Bool)`); `struct ChatBubble: View` (init `(message:isMine:layout:photoUrl:onRetry:)`); `struct ChatDayPill: View` (init `(day: Date)`); `struct ChatUnreadDivider: View`.

- [ ] **Step 1: Create `WBW/Chat/ChatBubble.swift`**

```swift
import SwiftUI

/// ฟองข้อความ — มุมมน มีหางเฉพาะฟองสุดท้ายของชุด (ท่า iMessage)
struct BubbleShape: Shape {
    let isMine: Bool
    let hasTail: Bool
    private let radius: CGFloat = 18
    private let tail: CGFloat = 6

    func path(in rect: CGRect) -> Path {
        let body = rect.insetBy(dx: 0, dy: 0)
        var p = Path(roundedRect: body, cornerRadius: radius)
        guard hasTail else { return p }
        // หางสามเหลี่ยมมนที่มุมล่างฝั่งคนพูด
        let y = body.maxY
        let x = isMine ? body.maxX : body.minX
        let dir: CGFloat = isMine ? 1 : -1
        var t = Path()
        t.move(to: CGPoint(x: x - dir * radius, y: y))
        t.addQuadCurve(to: CGPoint(x: x + dir * tail, y: y),
                       control: CGPoint(x: x, y: y))
        t.addQuadCurve(to: CGPoint(x: x - dir * radius * 0.6, y: y - radius * 0.5),
                       control: CGPoint(x: x - dir * radius * 0.1, y: y - radius * 0.2))
        t.closeSubpath()
        p.addPath(t)
        return p
    }
}

/// ป้ายคั่นวัน
struct ChatDayPill: View {
    let day: Date
    var body: some View {
        Text(ChatFormat.dayLabel(for: day))
            .font(.system(size: 11, weight: .semibold))
            .foregroundStyle(.secondary)
            .padding(.horizontal, 12).padding(.vertical, 5)
            .background(Color.black.opacity(0.05), in: Capsule())
            .frame(maxWidth: .infinity)
            .padding(.vertical, 10)
    }
}

/// เส้น "ข้อความใหม่"
struct ChatUnreadDivider: View {
    var body: some View {
        HStack(spacing: 8) {
            Rectangle().fill(Color.red.opacity(0.25)).frame(height: 1)
            Text("ข้อความใหม่")
                .font(.system(size: 10, weight: .semibold))
                .foregroundStyle(Color.red.opacity(0.7))
            Rectangle().fill(Color.red.opacity(0.25)).frame(height: 1)
        }
        .padding(.vertical, 8)
    }
}

/// ฟองข้อความหนึ่งฟอง
struct ChatBubble: View {
    let message: ChatMessage
    let isMine: Bool
    let layout: ChatRow.Layout
    let photoUrl: String?
    let onRetry: () -> Void

    var body: some View {
        HStack(alignment: .bottom, spacing: 6) {
            if isMine { Spacer(minLength: 48) }
            if !isMine {
                // ที่ว่างขนาด avatar เสมอ เพื่อให้ฟองในชุดเดียวกันเรียงตรงกัน
                Group {
                    if layout.isFirstInGroup {
                        ProfileAvatar(name: message.senderName, photoUrl: photoUrl, size: 30)
                    } else {
                        Color.clear
                    }
                }
                .frame(width: 30, height: 30)
            }
            VStack(alignment: isMine ? .trailing : .leading, spacing: 2) {
                if !isMine && layout.isFirstInGroup {
                    Text(message.senderName)
                        .font(.system(size: 11)).foregroundStyle(.secondary)
                        .padding(.leading, 4)
                }
                HStack(alignment: .bottom, spacing: 4) {
                    if isMine && layout.showTime { timeLabel }
                    Text(message.body)
                        .font(.system(size: 15))
                        .foregroundStyle(isMine ? .white : Color.wbwInk)
                        .padding(.horizontal, 12).padding(.vertical, 8)
                        .background(isMine ? Color.wbwGold : Color.white,
                                    in: BubbleShape(isMine: isMine, hasTail: layout.isLastInGroup))
                    if isMine { stateIcon }
                    if !isMine && layout.showTime { timeLabel }
                }
            }
            if !isMine { Spacer(minLength: 48) }
        }
        .padding(.vertical, layout.isFirstInGroup ? 5 : 1)
    }

    private var timeLabel: some View {
        Text(ChatFormat.time(message.displayTime))
            .font(.system(size: 10))
            .foregroundStyle(.secondary)
    }

    @ViewBuilder private var stateIcon: some View {
        switch message.state {
        case .pending: Image(systemName: "clock").font(.system(size: 10)).foregroundStyle(.secondary)
        case .sent:    EmptyView()      // สถานะ "ส่งแล้ว/อ่านแล้ว" ย้ายไปบรรทัดสรุปใต้ข้อความล่าสุด
        case .failed:  Button(action: onRetry) {
                           Image(systemName: "exclamationmark.circle.fill")
                               .font(.system(size: 12)).foregroundStyle(.red)
                       }
        }
    }
}
```

- [ ] **Step 2: Render rows in `GroupChatView`**

In `/Users/park/wbw-ios-fontend/WBW/GroupChatView.swift`, replace the whole
`messageList` computed property with:

```swift
    private var rows: [ChatRow] {
        ChatRowBuilder.build(store.messages, myLastReadId: store.myLastReadId,
                             myId: profile.me?.userId ?? "")
    }

    private var messageList: some View {
        ScrollView {
            LazyVStack(spacing: 0) {
                ForEach(rows) { row in
                    switch row {
                    case let .day(d):
                        ChatDayPill(day: d)
                    case .unreadMark:
                        ChatUnreadDivider()
                    case let .message(m, layout):
                        ChatBubble(message: m, isMine: store.isMine(m), layout: layout,
                                   photoUrl: members[m.senderId]?.photoUrl,
                                   onRetry: { store.retry(m) })
                    }
                }
            }
            .padding(.horizontal, 14).padding(.vertical, 10)
        }
        .defaultScrollAnchor(.bottom)
        .scrollDismissesKeyboard(.interactively)
    }
```

Delete the now-unused `scrollToLast(_:)` method and the entire private
`MessageBubble` struct at the bottom of the file.

- [ ] **Step 3: Build and run the tests**

```bash
cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'
```

Expected: build succeeds, all tests still PASS.

- [ ] **Step 4: Check it by eye in the simulator**

Run the app, open the group chat. Expected: a day pill above the first message
(`วันนี้`), one time under the last bubble of each burst, avatars and names only
on the first bubble of a burst, tails only on the last bubble, and the view opens
already scrolled to the bottom.

- [ ] **Step 5: Commit**

```bash
cd /Users/park/wbw-ios-fontend
git add WBW/Chat/ChatBubble.swift WBW/GroupChatView.swift
git commit -m "feat(chat): เวลา ป้ายคั่นวัน จับกลุ่มฟอง และหางฟองแบบ iMessage"
```

---

### Task 7: Read cursor endpoint

Adds `POST /groups/:id/chat/read` so cursors actually move, and notifies the group so other members' parked long-polls pick up the new read state immediately.

**Files:**
- Modify: `/Users/park/Desktop/WBW/backend/api/src/routes/chatRoutes.js`
- Modify: `/Users/park/Desktop/WBW/backend/api/scripts/chat-smoke.mjs`

**Interfaces:**
- Consumes: `notifyGroup` from `../chatEvents.js`.
- Produces: `POST /groups/:groupId/chat/read` with body `{last_read_id: number}` → `200 {ok: true}`; `403` when not a member; `400` on a malformed id.

- [ ] **Step 1: Add the failing read-receipt checks to the smoke script**

In `api/scripts/chat-smoke.mjs`, insert this block **after the two long-poll blocks
added in Task 2** and immediately before the `// endpoint เดิมต้องไม่เปลี่ยนพฤติกรรม`
comment:

```js
  // read cursor: B อ่าน → A ต้องเห็น cursor ของ B ขยับ
  {
    await b('POST', `/groups/${GROUP_ID}/join`);
    const sent = await a('POST', `/groups/${GROUP_ID}/messages`, {
      client_id: crypto.randomUUID(), body: 'ให้ B อ่าน', device_time: new Date().toISOString(),
    });
    const msgId = Number(sent.body.id);

    const before = await a('GET', `/groups/${GROUP_ID}/chat/sync?wait=0`);
    const bBefore = before.body.cursors.find((c) => Number(c.last_read_id) >= msgId);
    check('ก่อนอ่าน ยังไม่มีใครอ่านถึงข้อความนี้', !bBefore, JSON.stringify(before.body.cursors));

    const read = await b('POST', `/groups/${GROUP_ID}/chat/read`, { last_read_id: msgId });
    check('POST /chat/read ตอบ 200', read.status === 200, `status ${read.status} ${read.text}`);

    const after2 = await a('GET', `/groups/${GROUP_ID}/chat/sync?wait=0`);
    const readers = after2.body.cursors.filter((c) => Number(c.last_read_id) >= msgId).length;
    check('หลังอ่าน มี 1 คนอ่านถึงข้อความนี้', readers === 1, `นับได้ ${readers}`);

    // cursor เดินหน้าอย่างเดียว — ส่งค่าต่ำกว่าเดิมต้องไม่ถอยหลัง
    await b('POST', `/groups/${GROUP_ID}/chat/read`, { last_read_id: 1 });
    const after3 = await a('GET', `/groups/${GROUP_ID}/chat/sync?wait=0`);
    const stillRead = after3.body.cursors.filter((c) => Number(c.last_read_id) >= msgId).length;
    check('cursor ไม่ถอยหลัง', stillRead === 1, `นับได้ ${stillRead}`);
  }
```

- [ ] **Step 2: Run it to verify it fails**

```bash
cd /Users/park/Desktop/WBW/backend && node api/scripts/chat-smoke.mjs
```

Expected: `POST /chat/read ตอบ 200` FAILS with status 404.

- [ ] **Step 3: Add the endpoint**

In `api/src/routes/chatRoutes.js`, extend the `chatEvents` import:

```js
import { waitForGroup, notifyGroup } from '../chatEvents.js';
```

and append this handler above `export default router;`:

```js
// อ่านถึง id ไหน — ยิงซ้ำค่าเดิมได้ ใช้เป็น heartbeat "กำลังดูจอแชทอยู่" (push จะข้ามคนนี้)
router.post('/read', requireAuth, async (req, res) => {
  const groupId = Number(req.params.groupId);
  if (!Number.isInteger(groupId)) return res.status(400).json({ error: 'กลุ่มไม่ถูกต้อง' });
  const userId = req.user.sub;
  if (!(await isMember(userId, groupId))) return res.status(403).json({ error: 'ต้องอยู่ในกลุ่มนี้ก่อน' });

  const lastReadId = Number(req.body?.last_read_id);
  if (!Number.isInteger(lastReadId) || lastReadId < 0) {
    return res.status(400).json({ error: 'last_read_id ไม่ถูกต้อง' });
  }

  // GREATEST — cursor เดินหน้าอย่างเดียว กัน request มาสลับลำดับแล้วถอยหลัง
  await query(
    `UPDATE group_chat_state
     SET last_read_id = GREATEST(last_read_id, $3::bigint), read_at = now()
     WHERE user_id = $1 AND group_id = $2`,
    [userId, groupId, lastReadId]
  );

  res.json({ ok: true });
  // ปลุกคนอื่นให้เห็นสถานะ "อ่านแล้ว" สด — ข้ามตัวเอง
  notifyGroup(groupId, userId);
});
```

- [ ] **Step 4: Restart and run the smoke test**

```bash
cd /Users/park/Desktop/WBW/backend
docker compose restart api && until curl -sf "$API_BASE/health" >/dev/null; do sleep 1; done
node api/scripts/chat-smoke.mjs
```

Expected: every check PASSes, ending with `ผ่านทั้งหมด`.

- [ ] **Step 5: Commit (backend repo)**

```bash
cd /Users/park/Desktop/WBW/backend
git add api/src/routes/chatRoutes.js api/scripts/chat-smoke.mjs
git commit -m "feat(chat): endpoint /chat/read + ปลุกสถานะอ่านให้คนอื่น"
```

---

### Task 8: "อ่านแล้ว N" under my latest message

Renders the read status line and wires the heartbeat already built into `ChatSession`.

**Files:**
- Modify: `/Users/park/wbw-ios-fontend/WBW/Chat/ChatBubble.swift` (add `ChatReadStatus`)
- Modify: `/Users/park/wbw-ios-fontend/WBW/GroupChatView.swift` (render it under the last message I sent)
- Modify: `/Users/park/wbw-ios-fontend/WBWTests/ChatSessionTests.swift` (status text test)

**Interfaces:**
- Consumes: `ChatSession.readCount(for:)`, `ChatSession.memberCount`, `ChatMessage.state`.
- Produces: `enum ChatReadStatus { static func text(readCount: Int, memberCount: Int) -> String }`; `struct ChatReadStatusLine: View` (init `(text: String)`).

- [ ] **Step 1: Write the failing status-text test**

Append to `/Users/park/wbw-ios-fontend/WBWTests/ChatSessionTests.swift`, inside the class:

```swift
    func testReadStatusText() {
        XCTAssertEqual(ChatReadStatus.text(readCount: 0, memberCount: 10), "ส่งแล้ว")
        XCTAssertEqual(ChatReadStatus.text(readCount: 3, memberCount: 10), "อ่านแล้ว 3")
        // ครบทุกคนที่ไม่ใช่เรา = member_count - 1
        XCTAssertEqual(ChatReadStatus.text(readCount: 9, memberCount: 10), "อ่านแล้ว 9 · ทั้งกลุ่ม")
        // กลุ่มมีเราคนเดียว — ไม่มีใครให้อ่าน
        XCTAssertEqual(ChatReadStatus.text(readCount: 0, memberCount: 1), "ส่งแล้ว")
    }
```

- [ ] **Step 2: Run it to verify it fails**

```bash
cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'
```

Expected: compile failure — `cannot find 'ChatReadStatus' in scope`.

- [ ] **Step 3: Add the status view to `WBW/Chat/ChatBubble.swift`**

Append at the bottom of the file:

```swift
/// ข้อความสถานะใต้ข้อความล่าสุดที่เราส่ง
enum ChatReadStatus {
    static func text(readCount: Int, memberCount: Int) -> String {
        guard readCount > 0 else { return "ส่งแล้ว" }
        let others = max(memberCount - 1, 0)   // ทุกคนยกเว้นเรา
        return readCount >= others ? "อ่านแล้ว \(readCount) · ทั้งกลุ่ม" : "อ่านแล้ว \(readCount)"
    }
}

struct ChatReadStatusLine: View {
    let text: String
    var body: some View {
        Text(text)
            .font(.system(size: 10))
            .foregroundStyle(.secondary)
            .frame(maxWidth: .infinity, alignment: .trailing)
            .padding(.trailing, 4).padding(.top, 2).padding(.bottom, 6)
            .animation(.easeOut(duration: 0.2), value: text)
    }
}
```

- [ ] **Step 4: Render it under my latest sent message**

In `/Users/park/wbw-ios-fontend/WBW/GroupChatView.swift`, add this computed
property next to `rows`:

```swift
    /// clientId ของข้อความล่าสุดที่เราส่งและส่งสำเร็จแล้ว — จุดที่โชว์สถานะอ่าน
    private var statusAnchorId: String? {
        store.messages.last { store.isMine($0) && $0.state == .sent }?.clientId
    }
```

and in the `case let .message(m, layout):` branch of the `ForEach`, replace the
single `ChatBubble(...)` call with:

```swift
                    case let .message(m, layout):
                        VStack(spacing: 0) {
                            ChatBubble(message: m, isMine: store.isMine(m), layout: layout,
                                       photoUrl: members[m.senderId]?.photoUrl,
                                       onRetry: { store.retry(m) })
                            if m.clientId == statusAnchorId {
                                ChatReadStatusLine(
                                    text: ChatReadStatus.text(readCount: store.readCount(for: m),
                                                              memberCount: store.memberCount))
                            }
                        }
```

- [ ] **Step 5: Run the tests to verify they pass**

```bash
cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'
```

Expected: `testReadStatusText` PASSes along with everything else.

- [ ] **Step 6: Verify end to end**

With the backend running, send a message from the simulator, then mark it read as
the other user:

```bash
cd /Users/park/Desktop/WBW/backend && node -e "
const BASE=process.env.API_BASE,G=process.env.GROUP_ID;
const t=(await (await fetch(BASE+'/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:process.env.U1,password:process.env.P1})})).json()).token;
const s=await (await fetch(BASE+'/groups/'+G+'/chat/sync?wait=0',{headers:{authorization:'Bearer '+t}})).json();
const max=Math.max(0,...s.messages.map(m=>Number(m.id)));
await fetch(BASE+'/groups/'+G+'/chat/read',{method:'POST',headers:{authorization:'Bearer '+t,'content-type':'application/json'},body:JSON.stringify({last_read_id:max})});
console.log('อ่านถึง',max);
"
```

Expected: within a second the simulator's status line flips from `ส่งแล้ว` to `อ่านแล้ว 1`.

- [ ] **Step 7: Commit**

```bash
cd /Users/park/wbw-ios-fontend
git add WBW/Chat/ChatBubble.swift WBW/GroupChatView.swift WBWTests/ChatSessionTests.swift
git commit -m "feat(chat): สถานะ อ่านแล้ว N ใต้ข้อความล่าสุดของเรา"
```

---

### Task 9: Chat push from the backend

Sends an FCM push per message to members who are not the sender and are not
actively reading, collapsed per group, with a per-user badge.

**Files:**
- Modify: `/Users/park/Desktop/WBW/backend/api/src/push.js`
- Modify: `/Users/park/Desktop/WBW/backend/api/src/routes/groupRoutes.js` (call it after insert)

**Interfaces:**
- Consumes: `ensureInit()` and `query` already inside `push.js`; the `device_token` table.
- Produces: `sendChatPush(msg)` where `msg` is the inserted row from `POST /groups/:id/messages` (`{id, group_id, sender_id, body, first_name, last_name}`).

- [ ] **Step 1: Add `sendChatPush` to `api/src/push.js`**

Append at the bottom of the file:

```js
/** ผู้รับ push ของข้อความแชท + badge ของแต่ละคน
 *  ตัดคนส่งออก และตัดคนที่เพิ่งอ่านภายใน 25 วิ (= กำลังเปิดจอแชทอยู่ ไคลเอนต์ยิง heartbeat ทุก 10 วิ) */
async function chatPushTargets(groupId, senderId) {
  const { rows } = await query(
    `SELECT d.token,
            (SELECT COUNT(*) FROM group_message m
              WHERE m.group_id = s.group_id AND m.id > s.last_read_id) AS unread
     FROM group_chat_state s
     JOIN participant_profile p ON p.user_id = s.user_id AND p.group_id = s.group_id
     JOIN device_token d ON d.user_id = s.user_id
     WHERE s.group_id = $1
       AND s.user_id <> $2
       AND now() - s.read_at > interval '25 seconds'`,
    [groupId, senderId]
  );
  return rows.map((r) => ({ token: r.token, badge: Number(r.unread) || 0 }));
}

/** ส่ง push ข้อความแชท — เรียกแบบ fire-and-forget หลังตอบ 201 แล้ว */
export async function sendChatPush(msg) {
  const m = ensureInit();
  if (!m) return;
  try {
    const targets = await chatPushTargets(msg.group_id, msg.sender_id);
    if (!targets.length) return;

    const title = [msg.first_name, msg.last_name].filter(Boolean).join(' ') || 'ข้อความใหม่';
    const body = String(msg.body || '').slice(0, 120);
    const invalid = [];

    for (let i = 0; i < targets.length; i += 500) {
      const batch = targets.slice(i, i + 500);
      const res = await m.sendEach(
        batch.map((t) => ({
          token: t.token,
          notification: { title, body },
          data: { type: 'chat', group_id: String(msg.group_id), message_id: String(msg.id) },
          apns: {
            // collapse-id — ข้อความรัวๆ ทับอันเดิม ไม่ท่วม lock screen
            headers: { 'apns-priority': '10', 'apns-collapse-id': `chat-${msg.group_id}` },
            payload: { aps: { sound: 'default', badge: t.badge, 'thread-id': `chat-${msg.group_id}` } },
          },
          android: { priority: 'high', collapseKey: `chat-${msg.group_id}` },
        }))
      );
      res.responses.forEach((r, idx) => {
        if (r.success) return;
        const code = r.error?.code || '';
        if (code === 'messaging/registration-token-not-registered' || code === 'messaging/invalid-argument') {
          invalid.push(batch[idx].token);
        }
      });
    }

    if (invalid.length) {
      await query(`DELETE FROM device_token WHERE token = ANY($1)`, [invalid]);
    }
    console.log(`[push] แชทกลุ่ม ${msg.group_id} → ${targets.length} เครื่อง (ลบ token เสีย ${invalid.length})`);
  } catch (e) {
    console.error('[push] แชทส่งล้มเหลว:', e.message);
  }
}
```

- [ ] **Step 2: Call it after a message is inserted**

In `api/src/routes/groupRoutes.js`, extend the push import (add it if the file
has none):

```js
import { sendChatPush } from '../push.js';
```

and in `POST /:groupId/messages`, extend the block added in Task 2:

```js
    res.status(201).json(rows[0]);
    // ปลุก long-poll ของทุกคนในกลุ่ม (actor = null เพื่อให้เครื่องอื่นของคนส่งตื่นด้วย)
    notifyGroup(groupId, null);
    sendChatPush(rows[0]);   // fire-and-forget — push พังไม่กระทบ 201
```

- [ ] **Step 3: Verify the target query without Firebase credentials**

`ensureInit()` returns null when `GOOGLE_APPLICATION_CREDENTIALS` is unset, so
`sendChatPush` exits before touching FCM. Test the SQL directly instead:

```bash
cd /Users/park/Desktop/WBW/backend
docker compose --profile localdb exec -T db psql -U wbw -d wbw -c "
SELECT s.user_id, s.last_read_id, now() - s.read_at AS idle
FROM group_chat_state s
JOIN participant_profile p ON p.user_id = s.user_id AND p.group_id = s.group_id
WHERE s.group_id = ${GROUP_ID};"
```

Expected: one row per current member with an `idle` interval. Confirm that a user
who just called `/chat/read` shows `idle` under 25 seconds, and that a user who has
not shows more.

- [ ] **Step 4: Confirm nothing regressed**

```bash
cd /Users/park/Desktop/WBW/backend
docker compose restart api && until curl -sf "$API_BASE/health" >/dev/null; do sleep 1; done
node api/scripts/chat-smoke.mjs
```

Expected: `ผ่านทั้งหมด`.

- [ ] **Step 5: Commit (backend repo)**

```bash
cd /Users/park/Desktop/WBW/backend
git add api/src/push.js api/src/routes/groupRoutes.js
git commit -m "feat(chat): push ข้อความแชท ยุบต่อกลุ่ม ข้ามคนที่กำลังอ่าน"
```

---

### Task 10: iOS push routing, in-app toast, tab badge

Routes chat pushes to the chat screen, suppresses the system banner while the app
is in the foreground (a custom toast takes over), and badges the group tab.

**Files:**
- Create: `/Users/park/wbw-ios-fontend/WBW/Chat/ChatToast.swift`
- Modify: `/Users/park/wbw-ios-fontend/WBW/AppDelegate.swift`
- Modify: `/Users/park/wbw-ios-fontend/WBW/MainTabView.swift`

**Interfaces:**
- Consumes: `ChatSession.unreadCount`, `ChatSession.incoming`, `ProfileAvatar`.
- Produces: `Notification.Name.openGroupChat`; `struct ChatToast: View` (init `(message: ChatMessage, photoUrl: String?, onTap: () -> Void)`).

- [ ] **Step 1: Create `WBW/Chat/ChatToast.swift`**

```swift
import SwiftUI

/// แบนเนอร์ในแอป — เด้งตอนอยู่ในแอปแต่ไม่ได้เปิดจอแชท (แทน banner ของระบบ)
struct ChatToast: View {
    let message: ChatMessage
    let photoUrl: String?
    let onTap: () -> Void

    var body: some View {
        Button(action: onTap) {
            HStack(spacing: 10) {
                ProfileAvatar(name: message.senderName, photoUrl: photoUrl, size: 34)
                VStack(alignment: .leading, spacing: 1) {
                    Text(message.senderName)
                        .font(.system(size: 13, weight: .semibold))
                        .foregroundStyle(Color.wbwInk)
                    Text(message.body)
                        .font(.system(size: 13))
                        .foregroundStyle(.secondary)
                        .lineLimit(1)
                }
                Spacer(minLength: 0)
            }
            .padding(.horizontal, 14).padding(.vertical, 10)
            .background(.regularMaterial, in: RoundedRectangle(cornerRadius: 18))
            .shadow(color: .black.opacity(0.12), radius: 12, y: 4)
            .padding(.horizontal, 12)
        }
        .buttonStyle(.plain)
    }
}
```

- [ ] **Step 2: Route chat pushes in `AppDelegate.swift`**

Add the notification name next to the existing one:

```swift
extension Notification.Name {
    /// โพสต์เมื่อผู้ใช้แตะ push — ให้ MainTabView สลับไปแท็บประกาศ
    static let openNotificationsTab = Notification.Name("openNotificationsTab")
    /// โพสต์เมื่อผู้ใช้แตะ push ของแชท — ให้ MainTabView เปิดจอแชท
    static let openGroupChat = Notification.Name("openGroupChat")
}
```

Replace the two `UNUserNotificationCenterDelegate` methods with:

```swift
    // แสดง banner ตอนแอปเปิดอยู่ (foreground) — ยกเว้นแชท ซึ่งใช้ toast ในแอปแทน
    func userNotificationCenter(
        _ center: UNUserNotificationCenter,
        willPresent notification: UNNotification,
        withCompletionHandler completionHandler: @escaping (UNNotificationPresentationOptions) -> Void
    ) {
        let info = notification.request.content.userInfo
        if (info["type"] as? String) == "chat" {
            completionHandler([])   // in-app toast ของเราเด้งเอง ไม่ให้ซ้อน
            return
        }
        completionHandler([.banner, .sound, .badge])
    }

    // แตะ notification → เปิดหน้าที่ตรงกับชนิด
    func userNotificationCenter(
        _ center: UNUserNotificationCenter,
        didReceive response: UNNotificationResponse,
        withCompletionHandler completionHandler: @escaping () -> Void
    ) {
        let info = response.notification.request.content.userInfo
        let name: Notification.Name = (info["type"] as? String) == "chat" ? .openGroupChat : .openNotificationsTab
        NotificationCenter.default.post(name: name, object: nil)
        completionHandler()
    }
```

- [ ] **Step 3: Badge and toast in `MainTabView.swift`**

Add the badge to the group tab:

```swift
                Tab(value: 3) {
                    GroupTabView(onBack: { tab = 0 }, onOpenChat: { chatOpen = true })
                } label: {
                    Image(systemName: profile.me?.groupId == nil ? "sharedwithyou" : "message.fill")
                }
                .badge(chat.unreadCount)
```

Add the push route next to the existing `.onReceive`:

```swift
            .onReceive(NotificationCenter.default.publisher(for: .openGroupChat)) { _ in
                guard profile.me?.groupId != nil else { return }
                tab = 3
                chatOpen = true
            }
```

Add the toast overlay inside the outer `ZStack`, after the chat overlay block:

```swift
            // แบนเนอร์ในแอป — เฉพาะตอนไม่ได้เปิดจอแชท
            if let m = chat.incoming, !chatOpen {
                VStack {
                    ChatToast(message: m, photoUrl: nil, onTap: {
                        chat.incoming = nil
                        tab = 3
                        chatOpen = true
                    })
                    Spacer()
                }
                .padding(.top, 8)
                .transition(.move(edge: .top).combined(with: .opacity))
                .zIndex(2)
                .task(id: m.clientId) {
                    try? await Task.sleep(nanoseconds: 3_500_000_000)
                    chat.incoming = nil
                }
            }
```

and extend the existing `.animation(...)` line to cover the toast:

```swift
        .animation(.spring(response: 0.42, dampingFraction: 0.78), value: chatOpen)
        .animation(.spring(response: 0.4, dampingFraction: 0.8), value: chat.incoming?.clientId)
```

- [ ] **Step 4: Build and test**

```bash
cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'
```

Expected: build succeeds, all tests PASS.

- [ ] **Step 5: Verify by hand**

1. Open the app on a screen other than chat. Send a message from the other user
   via the snippet in Task 5 Step 8. Expected: the toast slides in from the top,
   the group tab shows a badge, tapping the toast opens the chat.
2. Open the chat screen and send another. Expected: no toast, no badge, message
   appears inline.
3. Background the app and send another. Expected: a system notification titled
   with the sender's name; tapping it opens the app on the chat screen. (Requires
   `GOOGLE_APPLICATION_CREDENTIALS` set on the API and a real device for APNs —
   on the simulator, verify only steps 1 and 2.)

- [ ] **Step 6: Commit**

```bash
cd /Users/park/wbw-ios-fontend
git add WBW/Chat/ChatToast.swift WBW/AppDelegate.swift WBW/MainTabView.swift
git commit -m "feat(chat): badge แท็บ + toast ในแอป + เปิดแชทจาก push"
```

---

**As built.** Shipped as `b0cdaf1` with no deviation from the snippets above — all three
files match the plan verbatim, which is why there is no differences list here the way
Task 5 has one. The interfaces the task consumes were checked against the shipped code
before building: `ChatSession.incoming` is `@Published var` (so `MainTabView` may clear
it), `unreadCount` is `@Published private(set)` (read-only, which is all `.badge()`
needs), and `ChatMessage` is a SwiftData `@Model` class carrying `clientId`, `body`, and
`senderName`.

**Step 5 verified on 2026-08-01 — against SUS, not Node.** The toast and the badge were
both seen on screen: a message posted by the other account rendered as `ChatToast` at the
top of the Home tab (avatar initial, sender name, body) while the group tab's badge went
1 → 2. Round trip from POST to the client rendering it was 86 ms, off LISTEN/NOTIFY rather
than the 25 s poll ceiling.

The blocker this note used to describe (no usable credentials) was never the real obstacle:
`Session.swift` already carries a DEBUG launch hook, so
`xcrun simctl launch <sim> <bundle> -uitestToken <jwt> -uitestUser <username>` seeds a
session with a token minted straight from the API, no typing and no password needed.

Still unproven: tapping the toast to open the chat, and the foreground
`completionHandler([])` that suppresses the system banner. Neither can be driven headlessly
— `simctl` has no tap primitive, and AppleScript cannot reach controls inside the simulator
canvas because they are drawn, not real AX elements.

Two traps that cost an hour, worth knowing before repeating this:

1. **Do not `simctl uninstall` to clear app state.** It also clears the notification
   permission grant, so the next launch raises the system permission alert. That alert drops
   the scene to `.inactive`, and `MainTabView.swift`'s `.onChange(of: scenePhase)` calls
   `chat.stop()` — the long-poll dies and stays dead for as long as the alert is up. It
   presents exactly like a hung sync loop with no error anywhere. Delete the SwiftData store
   instead, or answer the alert before drawing any conclusion.
2. **Launch with `--console-pty`.** The `[chat] sync→ / sync←` NSLog lines are the only
   usable instrument; `log show` against the simulator returns them fully redacted as
   `<compose failure>`.

**Gotcha for whoever verifies this.** `WBW/Config.swift` is deliberately left dirty in the
working tree at `Backend = .nodeLocal` and is excluded from Step 6's `git add` — the
committed value stays `.prodNode`. Hand-verification needs the local value; shipping needs
it reverted. Check `git status` before merging.

`ChatToast` is constructed with `photoUrl: nil` because Task 9's push payload carries no
photo, so the avatar always falls back to `ProfileAvatar`'s first-initial rendering. If the
payload ever gains a photo URL, this call site is the one place to thread it through.

---

### Task 11: iMessage scroll polish

Stops the list from yanking when you are reading history, adds the new-message
pill, send haptics, and the left-drag timestamp reveal.

**Files:**
- Modify: `/Users/park/wbw-ios-fontend/WBW/GroupChatView.swift`
- Modify: `/Users/park/wbw-ios-fontend/WBW/Chat/ChatBubble.swift` (accept a reveal offset)

**Interfaces:**
- Consumes: `ChatSession.messages`, `ChatSession.unreadCount`.
- Produces: no new public types — behaviour changes only.

- [ ] **Step 1: Let bubbles shift for the timestamp reveal**

In `WBW/Chat/ChatBubble.swift`, add a property to `ChatBubble` after `onRetry`:

```swift
    /// ระยะที่แถวถูกลากไปทางซ้าย (ปัดซ้ายค้างเพื่อดูเวลาทุกฟอง)
    var revealOffset: CGFloat = 0
```

Change the `timeLabel` usage so the always-on reveal column sits on the trailing
edge. Replace the `body`'s outer `HStack` closing modifiers (`.padding(.vertical, ...)`)
with:

```swift
        .padding(.vertical, layout.isFirstInGroup ? 5 : 1)
        .offset(x: -revealOffset)
        .overlay(alignment: .trailing) {
            if revealOffset > 1 {
                Text(ChatFormat.time(message.displayTime))
                    .font(.system(size: 10))
                    .foregroundStyle(.secondary)
                    .frame(width: 48)
                    .offset(x: 56 - revealOffset)
                    .opacity(Double(revealOffset / 56))
            }
        }
```

- [ ] **Step 2: Rework `messageList` in `GroupChatView.swift`**

Add these state properties next to `draft`:

```swift
    @State private var atBottom = true
    @State private var reveal: CGFloat = 0
    @State private var scrollTarget: String?
```

Replace `messageList` with:

```swift
    private var messageList: some View {
        ScrollViewReader { proxy in
            ScrollView {
                LazyVStack(spacing: 0) {
                    ForEach(rows) { row in
                        switch row {
                        case let .day(d):
                            ChatDayPill(day: d)
                        case .unreadMark:
                            ChatUnreadDivider()
                        case let .message(m, layout):
                            VStack(spacing: 0) {
                                ChatBubble(message: m, isMine: store.isMine(m), layout: layout,
                                           photoUrl: members[m.senderId]?.photoUrl,
                                           onRetry: { store.retry(m) },
                                           revealOffset: reveal)
                                if m.clientId == statusAnchorId {
                                    ChatReadStatusLine(
                                        text: ChatReadStatus.text(readCount: store.readCount(for: m),
                                                                  memberCount: store.memberCount))
                                }
                            }
                            .id(m.clientId)
                            .transition(.move(edge: .bottom).combined(with: .opacity)
                                            .combined(with: .scale(scale: 0.92, anchor: .bottom)))
                        }
                    }
                }
                .padding(.horizontal, 14).padding(.vertical, 10)
            }
            .defaultScrollAnchor(.bottom)
            .scrollDismissesKeyboard(.interactively)
            .onScrollGeometryChange(for: Bool.self) { g in
                g.contentOffset.y + g.containerSize.height >= g.contentSize.height - 40
            } action: { _, isBottom in
                atBottom = isBottom
            }
            // ปัดซ้ายค้าง = เผยเวลาทุกฟอง ปล่อยแล้วสปริงกลับ
            .simultaneousGesture(
                DragGesture(minimumDistance: 12)
                    .onChanged { v in
                        guard abs(v.translation.width) > abs(v.translation.height) else { return }
                        reveal = min(max(-v.translation.width, 0), 56)
                    }
                    .onEnded { _ in
                        withAnimation(.spring(response: 0.3, dampingFraction: 0.8)) { reveal = 0 }
                    }
            )
            .overlay(alignment: .bottom) { newMessagePill(proxy) }
            .onChange(of: store.messages.count) { _, _ in
                // อยู่ล่างสุดอยู่แล้วค่อยตามลงไป — กำลังเลื่อนอ่านย้อนหลังห้ามกระชาก
                guard atBottom, let last = store.messages.last else { return }
                withAnimation(.spring(response: 0.35, dampingFraction: 0.82)) {
                    proxy.scrollTo(last.clientId, anchor: .bottom)
                }
            }
        }
    }

    @ViewBuilder
    private func newMessagePill(_ proxy: ScrollViewProxy) -> some View {
        if !atBottom && store.unreadCount > 0 {
            Button {
                guard let last = store.messages.last else { return }
                withAnimation(.spring(response: 0.35, dampingFraction: 0.82)) {
                    proxy.scrollTo(last.clientId, anchor: .bottom)
                }
            } label: {
                Label("ข้อความใหม่ \(store.unreadCount)", systemImage: "arrow.down")
                    .font(.system(size: 12, weight: .semibold))
                    .foregroundStyle(.white)
                    .padding(.horizontal, 14).padding(.vertical, 8)
                    .background(Color.wbwGold, in: Capsule())
                    .shadow(color: .black.opacity(0.15), radius: 8, y: 3)
            }
            .padding(.bottom, 10)
            .transition(.move(edge: .bottom).combined(with: .opacity))
        }
    }
```

- [ ] **Step 3: Add send haptics**

In `GroupChatView.swift`, add state for the trigger next to `draft`:

```swift
    @State private var sentTick = 0
```

Change `send()` to bump it:

```swift
    private func send() {
        store.send(draft, senderName: profile.me?.displayName ?? "ฉัน")
        draft = ""
        sentTick += 1
    }
```

and add the feedback modifier to the outermost `VStack` in `body`, next to
`.background(bg.ignoresSafeArea())`:

```swift
        .sensoryFeedback(.impact(weight: .light), trigger: sentTick)
        .sensoryFeedback(.error, trigger: store.messages.filter { $0.state == .failed }.count)
```

- [ ] **Step 4: Build and test**

```bash
cd /Users/park/wbw-ios-fontend && xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'
```

Expected: build succeeds, all tests PASS.

- [ ] **Step 5: Verify by hand in the simulator**

1. Scroll up into history, then have the other user send a message. Expected: the
   view does not move; the `ข้อความใหม่ N` pill appears; tapping it scrolls to the
   bottom and the pill disappears.
2. Drag the list left. Expected: bubbles shift and times fade in on the right;
   releasing springs back.
3. Drag the list down over the keyboard. Expected: the keyboard follows the finger.
4. Send a message. Expected: a light haptic (on device) and a spring-in bubble.

- [ ] **Step 6: Commit**

```bash
cd /Users/park/wbw-ios-fontend
git add WBW/GroupChatView.swift WBW/Chat/ChatBubble.swift
git commit -m "feat(chat): เลื่อนแบบ iMessage — pill ข้อความใหม่ ปัดซ้ายดูเวลา haptic"
```

---

**As built.** Shipped as `a22cfec`, with two deliberate departures from the snippets above.

**The pill counts messages itself instead of reading `store.unreadCount`.** The snippet
gates on `!atBottom && store.unreadCount > 0`, which can essentially never be true:
`ChatSession.swift` runs `if screenVisible { markRead() }` on every sync that brings new
messages, so with the chat screen open `unreadCount` is driven back to 0 the moment a
message lands. Following the plan literally ships a pill that never appears. Fixing it in
`markRead()` was the wrong lever — "screen open means read" is a spec decision and the value
feeds everyone else's "อ่านแล้ว N" — so `GroupChatView` keeps a local `newBelow` counter,
incremented when messages arrive while scrolled away and reset on reaching the bottom.

**Auto-follow also fires for your own messages**, not just when already at the bottom.
Sending while scrolled up should bring you to what you just sent; the snippet's `atBottom`
guard alone would leave you stranded in history.

The reveal column skips bubbles that already show a time (last-in-group), otherwise the
same timestamp renders twice during a left drag.

**Verified:** build plus 35 tests; the chat screen opened on the simulator against SUS via
the `-uitestChat` launch flag renders correctly (grouping, tails, time only on last-in-group,
read-status line), and auto-follow works — a message from the other account arrived and the
list scrolled to it on its own.

**Not verified — needs a human with a finger:** the pill itself (requires scrolling up), the
left-drag time reveal, interactive keyboard dismissal, and the haptics (which the simulator
does not produce at all). `simctl` has no tap or drag primitive, and AppleScript can only
reach the `UITabBar`, which is a real AX element; SwiftUI buttons and the ScrollView are
drawn, so hit-testing never delivers a touch. `-uitestChat` is how you get onto the screen
in the first place — see the Task 10 note for the rest of the headless toolkit.

---

### Task 12: Deployment checklist for long-poll

The three infrastructure risks from the spec, verified before this ships.

**Files:**
- Create: `/Users/park/Desktop/WBW/backend/docs/chat-v2-deploy.md`

**Interfaces:** none — operational documentation.

- [ ] **Step 1: Check the reverse proxy in front of the API**

A parked 25-second request must not be buffered or cut off. If nginx sits in
front, the server block needs:

```nginx
location / {
    proxy_pass http://api:4000;
    proxy_buffering off;        # ห้าม buffer — long-poll ต้องตอบทันทีที่ตื่น
    proxy_read_timeout 60s;     # ต้องมากกว่า wait=25 ของ /chat/sync
    proxy_http_version 1.1;
}
```

Verify from outside the proxy:

```bash
time curl -s -o /dev/null -w '%{http_code}\n' \
  -H "Authorization: Bearer $TOKEN" \
  "$PUBLIC_BASE/groups/$GROUP_ID/chat/sync?wait=20"
```

Expected: `200` after roughly 20 seconds — not an immediate return, and not a 502/504.

- [ ] **Step 2: Check the file-descriptor limit**

```bash
cd /Users/park/Desktop/WBW/backend
docker compose exec api sh -c 'ulimit -n'
```

Expected: at least 65535. If it is lower, add to the `api` service in `docker-compose.yml`:

```yaml
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
```

- [ ] **Step 3: Check Postgres connection headroom**

Each worker opens one dedicated `LISTEN` connection on top of the pool.

```bash
docker compose --profile localdb exec -T db psql -U wbw -d wbw -c "SHOW max_connections;"
docker compose --profile localdb exec -T db psql -U wbw -d wbw -c "SELECT count(*) FROM pg_stat_activity;"
```

Expected: `max_connections` comfortably above (workers × pool size) + workers. With
`availableParallelism()` workers and the default `pg` pool of 10, budget 11 per worker.

- [ ] **Step 4: Write the checklist down**

Create `/Users/park/Desktop/WBW/backend/docs/chat-v2-deploy.md` recording the three
checks above, the actual measured values, and this note:

```markdown
# แชท v2 — เช็คก่อน deploy

long-poll ค้าง request สูงสุด 25 วิ. ต้องผ่านทั้ง 3 ข้อ:

1. reverse proxy — `proxy_buffering off`, read timeout > 30 วิ
2. `ulimit -n` >= 65535 บนโฮสต์ API (1 connection ค้างต่อผู้ใช้ที่ active)
3. `max_connections` ของ Postgres รองรับ (worker × pool) + worker
   (LISTEN ใช้ connection เฉพาะ 1 ตัวต่อ worker)

ค่าที่วัดได้จริง: <กรอกตอนรัน>

**หมายเหตุ Android:** แอป Android ยังใช้ `/groups/:id/messages` แบบเดิม ไม่เคยยิง
`/chat/read` → cursor ค้างที่ 0 → คน Android จะไม่ถูกนับใน "อ่านแล้ว N" จนกว่าจะ
mirror ฝั่ง Android ตามทีหลัง
```

- [ ] **Step 5: Commit (backend repo)**

```bash
cd /Users/park/Desktop/WBW/backend
git add docs/chat-v2-deploy.md docker-compose.yml
git commit -m "docs(chat): เช็คลิสต์ก่อน deploy long-poll"
```

---

## Coverage against the spec

| Spec section | Task |
|---|---|
| §1.1 schema + backfill | 1 |
| §1.2 join/leave state | 1 |
| §1.3 `/chat/sync` shape, cut-off filter, cursors, `LIMIT 50` drain | 1, 2 |
| §1.3 `/chat/read` | 7 |
| §1.4 `LISTEN/NOTIFY`, `waitForGroup`, reconnect | 2 |
| §1.5 push targets, collapse, per-user badge | 9 |
| §2.1 session lifted to `MainTabView` | 5 |
| §2.2 sync loop, timeout handling, backoff | 5 |
| §2.3 `since_id` purge | 5 |
| §2.4 read cursor + 10 s heartbeat | 5 |
| §2.5 derived values | 5, 8 |
| §2.6 `scenePhase` lifecycle | 5 |
| §3.1 `buildRows` | 4 |
| §3.2 day separators | 4, 6 |
| §3.3 time + left-drag reveal | 6, 11 |
| §3.4 grouping + tails | 4, 6 |
| §3.5 read status | 8 |
| §3.6 scroll anchor, keyboard, pill, haptics, unread divider | 6, 11 |
| §3.7 badge, toast, push routing | 10 |
| §5 error handling | 5 (loop, 403, offline), 7 (400), 9 (push failure) |
| §6 testing | 1, 2, 4, 5, 7, 8 |
| §8 risks 1–3 | 12 |
| §8 risk 4 (Android) | 12 (documented) |
| §8 risk 5 (push volume) | 9 (collapse + 25 s active-reader filter) |
