# หน้า Map 3D — แทน MapLibre ด้วย map.usdz — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** เปลี่ยนแท็บ Map จาก MapLibre เป็นโมเดล 3D `map.usdz` ที่หมุน/ซูมได้ แตะแท่งแดง 8 แท่งแล้วขึ้นชื่อฐาน และมีจุดตำแหน่งผู้ใช้จาก GPS

**Architecture:** `Map3DScreen` เป็น `RealityView` จอเดียวจบ ไม่ยืมกลไก host/claim/release ของฉากป่า (ฉากป่าต้องมีเพราะหลายจอใช้ร่วมกันและฉากอยู่ใต้ `RootView` คนละต้นไม้กับจอ — แผนที่อยู่จอเดียว `RealityView` เกิดและตายไปกับจอได้เลย) โหลดโมเดลด้วย `Entity(named: "map")` ทรงเดียวกับที่ `ForestSceneView.swift:49` โหลด `forest.usdz` ตรรกะที่เทสได้ (แปลงพิกัด, ตารางหมุด, สวิตช์) แยกออกเป็นไฟล์ฟังก์ชันบริสุทธิ์

**Tech Stack:** Swift 5, SwiftUI, RealityKit, CoreLocation, XCTest, XcodeGen (`project.yml` ผูก sources เป็นโฟลเดอร์ ไฟล์ใหม่เข้า target เองหลัง `xcodegen generate`)

**Spec:** `docs/superpowers/specs/2026-08-07-map3d-usdz-design.md`

## Global Constraints

- **ห้าม `git add -A` หรือ `git commit -a`** — repo นี้มีงานที่ยังไม่ commit ของคนอื่นวางอยู่ (`claude-code-server-side-swift-skills/`) ทุก commit ต้อง `git add <ไฟล์ที่ระบุ>` ตามที่เขียนไว้ในแต่ละ Task เท่านั้น
- คำสั่งเทสมาตรฐาน: `xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'` — ก่อนเริ่ม Task 1 ให้รันหนึ่งรอบแล้วจดจำนวนเทสไว้ (ตอนเขียนแผนคือ **141**) ทุก Task บอกไว้ว่าเลขต้องขยับเป็นเท่าไร
- app target เป็น test host ของ `WBWTests` ทุก `xcodebuild test` จึง boot แอปทั้งตัว
- deployment target iOS 18.0, `SWIFT_VERSION: "5.0"`, `TARGETED_DEVICE_FAMILY: "1"` (iPhone เท่านั้น)
- คอมเมนต์และข้อความในแอปเป็นภาษาไทย ตามของเดิมทั้งไฟล์
- **ห้ามลบของฉากป่า** — `WBW/Scene3D/`, `WBW/Resources/forest.usdz`, `tree.usdz`, `models/*.glb`, `scripts/bake-forest.py`, `ForestMathTests`, `GyroParallaxTests` ไม่เกี่ยวกับงานนี้เลย
- ไม่แตะ backend รีโปไหน ไม่เพิ่ม endpoint ใหม่
- สภาพแวดล้อมนี้ **ไม่มี tap tooling** — อะไรที่ต้องแตะเพื่อดู ต้องมี launch flag เปิดตรงๆ (ทรงเดียวกับ `uitestChat`/`uitestFeedback` ที่ `MainTabView.swift`) และป๊อปอัพขอสิทธิ์แจ้งเตือนจะโผล่ทุกครั้งที่ติดตั้งใหม่ ตอบมันไม่ได้ (ดู `docs/forest-3d-verification.md:61`) ให้ถ่ายซ้ำรอบสองหลังป๊อปอัพหาย
- JWT สำหรับ `-uitestToken` ต้องเป็นของจริงจาก production — บัญชี `6931900013` (รหัสถาม Park) บัญชีใน `docs/sus-test-backend.md` ใช้กับ prod ไม่ได้

## File Structure

| ไฟล์ | สถานะ | หน้าที่ |
|---|---|---|
| `WBW/Resources/map.usdz` | เพิ่ม | โมเดลพื้นที่งาน 11 MB (คัดลอกจาก `~/Downloads/map.usdz`) |
| `WBW/Map3D/Map3DScreen.swift` | สร้าง | `RealityView` + camera controls + overlay เครดิต + การ์ดชื่อฐาน + จุดตำแหน่ง |
| `WBW/Map3D/Map3DPins.swift` | สร้าง | ตารางจับคู่ชื่อ prim ↔ ลำดับฐาน + ประกอบข้อความบนการ์ด |
| `WBW/Map3D/Map3DGeo.swift` | สร้าง | lat/lng → พิกัดโมเดล (ฟังก์ชันบริสุทธิ์) |
| `WBW/Map3D/Map3DLocation.swift` | สร้าง | `CLLocationManager` wrapper บางๆ |
| `WBW/Config.swift` | แก้ | เพิ่ม `Config.map3D` |
| `WBW/MainTabView.swift:49` | แก้ | `MapScreen()` → `Map3DScreen()` |
| `WBW/MapScreen.swift` | ลบ (Task 5) | MapLibre เดิม |
| `project.yml` | แก้ (Task 5) | ถอด package `MapLibre` |
| `WBW/Info.plist` · `WBW/Info-Debug.plist` | แก้ (Task 4) | เพิ่ม `NSLocationWhenInUseUsageDescription` |
| `WBWTests/Map3DPinsTests.swift` | สร้าง | เทสตารางหมุด |
| `WBWTests/Map3DGeoTests.swift` | สร้าง | เทสการแปลงพิกัด |
| `WBWTests/Map3DConfigTests.swift` | สร้าง | เทสสวิตช์ |
| `WBWTests/AppStoreConfigTests.swift:65` | แก้ (Task 4) | กลับด้านเทสที่ห้ามมีคีย์ตำแหน่ง |
| `docs/superpowers/specs/2026-07-31-map3d-glb-design.md` | แก้ (Task 5) | เติมสถานะว่าถูกแทนที่ |
| `docs/map3d-verification.md` | สร้าง (Task 5) | บันทึกผลยืนยันจริง |

---

### Task 1: โมเดลขึ้นจอให้ได้ก่อน

ความเสี่ยงตัวจริงของงานทั้งหมดอยู่ที่นี่ — โมเดลอาจไม่ขึ้น ขึ้นแบบนอนตะแคง (usdz เป็น Z-up แต่ RealityKit เป็น Y-up) หรือ texture หลุด **ทำก่อนและทำลำพัง ถ้าไม่ผ่านให้กลับไปแก้ที่ Blender ไม่ใช่ที่โค้ด**

**Files:**
- Create: `WBW/Resources/map.usdz` (คัดลอก), `WBW/Map3D/Map3DScreen.swift`
- Modify: `WBW/MainTabView.swift:49`

**Interfaces:**
- Consumes: ไม่มี (งานแรก)
- Produces: `struct Map3DScreen: View`

**ทำไมไม่มีเทสยูนิต:** Task นี้ไม่มีตรรกะเลย มีแต่การโหลดไฟล์กับตั้งกล้อง — พิสูจน์ได้ทางเดียวคือดูรูป เทสยูนิตเริ่มที่ Task 2

- [ ] **Step 1: คัดลอกไฟล์โมเดล**

```bash
cp ~/Downloads/map.usdz WBW/Resources/map.usdz
ls -la WBW/Resources/map.usdz    # ต้องได้ ~11 MB
```

- [ ] **Step 2: สร้าง `WBW/Map3D/Map3DScreen.swift`**

```swift
import RealityKit
import SwiftUI

/// แท็บแผนที่ — โมเดล 3D ของพื้นที่งาน (map.usdz) แทน MapLibre เดิม
///
/// ไม่ใช้ ForestSceneHost: ฉากป่าต้องมี host เพราะ 4 จอใช้ฉากเดียวกันและฉากถูกวาดที่ RootView
/// ซึ่งอยู่คนละ hosting context กับจอ — แผนที่อยู่จอเดียว RealityView เกิดและตายไปกับจอนี้ได้เลย
struct Map3DScreen: View {
    /// โหลดโมเดลไม่สำเร็จ — โชว์ข้อความแทนจอเปล่า (ทรงเดียวกับ ForestSceneHost.loadFailed)
    @State private var loadFailed = false

    var body: some View {
        ZStack {
            Color.wbwForestVoid.ignoresSafeArea()

            if loadFailed {
                VStack(spacing: 12) {
                    Text("เปิดแผนที่ไม่ได้")
                        .font(.system(size: 17, weight: .semibold))
                        .foregroundStyle(.white)
                    Text("ลองเข้าใหม่อีกครั้ง")
                        .font(.system(size: 13))
                        .foregroundStyle(.white.opacity(0.7))
                }
            } else {
                RealityView { content in
                    let root = Entity()
                    content.add(root)

                    let map: Entity
                    do {
                        map = try await Entity(named: "map")
                    } catch {
                        // NSLog ไม่ใช่ print — print() ไม่โผล่ใน unified log ของ simulator
                        // (ยืนยันมาแล้วตอน debug จอขาวของฉากป่า) ต้องใช้ NSLog ถึงจะ grep เจอผ่าน
                        // `xcrun simctl spawn booted log stream`
                        NSLog("[Map3DScreen] Entity(named: \"map\") threw: %@", String(describing: error))
                        await MainActor.run { loadFailed = true }
                        return
                    }
                    map.name = "Map"

                    // โมเดลเป็นเมตรจริง รัศมีราว 1.9 กม. — ย่อให้ทั้งก้อนพอดีกรอบ 2 หน่วย
                    // ก่อนค่อยให้ camera controls จัดการระยะ ไม่งั้นกล้องเริ่มต้นจะอยู่ในเนื้อโมเดล
                    let bounds = map.visualBounds(relativeTo: nil)
                    let widest = max(bounds.extents.x, max(bounds.extents.y, bounds.extents.z))
                    if widest > 0 { map.scale = SIMD3<Float>(repeating: 2 / widest) }
                    map.position = -bounds.center * map.scale.x

                    root.addChild(map)

                    #if DEBUG
                    NSLog("[Map3DScreen] map.visualBounds = %@", String(describing: bounds))
                    NSLog("[Map3DScreen] map.children.count = %d", map.children.count)
                    #endif

                    let sun = DirectionalLight()
                    sun.light.intensity = 4000
                    sun.look(at: .zero, from: SIMD3<Float>(2, 4, 2), relativeTo: nil)
                    root.addChild(sun)

                    // ไฟเติมจากฝั่งตรงข้าม — ด้านเงาของอาคารไม่ดำสนิท (ทรงเดียวกับ ForestSceneView)
                    let fill = DirectionalLight()
                    fill.light.intensity = 1200
                    fill.look(at: .zero, from: SIMD3<Float>(-3, 2, -3), relativeTo: nil)
                    root.addChild(fill)
                }
                .realityViewCameraControls(.orbit)
                .ignoresSafeArea()
            }
        }
    }
}
```

- [ ] **Step 3: ต่อเข้าแท็บ**

`WBW/MainTabView.swift:49` เปลี่ยนจาก

```swift
                Tab(value: 1) { MapScreen() } label: { Image(systemName: "map.fill") }
```

เป็น

```swift
                Tab(value: 1) { Map3DScreen() } label: { Image(systemName: "map.fill") }
```

**ยังไม่ลบ `MapScreen.swift`** — เก็บไว้จนถึง Task 5 เผื่อต้องเทียบของเดิม

- [ ] **Step 4: build + เทสชุดเต็มต้องยังผ่าน**

Run: `xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'`

Expected: PASS · จำนวนเทสเท่ากับก่อนเริ่ม Task 1 เป๊ะ (141) — Task นี้ไม่เพิ่มเทส

- [ ] **Step 5: ติดตั้งลงซิมแล้วถ่ายรูปแท็บ Map**

```bash
xcodebuild -scheme WBW -configuration Debug \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -derivedDataPath /tmp/wbw-map3d clean build
xcrun simctl install booted /tmp/wbw-map3d/Build/Products/Debug-iphonesimulator/WBW.app
TOKEN=<JWT จริงของ 6931900013>
xcrun simctl terminate booted th.ac.mfu.wbwSwift
xcrun simctl launch booted th.ac.mfu.wbwSwift -uitestToken "$TOKEN" -uitestTab 1
# รอ 12 วินาที (โมเดล 11 MB โหลดนานกว่า forest.usdz มาก) แล้วถ่าย
xcrun simctl io booted screenshot /tmp/wbw-map3d/01-map.png
```

เปิดดูด้วย Read tool แล้วตัดสินตามนี้:

| สิ่งที่เห็น | แปลว่า | ทำอะไรต่อ |
|---|---|---|
| เห็นภูมิประเทศ+อาคาร วางแบนมองจากมุมสูง | ผ่าน | ไป Step 7 |
| เห็นโมเดลแต่ **นอนตะแคง** | RealityKit ไม่ได้แปลง Z-up ให้ | Step 6 |
| จอเป็นสีพื้น `#0A1610` เปล่า | โหลดไม่ขึ้น | อ่าน log ตาม Step 6 ข้อ 2 |
| เห็นทรงโมเดลแต่ทุกอย่างเทา/ดำ | texture หรือไฟไม่ติด | Step 6 ข้อ 3 |

- [ ] **Step 6: ทางแก้ตามอาการ (ข้ามได้ถ้า Step 5 ผ่าน)**

1. **นอนตะแคง** — เติมบรรทัดนี้ต่อจาก `map.position = ...`:

```swift
                    // usdz ตัวนี้เป็น Z-up (ตรวจด้วย usdcat: upAxis = "Z") แต่ RealityKit เป็น Y-up
                    // ถ้าตัวโหลดไม่แปลงให้ ต้องหมุนเอง -90° รอบแกน X
                    map.orientation = simd_quatf(angle: -.pi / 2, axis: SIMD3<Float>(1, 0, 0))
```

2. **จอเปล่า** — ดู log จริงก่อนเดา:

```bash
xcrun simctl spawn booted log stream --predicate 'process == "WBW"' > /tmp/wbw-map3d/log.txt &
xcrun simctl launch booted th.ac.mfu.wbwSwift -uitestToken "$TOKEN" -uitestTab 1
# รอ 20 วินาที แล้วหยุด
grep -i "Map3DScreen" /tmp/wbw-map3d/log.txt
```

`Entity(named:) threw` = ไฟล์ไม่ได้เข้าบันเดิล (เช็คว่า `xcodegen generate` รันหลังคัดลอกไฟล์แล้ว) · `visualBounds` ที่ extents เป็น 0 = โมเดลว่างเปล่า ต้องกลับไปดูที่ Blender

3. **ทุกอย่างเทา/ดำ** — เพิ่ม `sun.light.intensity` เป็น 10000 แล้วถ่ายใหม่ ถ้ายังเทา แปลว่า texture ไม่ติดจริง หยุดแล้วรายงาน Park เพื่อ export ใหม่

**ถ้าแก้ทั้งสามข้อแล้วยังไม่ขึ้น หยุดทั้ง Task ห้ามเขียน Task 2-5 ทับปัญหานี้**

- [ ] **Step 7: เครดิตข้อมูล**

สเปก §7 บังคับให้มี เพราะโมเดลมาจาก maps3d.io ที่ใช้ imagery `satlas-superes-2023`
**ถาม Park ก่อนว่า export รอบ 2026-08-07 ใช้แหล่งเดิมจริงไหม** ถ้าเปลี่ยนไป ให้แก้ข้อความตามที่ Park บอก
ถ้ายังไม่ได้คำตอบ ใส่ข้อความตามสเปกไว้ก่อนแล้วจดไว้ในเอกสารยืนยัน Task 5 ว่ายังไม่ได้ยืนยัน

ใน `ZStack` ของ `body` ต่อจาก `mapView` เติม:

```swift
                VStack {
                    Spacer()
                    HStack {
                        Text("Satlas · Allen AI · © OpenStreetMap contributors")
                            .font(.system(size: 9))
                            .foregroundStyle(.white.opacity(0.55))
                        Spacer()
                    }
                    .padding(.horizontal, 12)
                    // พ้นแท็บบาร์ลอย — ค่าเดียวกับที่ฉากป่าใช้ วัดจากเครื่องจริงสองรุ่นมาแล้ว
                    .padding(.bottom, ForestSceneHost.tabBarClearance)
                }
                .allowsHitTesting(false)
```

- [ ] **Step 8: Commit**

```bash
git add WBW/Resources/map.usdz WBW/Map3D/Map3DScreen.swift WBW/MainTabView.swift
git commit -m "feat(map): แท็บ Map เป็นโมเดล 3D map.usdz แทน MapLibre

RealityView จอเดียวจบ ไม่ใช้ ForestSceneHost เพราะแผนที่ไม่ได้ถูกใช้ร่วมหลายจอ
ย่อโมเดล (เมตรจริง รัศมีราว 1.9 กม.) ให้พอดีกรอบ 2 หน่วยก่อนส่งให้ orbit camera
โหลดไม่สำเร็จโชว์ข้อความ ไม่ใช่จอเปล่า

MapScreen.swift ยังไม่ลบ รอ Task 5"
```

---

### Task 2: สวิตช์ `Config.map3D`

โมเดลนี้หนักกว่าฉากป่าที่เพิ่งถูกปิดไปวันนี้มาก (1,906 mesh + texture 6.5 MB เทียบกับ `forest.usdz` 647 KB) ต้องมีทางปิดตั้งแต่ commit นี้ ไม่ใช่ตามไปเติมทีหลังตอนเครื่องร้อนแล้ว

**Files:**
- Modify: `WBW/Config.swift:53` (ต่อจาก `forest3D`), `WBW/Map3D/Map3DScreen.swift`
- Test: `WBWTests/Map3DConfigTests.swift` (สร้างใหม่)

**Interfaces:**
- Consumes: `Map3DScreen` จาก Task 1
- Produces: `Config.map3D: Bool` (static let, ค่า `true`) · `Map3DScreen.shouldRender(map3D: Bool, underTest: Bool) -> Bool` (nonisolated static)

- [ ] **Step 1: เขียนเทสที่ยังไม่ผ่าน**

สร้าง `WBWTests/Map3DConfigTests.swift`:

```swift
import XCTest
@testable import WBW

/// สวิตช์เปิด/ปิดโมเดลแผนที่ 3D — ตรรกะเดียวที่ตัดสินว่าแท็บ Map จะโหลดโมเดลจริงไหม
/// (ทรงเดียวกับ ForestSceneHost.shouldClaim ดู WBWTests/ForestSceneHostTests.swift)
final class Map3DConfigTests: XCTestCase {

    func testRendersWhenOnAndNotUnderTest() {
        XCTAssertTrue(Map3DScreen.shouldRender(map3D: true, underTest: false),
                      "เปิดสวิตช์และไม่ได้รันเทส — ต้องโหลดโมเดลจริง")
    }

    func testDoesNotRenderWhenSwitchedOff() {
        XCTAssertFalse(Map3DScreen.shouldRender(map3D: false, underTest: false),
                       "ปิดที่ Config.map3D — ห้ามโหลดโมเดลไม่ว่ากรณีไหน")
    }

    func testDoesNotRenderUnderTestEvenWhenOn() {
        XCTAssertFalse(Map3DScreen.shouldRender(map3D: true, underTest: true),
                       "เทสยูนิตรันในโปรเซสเดียวกับแอป โหลด usdz 11 MB แล้ว exit() ทันทีเสี่ยง segfault")
    }

    func testDoesNotRenderWhenBothOff() {
        XCTAssertFalse(Map3DScreen.shouldRender(map3D: false, underTest: true))
    }
}
```

- [ ] **Step 2: รันเทสให้เห็นว่าพัง**

Run: `xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:WBWTests/Map3DConfigTests`

Expected: FAIL — compile error `type 'Map3DScreen' has no member 'shouldRender'`

- [ ] **Step 3: เพิ่มค่าใน `Config.swift`**

ต่อจาก `static let forest3D = false` (บรรทัด 53) ใน `enum Config`:

```swift

    /// โมเดลแผนที่ 3D ที่แท็บ Map — ปิดได้ด้วยค่าเดียวนี้ถ้าเครื่องรับไม่ไหว
    ///
    /// ปิด = แท็บ Map โชว์การ์ดข้อความแทน ไม่โหลด map.usdz เลยสักครั้ง (ดู Map3DScreen.shouldRender)
    /// ตั้ง true ไว้ต่างจาก forest3D โดยตั้งใจ — ฉากป่าถูกปิดเพราะอาการที่ยังไม่พิสูจน์
    /// (docs/forest-3d-off-verification.md §7) แผนที่ยังไม่ถูกวัด จึงเปิดไว้ก่อนแล้ววัด (Task 5)
    static let map3D = true
```

- [ ] **Step 4: เพิ่ม `shouldRender` แล้วให้ `body` ใช้**

ใน `WBW/Map3D/Map3DScreen.swift` เพิ่มเข้าไปใน `struct Map3DScreen` เหนือ `var body`:

```swift
    /// เทสยูนิตรันในโปรเซสเดียวกับแอป (app target เป็น test host) — ทรงเดียวกับ
    /// ForestSceneHost.isRunningUnderXCTest ที่มีเหตุผลยาวเขียนไว้แล้ว
    nonisolated static var isRunningUnderXCTest: Bool {
        NSClassFromString("XCTestCase") != nil
    }

    /// โหลดโมเดลจริงไหม — false = โชว์การ์ดข้อความแทน ไม่แตะ RealityView เลย
    ///
    /// `nonisolated` เพื่อให้เทสยูนิตเรียกได้โดยไม่ต้องอยู่บน main actor (ฟังก์ชันนี้ไม่แตะ state ใด)
    nonisolated static func shouldRender(map3D: Bool, underTest: Bool) -> Bool {
        map3D && !underTest
    }
```

แล้วห่อ `RealityView` เดิมด้วยเงื่อนไข — `body` ทั้งก้อนกลายเป็น:

```swift
    var body: some View {
        ZStack {
            Color.wbwForestVoid.ignoresSafeArea()

            if !Self.shouldRender(map3D: Config.map3D, underTest: Self.isRunningUnderXCTest) {
                // ปิดสวิตช์อยู่ — ต้องเหลือของที่อ่านรู้เรื่อง ไม่ใช่จอว่าง
                VStack(spacing: 12) {
                    Image(systemName: "map")
                        .font(.system(size: 34))
                        .foregroundStyle(.white.opacity(0.8))
                    Text("แผนที่ 3D ปิดชั่วคราว")
                        .font(.system(size: 17, weight: .semibold))
                        .foregroundStyle(.white)
                }
            } else if loadFailed {
                VStack(spacing: 12) {
                    Text("เปิดแผนที่ไม่ได้")
                        .font(.system(size: 17, weight: .semibold))
                        .foregroundStyle(.white)
                    Text("ลองเข้าใหม่อีกครั้ง")
                        .font(.system(size: 13))
                        .foregroundStyle(.white.opacity(0.7))
                }
            } else {
                mapView
            }
        }
    }
```

แล้วย้าย `RealityView { ... }.realityViewCameraControls(.orbit).ignoresSafeArea()` ทั้งก้อนจาก Task 1 ออกมาเป็น computed property แยก โดยไม่แก้เนื้อในเลยสักบรรทัด:

```swift
    private var mapView: some View {
        RealityView { content in
            // ...เนื้อเดิมจาก Task 1 ทั้งก้อน ไม่แก้...
        }
        .realityViewCameraControls(.orbit)
        .ignoresSafeArea()
    }
```

- [ ] **Step 5: รันเทสให้ผ่าน**

Run: `xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:WBWTests/Map3DConfigTests`

Expected: PASS — 4 tests, 0 failures

- [ ] **Step 6: รันชุดเต็ม**

Run: `xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'`

Expected: PASS · จำนวนเทส = 141 + 4 = **145**

- [ ] **Step 7: Commit**

```bash
git add WBW/Config.swift WBW/Map3D/Map3DScreen.swift WBWTests/Map3DConfigTests.swift
git commit -m "feat(map): สวิตช์ Config.map3D ปิดโมเดลแผนที่ได้ด้วยค่าเดียว

ปิดแล้วแท็บ Map โชว์การ์ด \"แผนที่ 3D ปิดชั่วคราว\" ไม่โหลด map.usdz เลย
ตั้ง true ไว้ต่างจาก forest3D ตั้งใจ — แผนที่ยังไม่เคยถูกวัดบนเครื่องจริง เปิดไว้ก่อนแล้ววัด"
```

---

### Task 3: หมุด 8 ฐาน แตะแล้วขึ้นชื่อ

**Files:**
- Create: `WBW/Map3D/Map3DPins.swift`
- Modify: `WBW/Map3D/Map3DScreen.swift`
- Test: `WBWTests/Map3DPinsTests.swift` (สร้างใหม่)

**Interfaces:**
- Consumes: `Map3DScreen` จาก Task 1-2 · `CheckinProgressStore` ที่มีอยู่แล้ว (`WBW/CheckinProgressStore.swift`) · `CheckinProgressItem.name`/`.sequence` (`WBW/Models.swift:248`)
- Produces:
  - `Map3DPins.entityNames: [String]` (8 ชื่อ เรียงตามลำดับฐาน 1-8)
  - `Map3DPins.sequence(forEntityNamed:) -> Int?`
  - `Map3DPins.label(sequence: Int, checkedIn: [CheckinProgressItem]) -> String`

**ข้อจำกัดที่ต้องรู้ก่อนเขียน:** `GET /wbw/me/progress` คืนแค่ฐานที่ **เช็คอินไปแล้ว** (`CheckinProgress` มี `total` กับ `checkedIn` เท่านั้น ดู `WBW/Models.swift:286`) — `CheckinProgress.pending` **ไม่ใช่** ฐานที่ยังไม่ไป มันคือฐานที่เช็คอินแล้วแต่ยังไม่ให้ความเห็น ลิสต์ชื่อฐานครบทุกฐานอยู่ที่ `GET /wbw/staff/checkpoints` ซึ่ง participant เรียกไม่ได้ ฐานที่ยังไม่เช็คอินจึงต้องโชว์ `"ฐานที่ N"` **ห้ามฮาร์ดโค้ดชื่อฐานเดาเอง**

- [ ] **Step 1: เขียนเทสที่ยังไม่ผ่าน**

สร้าง `WBWTests/Map3DPinsTests.swift`:

```swift
import XCTest
@testable import WBW

/// หมุดฐานบนโมเดลแผนที่ — ตารางจับคู่ prim กับลำดับฐาน และข้อความบนการ์ด
final class Map3DPinsTests: XCTestCase {

    func testTableCoversEightPinsWithNoDuplicates() {
        XCTAssertEqual(Map3DPins.entityNames.count, 8, "โมเดลมีแท่งแดง 8 แท่ง")
        XCTAssertEqual(Set(Map3DPins.entityNames).count, 8, "ชื่อ prim ห้ามซ้ำ")
    }

    func testSequenceLookupMatchesTableOrder() {
        XCTAssertEqual(Map3DPins.sequence(forEntityNamed: Map3DPins.entityNames[0]), 1)
        XCTAssertEqual(Map3DPins.sequence(forEntityNamed: Map3DPins.entityNames[7]), 8)
    }

    func testUnknownEntityHasNoSequence() {
        XCTAssertNil(Map3DPins.sequence(forEntityNamed: "Buildings"))
    }

    func testLabelUsesRealNameWhenCheckedIn() {
        let item = CheckinProgressItem(checkpointId: 12, name: "ฐานผาหมี", activityName: nil,
                                       sequence: 3, at: "2026-08-07T01:00:00Z",
                                       answered: false, rating: nil, comment: nil)
        XCTAssertEqual(Map3DPins.label(sequence: 3, checkedIn: [item]), "ฐานผาหมี")
    }

    func testLabelFallsBackToNumberWhenNotCheckedInYet() {
        // ชื่อฐานที่ยังไม่เช็คอิน backend ไม่ได้ส่งมาให้ participant เลย — ห้ามเดาชื่อ
        XCTAssertEqual(Map3DPins.label(sequence: 5, checkedIn: []), "ฐานที่ 5")
    }

    func testLabelFallsBackWhenCheckedInRowHasNoSequence() {
        let item = CheckinProgressItem(checkpointId: 12, name: "ฐานผาหมี", activityName: nil,
                                       sequence: nil, at: "2026-08-07T01:00:00Z",
                                       answered: false, rating: nil, comment: nil)
        XCTAssertEqual(Map3DPins.label(sequence: 3, checkedIn: [item]), "ฐานที่ 3")
    }
}
```

- [ ] **Step 2: รันเทสให้เห็นว่าพัง**

Run: `xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:WBWTests/Map3DPinsTests`

Expected: FAIL — compile error `cannot find 'Map3DPins' in scope`

- [ ] **Step 3: สร้าง `WBW/Map3D/Map3DPins.swift`**

```swift
import Foundation

/// หมุดฐานบนโมเดลแผนที่ — แท่งทรงกระบอกแดง 8 แท่งที่มากับ map.usdz อยู่แล้ว
///
/// โมเดลตั้งชื่อ prim ว่า Cylinder, Cylinder_001 … Cylinder_007 ซึ่งไม่ได้บอกว่าแท่งไหนคือฐานไหน
/// ลำดับในตารางข้างล่างจึงเป็นการจับคู่ที่ **ต้องยืนยันด้วยสกรีนช็อตกับ Park** ไม่ใช่ค่าที่พิสูจน์
/// จากตัวไฟล์ได้ — จับผิดคู่แปลว่าคนเดินผิดฐานจริง
enum Map3DPins {
    /// เรียงตามลำดับฐาน 1-8 · ลำดับนี้คือสิ่งที่ต้องยืนยันด้วยตา
    static let entityNames = [
        "Cylinder",
        "Cylinder_001",
        "Cylinder_002",
        "Cylinder_003",
        "Cylinder_004",
        "Cylinder_005",
        "Cylinder_006",
        "Cylinder_007",
    ]

    /// prim นี้เป็นฐานลำดับที่เท่าไร — nil = ไม่ใช่หมุด (อาคาร ถนน ฯลฯ)
    static func sequence(forEntityNamed name: String) -> Int? {
        guard let index = entityNames.firstIndex(of: name) else { return nil }
        return index + 1
    }

    /// ข้อความบนการ์ดตอนแตะหมุด
    ///
    /// ชื่อจริงมีให้เฉพาะฐานที่เช็คอินไปแล้ว (GET /wbw/me/progress คืนแค่ checked_in)
    /// ฐานที่ยังไม่ไปถึงไม่มีทางรู้ชื่อจากฝั่ง participant — คืน "ฐานที่ N" แทน ห้ามเดาชื่อ
    static func label(sequence: Int, checkedIn: [CheckinProgressItem]) -> String {
        if let match = checkedIn.first(where: { $0.sequence == sequence }) {
            return match.name
        }
        return "ฐานที่ \(sequence)"
    }
}
```

- [ ] **Step 4: รันเทสให้ผ่าน**

Run: `xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:WBWTests/Map3DPinsTests`

Expected: PASS — 6 tests, 0 failures

- [ ] **Step 5: ต่อหมุดเข้าจอ**

ใน `WBW/Map3D/Map3DScreen.swift`:

ก) เติม property ที่หัว struct ต่อจาก `@State private var loadFailed`:

```swift
    @EnvironmentObject private var progress: CheckinProgressStore
    /// ฐานที่แตะค้างไว้อยู่ — nil = ไม่มีการ์ด
    @State private var tappedSequence: Int?
```

ข) ใน closure ของ `RealityView` ต่อท้ายหลัง `root.addChild(map)` เติม:

```swift
                    // ให้แท่งแดงแตะได้ — ต้องมีทั้งสองคอมโพเนนต์ ขาดตัวใดตัวหนึ่ง tap ไม่เข้า
                    for name in Map3DPins.entityNames {
                        guard let pin = map.findEntity(named: name) else {
                            NSLog("[Map3DScreen] ไม่พบหมุดชื่อ %@ ในโมเดล", name)
                            continue
                        }
                        let bounds = pin.visualBounds(relativeTo: pin)
                        pin.components.set(CollisionComponent(shapes: [
                            .generateBox(size: bounds.extents).offsetBy(translation: bounds.center)
                        ]))
                        pin.components.set(InputTargetComponent())
                    }
```

ค) ต่อท้าย `.realityViewCameraControls(.orbit)` (ก่อน `.ignoresSafeArea()`) เติม gesture:

```swift
        .gesture(
            SpatialTapGesture()
                .targetedToAnyEntity()
                .onEnded { value in
                    // ไต่ขึ้นหา entity ที่อยู่ในตาราง — collision อาจโดนลูกของแท่ง ไม่ใช่ตัวแท่งเอง
                    var node: Entity? = value.entity
                    while let current = node {
                        if let sequence = Map3DPins.sequence(forEntityNamed: current.name) {
                            tappedSequence = sequence
                            return
                        }
                        node = current.parent
                    }
                }
        )
```

ง) ใน `ZStack` ของ `body` ต่อจาก `mapView` เติมการ์ด:

```swift
                if let tappedSequence {
                    VStack {
                        Spacer()
                        HStack {
                            Text(Map3DPins.label(sequence: tappedSequence,
                                                 checkedIn: progress.progress?.checkedIn ?? []))
                                .font(.system(size: 16, weight: .semibold))
                                .foregroundStyle(.white)
                            Spacer()
                            Button {
                                self.tappedSequence = nil
                            } label: {
                                Image(systemName: "xmark")
                                    .font(.system(size: 13, weight: .bold))
                                    .foregroundStyle(.white.opacity(0.8))
                            }
                        }
                        .padding(16)
                        .background(.ultraThinMaterial, in: RoundedRectangle(cornerRadius: 16))
                        .padding(.horizontal, 16)
                        .padding(.bottom, ForestSceneHost.tabBarClearance)
                    }
                }
```

ชื่อ property ซ้อนกันแบบนั้นถูกแล้ว — `CheckinProgressStore.progress` เป็น `CheckinProgress?` (`WBW/CheckinProgressStore.swift:10`) ทรงเดียวกับที่ `HomeView.swift:23` ใช้ `progress.progress?.stage` · store ถูก inject เป็น environment object ที่ `WBWApp.swift:11` อยู่แล้ว ไม่ต้องสร้างใหม่

- [ ] **Step 6: เพิ่ม launch flag สำหรับถ่ายรูปการ์ด**

สภาพแวดล้อมนี้แตะจอไม่ได้ ต้องเปิดการ์ดตรงๆ — ใน `Map3DScreen` เติมต่อจาก `.gesture(...)`:

```swift
        .onAppear {
            #if DEBUG
            // เปิดการ์ดฐานตรงๆ โดยไม่ต้องแตะจริง — ทรงเดียวกับ uitestChat/uitestFeedback ที่
            // MainTabView.swift เป็นทางเดียวที่ถ่ายรูปการ์ดได้ในสภาพแวดล้อมที่ไม่มี tap tooling
            let forced = UserDefaults.standard.integer(forKey: "uitestMapPin")
            if forced > 0 { tappedSequence = forced }
            #endif
        }
```

- [ ] **Step 7: รันชุดเต็ม**

Run: `xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'`

Expected: PASS · จำนวนเทส = 145 + 6 = **151**

- [ ] **Step 8: ถ่ายรูปยืนยันหมุดกับการ์ด**

```bash
xcodebuild -scheme WBW -configuration Debug \
  -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath /tmp/wbw-map3d build
xcrun simctl install booted /tmp/wbw-map3d/Build/Products/Debug-iphonesimulator/WBW.app
xcrun simctl terminate booted th.ac.mfu.wbwSwift
xcrun simctl launch booted th.ac.mfu.wbwSwift -uitestToken "$TOKEN" -uitestTab 1 -uitestMapPin 3
sleep 12
xcrun simctl io booted screenshot /tmp/wbw-map3d/02-pin.png
grep -c "ไม่พบหมุดชื่อ" /tmp/wbw-map3d/log.txt   # ต้องเป็น 0 ถ้าเก็บ log ไว้ด้วย
```

เปิดดู: ต้องเห็นแท่งแดงบนโมเดล และการ์ดล่างจอที่ไม่โดนแท็บบาร์ทับ

**ยังไม่จบ Task นี้จนกว่า Park จะยืนยันว่าแท่งไหนคือฐานไหน** — ส่งรูปให้ Park ดูแล้วถามว่าลำดับใน `Map3DPins.entityNames` ตรงกับของจริงไหม ถ้าไม่ตรงให้สลับลำดับในตารางแล้วถ่ายใหม่

- [ ] **Step 9: Commit**

```bash
git add WBW/Map3D/Map3DPins.swift WBW/Map3D/Map3DScreen.swift WBWTests/Map3DPinsTests.swift
git commit -m "feat(map): แตะแท่งแดงบนแผนที่แล้วขึ้นชื่อฐาน

ใช้แท่งที่มากับโมเดลอยู่แล้ว 8 แท่ง ไม่สร้างหมุดใหม่ · ชื่อฐานอ่านจาก CheckinProgressStore
ที่มีอยู่ ไม่ต่อ endpoint เพิ่ม

ฐานที่ยังไม่เช็คอินโชว์ \"ฐานที่ N\" เพราะ /wbw/me/progress คืนแค่ฐานที่เช็คอินแล้ว
ลิสต์ชื่อครบอยู่ที่ /wbw/staff/checkpoints ซึ่ง participant เรียกไม่ได้ — ไม่เดาชื่อ

การจับคู่แท่ง↔ฐานเป็นตารางที่ยืนยันด้วยตา ไม่ใช่ค่าที่พิสูจน์จากไฟล์ได้"
```

---

### Task 4: จุดตำแหน่งเรา

**ต้องได้ค่านี้ก่อนเริ่ม:** กรอบพิกัดจริงของโมเดล (`maps3d.json` หรือ `.blend` จาก Park) ถ้ายังไม่ได้ ให้ทำ Task 5 ก่อนแล้วย้อนมาทำอันนี้ — **ห้ามเดาค่าเงียบๆ**

ถ้า Park หาไฟล์ไม่เจอจริงๆ ใช้ค่าจาก export รอบ 2026-07-18 ที่สเปกเก่าบันทึกไว้ (SW `20.02371, 99.88272` / NE `20.06727, 99.92288`) **แล้วต้องเขียนคอมเมนต์กำกับในโค้ดว่าเป็นค่าสมมติจาก export คนละรอบ ยังไม่ได้พิสูจน์** และบันทึกข้อจำกัดนี้ในเอกสารยืนยัน Task 5

**Files:**
- Create: `WBW/Map3D/Map3DGeo.swift`, `WBW/Map3D/Map3DLocation.swift`
- Modify: `WBW/Map3D/Map3DScreen.swift`, `WBW/Info.plist`, `WBW/Info-Debug.plist`, `WBWTests/AppStoreConfigTests.swift:65`
- Test: `WBWTests/Map3DGeoTests.swift` (สร้างใหม่)

**Interfaces:**
- Consumes: `Map3DScreen` จาก Task 1-3
- Produces:
  - `Map3DGeo.Bounds` (struct: `south`, `west`, `north`, `east` — องศา)
  - `Map3DGeo.eventArea: Bounds` (ค่าจริงของโมเดล)
  - `Map3DGeo.modelPoint(latitude:longitude:in:) -> SIMD2<Float>?` (nil = นอกกรอบ)
  - `Map3DLocation` (`ObservableObject`, `@Published var coordinate: CLLocationCoordinate2D?`)

- [ ] **Step 1: เขียนเทสที่ยังไม่ผ่าน**

สร้าง `WBWTests/Map3DGeoTests.swift`:

```swift
import XCTest
@testable import WBW

/// แปลง lat/lng → พิกัดบนโมเดล · โมเดลกินพื้นที่ -1…1 ทั้งสองแกนหลังถูกย่อให้พอดีกรอบ
final class Map3DGeoTests: XCTestCase {

    private let bounds = Map3DGeo.Bounds(south: 20.0, west: 99.0, north: 21.0, east: 100.0)

    func testCenterLandsAtOrigin() {
        let p = Map3DGeo.modelPoint(latitude: 20.5, longitude: 99.5, in: bounds)
        XCTAssertEqual(p?.x ?? .nan, 0, accuracy: 0.01)
        XCTAssertEqual(p?.y ?? .nan, 0, accuracy: 0.01)
    }

    func testSouthWestCornerLandsAtMinusOne() {
        let p = Map3DGeo.modelPoint(latitude: 20.0, longitude: 99.0, in: bounds)
        XCTAssertEqual(p?.x ?? .nan, -1, accuracy: 0.01)
        XCTAssertEqual(p?.y ?? .nan, -1, accuracy: 0.01)
    }

    func testNorthEastCornerLandsAtPlusOne() {
        let p = Map3DGeo.modelPoint(latitude: 21.0, longitude: 100.0, in: bounds)
        XCTAssertEqual(p?.x ?? .nan, 1, accuracy: 0.01)
        XCTAssertEqual(p?.y ?? .nan, 1, accuracy: 0.01)
    }

    func testOutsideBoundsReturnsNil() {
        XCTAssertNil(Map3DGeo.modelPoint(latitude: 13.7, longitude: 100.5, in: bounds),
                     "กรุงเทพอยู่นอกพื้นที่งาน — ต้องไม่มีจุดบนโมเดล")
        XCTAssertNil(Map3DGeo.modelPoint(latitude: 20.5, longitude: 98.9, in: bounds))
    }

    func testEventAreaIsAWellFormedBox() {
        XCTAssertLessThan(Map3DGeo.eventArea.south, Map3DGeo.eventArea.north)
        XCTAssertLessThan(Map3DGeo.eventArea.west, Map3DGeo.eventArea.east)
    }
}
```

- [ ] **Step 2: รันเทสให้เห็นว่าพัง**

Run: `xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:WBWTests/Map3DGeoTests`

Expected: FAIL — compile error `cannot find 'Map3DGeo' in scope`

- [ ] **Step 3: สร้าง `WBW/Map3D/Map3DGeo.swift`**

```swift
import Foundation
import simd

/// แปลงพิกัดโลกจริง → พิกัดบนโมเดลแผนที่
///
/// โมเดลถูกย่อให้พอดีกรอบ 2 หน่วยตอนโหลด (ดู Map3DScreen) พิกัดที่คืนจึงอยู่ในช่วง -1…1
/// ทั้งสองแกน โดย x = ตะวันออก-ตะวันตก, y = เหนือ-ใต้ ผู้เรียกเป็นคนแปลงเป็นแกนของ RealityKit เอง
enum Map3DGeo {
    /// กรอบพิกัดสี่เหลี่ยมของพื้นที่งาน หน่วยเป็นองศา
    struct Bounds: Equatable {
        let south: Double
        let west: Double
        let north: Double
        let east: Double
    }

    /// กรอบจริงของ map.usdz — ค่าจากไฟล์ export ที่ Park ให้มา
    static let eventArea = Bounds(south: 20.02371, west: 99.88272,
                                  north: 20.06727, east: 99.92288)

    /// nil = อยู่นอกพื้นที่งาน (ไม่ต้องวาดจุด)
    ///
    /// ใช้การแมปเชิงเส้นตรงๆ ไม่แปลงเป็น EPSG:3857 ก่อน — ที่ละติจูด 20° กรอบกว้างราว 4 กม.
    /// ความบิดของ Mercator ในช่วงแคบขนาดนี้เล็กกว่าความคลาดของ GPS มือถือเอง
    static func modelPoint(latitude: Double, longitude: Double, in bounds: Bounds) -> SIMD2<Float>? {
        guard latitude >= bounds.south, latitude <= bounds.north,
              longitude >= bounds.west, longitude <= bounds.east else { return nil }
        let x = (longitude - bounds.west) / (bounds.east - bounds.west) * 2 - 1
        let y = (latitude - bounds.south) / (bounds.north - bounds.south) * 2 - 1
        return SIMD2<Float>(Float(x), Float(y))
    }
}
```

**ถ้าใช้ค่าสำรอง** (Park หาไฟล์ไม่เจอ) เปลี่ยนคอมเมนต์เหนือ `eventArea` เป็น:

```swift
    /// ⚠️ ค่าสมมติ — มาจาก export รอบ 2026-07-18 (สเปก 2026-07-31) ไม่ใช่ไฟล์ที่ใช้จริงตอนนี้
    /// ยังไม่เคยพิสูจน์ว่าตรงกับ map.usdz รอบนี้ พิสูจน์ได้ทางเดียวคือยืนที่ฐานจริงแล้วดูว่าจุดตรงไหม
```

- [ ] **Step 4: รันเทสให้ผ่าน**

Run: `xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:WBWTests/Map3DGeoTests`

Expected: PASS — 5 tests, 0 failures

- [ ] **Step 5: สร้าง `WBW/Map3D/Map3DLocation.swift`**

```swift
import CoreLocation
import Foundation

/// ตำแหน่งผู้ใช้สำหรับจุดบนแผนที่ — บางที่สุดเท่าที่พอใช้ ไม่มี background, ไม่มี heading
///
/// ไม่ให้สิทธิ์หรือหาไม่เจอ = coordinate เป็น nil เงียบๆ จอที่เหลือใช้งานได้ปกติ
/// (คนเปิดแอปจากบ้านก่อนวันงานเป็นเรื่องปกติ ไม่ใช่ความผิดพลาดที่ต้องแจ้ง)
@MainActor
final class Map3DLocation: NSObject, ObservableObject, CLLocationManagerDelegate {
    @Published private(set) var coordinate: CLLocationCoordinate2D?

    private let manager = CLLocationManager()

    override init() {
        super.init()
        manager.delegate = self
        manager.desiredAccuracy = kCLLocationAccuracyNearestTenMeters
        // 10 เมตร: จุดบนโมเดลกว้าง ~4 กม. ขยับถี่กว่านี้ตาไม่เห็นความต่าง แต่กินแบตเพิ่ม
        manager.distanceFilter = 10
    }

    func start() {
        manager.requestWhenInUseAuthorization()
        manager.startUpdatingLocation()
    }

    func stop() {
        manager.stopUpdatingLocation()
    }

    nonisolated func locationManager(_ manager: CLLocationManager,
                                     didUpdateLocations locations: [CLLocation]) {
        guard let last = locations.last else { return }
        Task { @MainActor in self.coordinate = last.coordinate }
    }

    nonisolated func locationManager(_ manager: CLLocationManager,
                                     didFailWithError error: Error) {
        Task { @MainActor in self.coordinate = nil }
    }
}
```

- [ ] **Step 6: เติมคีย์ขอสิทธิ์ทั้งสอง plist**

ใน `WBW/Info.plist` **และ** `WBW/Info-Debug.plist` เติมคู่คีย์นี้ (เรียงตามตัวอักษรให้เข้ากับของเดิม):

```xml
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>ใช้แสดงตำแหน่งของคุณบนแผนที่พื้นที่งาน ระหว่างเปิดแอปเท่านั้น</string>
```

- [ ] **Step 7: กลับด้านเทสที่ห้ามมีคีย์ตำแหน่ง**

`WBWTests/AppStoreConfigTests.swift:62-73` — คอมเมนต์ของเทสเดิมเขียนไว้เองว่า *"ถ้าวันหนึ่งเพิ่มฟีเจอร์แผนที่จริง ให้ลบเทสนี้พร้อมกับตอนเติมคีย์กลับ"* แทนที่ทั้งก้อนด้วย:

```swift
    /// เคยมีคีย์ตำแหน่งค้างอยู่ทั้งที่ไม่มีไฟล์ไหน import CoreLocation — ผู้ใช้เห็นข้อความขอสิทธิ์
    /// ที่ไม่มีวันถูกใช้ และเป็นเหตุให้ App Review ตีกลับได้ตรงๆ ตั้งแต่ 2026-08-07 แท็บ Map ใช้
    /// CoreLocation จริงแล้ว (Map3DLocation) เทสนี้จึงกลับด้าน: บังคับให้ **ต้องมี** คีย์
    /// ส่วนคลังรูปยังต้องห้ามมีเหมือนเดิม (PhotosPicker ไม่ต้องขอสิทธิ์)
    func testUsageDescriptionsMatchTheFrameworksTheAppActuallyUses() throws {
        for path in ["WBW/Info.plist", "WBW/Info-Debug.plist"] {
            let dict = try plist(path)
            let location = dict["NSLocationWhenInUseUsageDescription"] as? String
            XCTAssertFalse((location ?? "").isEmpty,
                           "\(path) ขาดคำอธิบายสิทธิ์ตำแหน่ง ทั้งที่ Map3DLocation ใช้ CoreLocation")
            XCTAssertNil(dict["NSPhotoLibraryUsageDescription"],
                         "\(path) ขอสิทธิ์คลังรูปทั้งที่ใช้ PhotosPicker ซึ่งไม่ต้องขอ")
        }
    }
```

- [ ] **Step 8: วาดจุดบนโมเดล**

ใน `WBW/Map3D/Map3DScreen.swift`:

ก) เติม property ที่หัว struct:

```swift
    @StateObject private var location = Map3DLocation()
```

ข) ใน closure ของ `RealityView` ต่อท้ายหลังบล็อกหมุดจาก Task 3 เติม:

```swift
                    // จุดตำแหน่งผู้ใช้ — สร้างไว้ก่อนแล้วซ่อน ค่อยย้ายตอนมีพิกัดจริง
                    // (สร้างทีหลังใน update closure ไม่ได้ เพราะ closure นั้นถูกเรียกทุกเฟรม)
                    let dot = ModelEntity(
                        mesh: .generateSphere(radius: 0.02),
                        materials: [UnlitMaterial(color: .systemBlue)]
                    )
                    dot.name = "UserDot"
                    dot.isEnabled = false
                    root.addChild(dot)
```

ค) เติม `update:` closure ให้ `RealityView` (พารามิเตอร์ตัวที่สอง ต่อจาก make closure):

```swift
                } update: { content in
                    guard let root = content.entities.first,
                          let dot = root.findEntity(named: "UserDot") else { return }
                    guard let coordinate = location.coordinate,
                          let point = Map3DGeo.modelPoint(latitude: coordinate.latitude,
                                                          longitude: coordinate.longitude,
                                                          in: Map3DGeo.eventArea) else {
                        dot.isEnabled = false
                        return
                    }
                    dot.isEnabled = true
                    // โมเดลกินพื้นที่ -1…1 บนระนาบ XZ ของ RealityKit (y คือความสูง)
                    // Map3DGeo คืน y เป็นแกนเหนือ-ใต้ จึงต้องสลับไปเป็น z และกลับเครื่องหมาย
                    // (แกน z ของ RealityKit ชี้เข้าหากล้อง = ทิศใต้)
                    dot.position = SIMD3<Float>(point.x, 0.05, -point.y)
                }
```

ง) เติมการเริ่ม/หยุดตามการเข้าออกจอ ต่อจาก `.onAppear` ของ Task 3:

```swift
        .onAppear { location.start() }
        .onDisappear { location.stop() }
```

- [ ] **Step 9: รันชุดเต็ม**

Run: `xcodegen generate && xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'`

Expected: PASS · จำนวนเทส = 151 + 5 = **156** (เทสที่กลับด้านใน `AppStoreConfigTests` ไม่ได้เพิ่มจำนวน)

- [ ] **Step 10: ยืนยันจุดด้วยตำแหน่งปลอมบนซิม**

```bash
xcrun simctl location booted set 20.045,99.9025      # กลางพื้นที่งาน
xcrun simctl launch booted th.ac.mfu.wbwSwift -uitestToken "$TOKEN" -uitestTab 1
sleep 12 && xcrun simctl io booted screenshot /tmp/wbw-map3d/03-dot-inside.png

xcrun simctl location booted set 13.7563,100.5018    # กรุงเทพ นอกพื้นที่
xcrun simctl terminate booted th.ac.mfu.wbwSwift
xcrun simctl launch booted th.ac.mfu.wbwSwift -uitestToken "$TOKEN" -uitestTab 1
sleep 12 && xcrun simctl io booted screenshot /tmp/wbw-map3d/04-dot-outside.png
```

Expected: รูปแรกมีจุดฟ้าบนโมเดล · รูปที่สองไม่มีจุดเลย แต่แผนที่ยังใช้ได้ปกติ
(ป๊อปอัพขอสิทธิ์ตำแหน่งจะโผล่รอบแรก ตอบไม่ได้ในสภาพแวดล้อมนี้ — ถ่ายรอบสองหลังมันหาย)

- [ ] **Step 11: Commit**

```bash
git add WBW/Map3D/Map3DGeo.swift WBW/Map3D/Map3DLocation.swift WBW/Map3D/Map3DScreen.swift \
        WBW/Info.plist WBW/Info-Debug.plist \
        WBWTests/Map3DGeoTests.swift WBWTests/AppStoreConfigTests.swift
git commit -m "feat(map): จุดตำแหน่งผู้ใช้บนแผนที่ 3D

แมปเชิงเส้นจากกรอบ lat/lng ของพื้นที่งานลงพิกัดโมเดล -1…1 ไม่แปลง EPSG:3857 ก่อน
เพราะกรอบกว้างราว 4 กม. ที่ละติจูด 20° ความบิดของ Mercator เล็กกว่าความคลาดของ GPS เอง

นอกกรอบหรือไม่ให้สิทธิ์ = ไม่มีจุด เงียบๆ จอที่เหลือใช้ได้ปกติ

เทสที่บังคับว่าห้ามมี NSLocationWhenInUseUsageDescription กลับด้านเป็นบังคับให้ต้องมี
ตามที่คอมเมนต์ของเทสนั้นเขียนสั่งไว้เองตั้งแต่ตอนเขียน"
```

---

### Task 5: ถอด MapLibre แล้ววัดของจริง

**Files:**
- Delete: `WBW/MapScreen.swift`
- Modify: `project.yml:8-10` (package `MapLibre`), `project.yml:21-22` (dependency), `docs/superpowers/specs/2026-07-31-map3d-glb-design.md` (หัวไฟล์)
- Create: `docs/map3d-verification.md`

**Interfaces:**
- Consumes: ทุกอย่างจาก Task 1-4
- Produces: ไม่มี symbol

- [ ] **Step 1: ยืนยันว่าไม่มีใครเรียก `MapScreen` แล้ว**

```bash
grep -rn "MapScreen\|MapLibre\|MLNMapView" WBW WBWTests
```

Expected: เจอเฉพาะใน `WBW/MapScreen.swift` เอง — ถ้าเจอที่อื่นให้แก้ก่อน

- [ ] **Step 2: ลบไฟล์กับ dependency**

```bash
git rm WBW/MapScreen.swift
```

ใน `project.yml` ลบสามที่:

```yaml
  MapLibre:
    url: https://github.com/maplibre/maplibre-gl-native-distribution
    from: 6.0.0
```

และในบล็อก `dependencies:` ของ target `WBW`:

```yaml
      - package: MapLibre
        product: MapLibre
```

- [ ] **Step 3: build สะอาดแล้วรันเทสชุดเต็ม**

```bash
xcodegen generate
xcodebuild test -scheme WBW -destination 'platform=iOS Simulator,name=iPhone 17'
```

Expected: PASS · จำนวนเทสยังเป็น **156** เท่าเดิม — การถอด dependency ไม่ควรทำให้เทสหาย
ถ้า build พังด้วย `no such module 'MapLibre'` แปลว่ายังมีไฟล์ที่ import อยู่ กลับไป Step 1

- [ ] **Step 4: Commit การถอด**

```bash
git add project.yml
git commit -m "refactor(map): ถอด MapLibre ออกจากโปรเจกต์

แท็บ Map ใช้โมเดล 3D แล้วตั้งแต่ Task 1 ไม่มีไฟล์ไหน import MapLibre เหลือ
ถอยกลับได้ด้วย git revert commit นี้ ไม่ใช่แค่กลับค่า flag"
```

- [ ] **Step 5: เติมสถานะที่หัวสเปกเก่า**

ใน `docs/superpowers/specs/2026-07-31-map3d-glb-design.md` เปลี่ยนบรรทัด
`- **Status:** Approved (design), pending implementation plan` เป็น:

```markdown
- **Status:** **ถูกแทนที่แล้วตั้งแต่ 2026-08-07** ด้วย `2026-08-07-map3d-usdz-design.md`
  ซึ่งทำเรื่องเดียวกันด้วยไฟล์ต้นทางคนละตัว (usdz ที่แปลงเสร็จแล้ว แทน GLB บนไดรฟ์นอกที่หายไป)
  เนื้อหาที่เหลือของไฟล์นี้ไม่แก้ เป็นบันทึกของตอนนั้น อย่าใช้เป็นคำสั่ง
```

ห้ามแก้เนื้อส่วนอื่นของไฟล์นี้

- [ ] **Step 6: วัดบนเครื่องจริง**

ข้อนี้คือข้อเดียวที่ตอบได้ว่าโมเดล 3D แลกมาด้วยอะไร ต้องใช้ iPhone จริง simulator วัดแทนไม่ได้

1. ติดตั้ง build ก่อนเริ่มงานนี้ — `git checkout main` (branch นี้แตกจาก `main` และก่อน Task 1
   มีแต่ commit เอกสาร) ลงเครื่อง เปิดค้างที่แท็บ Map (MapLibre) 5 นาที
   จด CPU จาก Xcode Debug Navigator + สังเกตอุณหภูมิ แล้ว `git checkout feature/map3d-usdz` กลับ
2. ติดตั้ง build ปัจจุบัน ทำแบบเดียวกัน 5 นาทีที่แท็บ Map (โมเดล 3D) จดค่าเดียวกัน
3. จดเวลาที่โมเดลใช้โหลดจนเห็นภาพครั้งแรกด้วย

**ถ้าโมเดลหนักกว่าจนใช้ไม่ไหว** ให้ตั้ง `Config.map3D = false` แล้วบันทึกตัวเลขไว้ตรงๆ —
ทางลดจริงคือลด mesh ใน Blender (join อาคารเป็นก้อนเดียว ลดขนาด texture) ไม่ใช่แก้โค้ดฝั่งแอป

- [ ] **Step 7: เขียนเอกสารยืนยัน**

สร้าง `docs/map3d-verification.md` โครงเดียวกับ `docs/forest-3d-off-verification.md`:
วิธีที่ใช้ (คำสั่ง build/launch จริง), สกรีนช็อตทั้งสี่รูปพร้อมสิ่งที่เห็น, ผลการยืนยันคู่หมุดกับ Park,
ตารางตัวเลข CPU/ความร้อน/เวลาโหลด ทั้งก่อนและหลัง, ข้อจำกัดที่รู้อยู่ (เช่น bbox เป็นค่าสมมติถ้าใช้ทางสำรอง)
และข้อสรุปที่ตรงกับตัวเลขจริง — **ห้ามเขียนว่าดีกว่า MapLibre ถ้าไม่มีตัวเลขรองรับ**

- [ ] **Step 8: Commit**

```bash
git add docs/map3d-verification.md docs/superpowers/specs/2026-07-31-map3d-glb-design.md
git commit -m "docs: ผลยืนยันแผนที่ 3D — สกรีนช็อต, คู่หมุด, ตัวเลขเครื่องจริง"
```

---

## ถ้าอยากถอยกลับไปใช้ MapLibre

`Config.map3D = false` ทำให้แท็บ Map เป็นการ์ดข้อความ **ไม่ใช่** MapLibre กลับมา —
ของเดิมถูกลบใน Task 5 ต้อง `git revert` commit นั้นถึงจะได้คืน
