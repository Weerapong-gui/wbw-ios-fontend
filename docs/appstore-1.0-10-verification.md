# บันทึกการตรวจ — รอบเตรียมส่ง App Review 1.0 (10)

วันที่ 2026-08-21 · ทุกข้อด้านล่างรันจริงบนเครื่องนี้ ไม่ได้อ่านโค้ดแล้วสรุปเอา ·
รอบนี้ต่างจาก `docs/appstore-1.0-8-verification.md` ตรงที่ **ฟีเจอร์ SOS เข้ามาด้วย**

## 1. ที่แก้ไปรอบนี้ เรียงตามความเสี่ยงโดนตีกลับ

| # | อาการ | Guideline | commit |
|---|---|---|---|
| P0-3 | เบอร์ฉุกเฉินจากเซิร์ฟเวอร์ต่อ `URL(string:)!` = crash บนจอฉุกเฉิน | 2.1 (crash) | `01fb994` |
| P0-1 | จอสถานะ SOS ขังคนกดไว้ถาวรเมื่อเคสยังเปิด (ในโหมดเดโม่เกิดแน่นอน) | 2.1 | `0cb8e83` |
| P0-2 | จอ SOS ฮาร์ดโค้ดภาษาไทย 37 จุด — ผู้รีวิวอ่านไม่ออกทั้งใบ | 2.1 | `e9d2682` |
| — | โหมดเดโม่ยังเด้งกล่องขอตำแหน่งทางจอ SOS | 5.1.1 | `2e49910` |
| P1-1 | ไม่มี `InfoPlist.strings` — กล่องขอสิทธิ์เป็นไทยล้วน | 5.1.1 | `f8ca2e3` |
| P1-3 | privacy manifest ประกาศ `PhotosorVideos` ทั้งที่ไม่มีตัวเลือกรูป | 5.1.2 | `3257db5` |
| P1-2 | ขอสิทธิ์ตำแหน่งทันทีที่ล็อกอิน ไม่มีบริบท | 5.1.1 | `f3b9caa` |
| P1-4 | SOS ไม่มีคำเตือนว่าไม่ใช่บริการฉุกเฉินจริง | 1.4.1 | รวมใน `0cb8e83` |
| P2-1/2 | พื้นที่รับนิ้ว < 44pt และปุ่มไอคอนไม่มีป้าย VoiceOver | 4.0 | `d229cce` |
| P2-3 | timeout 60 วิ + จอประกาศโกหกว่า "ยังไม่มีประกาศ" ตอนยิงไม่ถึง | 2.1 | `fa2a565` |
| — | แถว "ดูรายละเอียด ›" ในแท็บกิจกรรมกดไม่ได้ (รูปแบบเดียวกับที่เคยโดน 2.1 ที่หน้าล็อกอิน) | 2.1 | `e8d173c` |
| — | แท็บแผนที่ยังขอสิทธิ์ตำแหน่งในโหมดเดโม่ + บัตรการแพทย์เป็นอังกฤษล้วน | 5.1.1 | `8313eb6` |
| — | สกรีนช็อตชุดเดิมไม่ตรงกับแอปแล้วทั้ง 9 ใบ | **2.3.3** | `docs/appstore/README.md` |

## 2. build / test

| คำสั่ง | ผล |
|---|---|
| `xcodegen generate` | ผ่าน |
| `xcodebuild -configuration Debug ... test` | `** TEST SUCCEEDED **` · **504 เทส 0 fail** (ฐานเดิม 481 ก่อนงานนี้) |
| `xcodebuild -configuration Release -destination 'generic/platform=iOS Simulator' build` | `** BUILD SUCCEEDED **` |
| `./scripts/check-localization.sh` | ผ่าน · 383 คีย์ ครบสองภาษา ไม่มี format specifier ต่างกัน |
| `./scripts/check-skill-refs.sh` | `skill ยังตรงกับ repo` (แดงมาตั้งแต่ก่อนงานนี้ — `WBW/SOS/` ไม่เคยถูกใส่ในแผนที่โปรเจกต์) |

## 3. ตรวจ binary ที่จะส่งขึ้น store

รันบน `Release-iphonesimulator/WBW.app`:

| ตรวจอะไร | ผล |
|---|---|
| `login_demo_title` (ปุ่ม Demo) | **พบ** → โหมดเดโม่ติดไปกับ build จริง ไม่ได้อยู่ใน `#if DEBUG` |
| `uitestDemo` / `uitestToken` / `uitestSOSStatus` / `uitestLocationPrimer` / `uitestNotiLoadFailed` | **ไม่พบสักตัว** → launch args ทั้งชุดถูกตัดออกจาก Release |
| `en.lproj/InfoPlist.strings` + `th.lproj/InfoPlist.strings` | อยู่ใน bundle ทั้งคู่ |
| `PrivacyInfo.xcprivacy` → `PhotosorVideos` | **ไม่พบ** (ถอดออกแล้ว) |
| `NSAppTransportSecurity` ใน Info.plist | **ไม่พบ** → คีย์ LAN ไม่หลุดจาก Debug |
| `CFBundleVersion` | **10** (build 9 อัปไปแล้ว ส่งซ้ำ ASC ปฏิเสธตั้งแต่ตอนอัป) |
| `route_wbw.json` | อยู่ใน bundle |

## 4. ตรวจด้วยตาบน simulator — ตั้งเครื่องเป็น **English** ทุกใบ

`xcrun simctl erase` ก่อนถ่ายทุกครั้ง (dialog สิทธิ์ที่ค้างใน SpringBoard บังใบที่ถ่ายต่อจากนั้น)

| จอ | ยืนยันอะไร |
|---|---|
| กล่องขอสิทธิ์กล้อง | ขึ้น `"Doi Walk" would like to access the Camera.` พร้อมคำอธิบายอังกฤษเต็มใบ — ไม่ใช่ไทย |
| จอสถานะ SOS | ทุกข้อความเป็นอังกฤษ · ปุ่ม **Minimize · back to the app** โผล่ตอนสถานะ `.received` · คำเตือน "This alerts the event team, not emergency services. In a real emergency call 1669." อยู่ท้ายจอ |
| จออธิบายก่อนขอตำแหน่ง | เรนเดอร์จริง ปุ่มเป็นสีเขียวของแอป ไม่ใช่ฟ้าของระบบ |
| จอประกาศตอนยิงไม่ถึง | "Couldn't load announcements / Can't reach the server..." + ปุ่ม Retry — ไม่ใช่ "No announcements yet" |
| จอล็อกอิน | ปุ่มตาเปิด/ปิดรหัสผ่านโตขึ้นเฉพาะพื้นที่รับนิ้ว **กราฟิกคงเดิม** (ไอคอนไม่โต ช่องไม่บวม) |

## 5. บั๊กที่เจอระหว่างทำงานนี้ และแก้ไปแล้ว

**โหมดเดโม่ยังเด้งกล่องขอตำแหน่ง** — `Session.startDemo()` ตั้งใจไม่ขอสิทธิ์ด้วยเหตุผลนี้ตรง ๆ
มาตั้งแต่แรก แต่ `SOSStatusView.onAppear` เรียก `requestPermissionIfNeeded()` เองอีกทาง ผู้รีวิว
ที่กดปุ่ม SOS ในโหมดเดโม่จึงยังเจอกล่องอยู่ดี · ประตูย้ายไปอยู่ที่ `SOSLocator` จุดเดียว

**เทสที่เขียวทั้งที่ตรวจอะไรไม่ได้เลย** — ตัวกวาดพื้นที่รับนิ้วเขียนผิดสองรอบและ**เขียวทั้งสองรอบ**:
รอบแรกหน้าต่าง N บรรทัดตายตัวตัดกลางปุ่มจนหาเจอ 2 ตัวจากทั้งแอป รอบสอง guard `Label(` ดันไป
แมตช์คำว่า `accessibilityLabel` เอง ปุ่มที่ติดป้ายถูกแล้วเลยถูกข้ามหมด เหลือตรวจได้จริงปุ่มเดียว
· จึงมีเทสคู่ที่นับจำนวนที่กวาดเจอไว้ด้วย (`testTheSweepActuallyFindsIconOnlyButtons`) — ทรงเดียว
กับที่ `LocalizationDashTests` ทำอยู่แล้ว

**`git add` ทีละไฟล์ยังไม่พอ — ไฟล์เดียวก็มีงานสองเจ้าของได้** commit a11y รอบแรกดึงงาน
ที่ยัง uncommitted อยู่ใน `Map3DScreen.swift` (ฟีเจอร์วงแสงหมุด `spotlightSequence`) ติดไป
ด้วยทั้งก้อน — 9 จาก 10 hunk ในไฟล์นั้นไม่ใช่ของงานนี้เลย · กติกาข้อ 7 เขียนว่าห้าม
`git add -A` ซึ่งทำตามแล้ว แต่กับดักจริงอยู่ลึกกว่านั้น: **ไฟล์ที่ตัวเองแตะอาจมีงานของคนอื่น
ค้างอยู่ในไฟล์เดียวกัน** ต้องดู `git diff <ไฟล์>` ก่อน add ทุกครั้ง ไม่ใช่แค่เลือกชื่อไฟล์
· แก้ด้วยการเขียน history ใหม่ (สาขายังไม่ push) โดยมี tag `backup/before-a11y-split-20260822`
สำรองไว้ · ยืนยันหลังแยกแล้ว: commit a11y ตัวใหม่ build ผ่านเดี่ยว ๆ โดยไม่ต้องพึ่ง WIP,
`git diff <tag>` เหลือเฉพาะ WIP ที่ยัง uncommitted อยู่เดิม ไม่มีอะไรหาย, และเทส 502 ตัวยังผ่าน

**สถานะซิมูเลเตอร์ค้างหลอกให้ไล่หาสาเหตุผิด** — `-uitestNotifications` ไม่เปิดชีตอยู่สองรอบ
ทั้งที่โค้ดถูก หายไปเองหลัง `simctl erase` · ตรงกับกับดักเดิมที่ `build-and-run.md` จดไว้แล้ว

## 6. ที่ยังไม่ได้ทำ — ต้องเสร็จก่อนกด Submit

1. **ยังไม่ได้รันบนเครื่องจริงเลยสักครั้ง** (ข้อเดียวกับรอบที่แล้วที่ยังค้างอยู่) — สามอย่างที่
   simulator พิสูจน์ไม่ได้: `CMPedometer`, ความแม่นของ GPS, และ **`map.usdz` ขนาด 9.4 MB ที่มี
   entity 2,201 ก้อน** บนแท็บ Map ซึ่ง `Config.map3D = true` เปิดไว้โดยยังไม่เคยวัดบนฮาร์ดแวร์จริง
   (`WBW/Config.swift` เขียนไว้เอง) — หน่วยความจำไม่พอเมื่อไหร่คือ crash ตอนรีวิว
2. **Privacy Policy ยังไม่ถูกเผยแพร่** — `docs/privacy-policy-draft.md` มี `⟨ตัดสิน⟩` ค้าง 4 จุด
   ASC บังคับให้กรอก URL ที่เปิดได้จริง และแอปนี้เก็บข้อมูลสุขภาพซึ่งผู้ตรวจจะเปิดอ่าน
3. **Privacy Nutrition Label ใน ASC** ยังไม่ได้กรอกให้ตรงกับ manifest ที่แก้แล้ว
4. **สองจอเขียนเสร็จแล้วแต่เข้าไม่ถึง** — `SURunView` (จับระยะเดิน/นับก้าว มีเทสคุมสองไฟล์)
   และ `MyQRCodeView` (QR เต็มจอ) ไม่มีโค้ดไหนในแอปอ้างถึงเลย มีแค่ `#Preview` ของตัวเอง ·
   รอบนี้ตัดสินใจถอดลูกศร "ดูรายละเอียด" ที่พาไปไม่ถึงไหนออกก่อน ยังไม่ต่อจอเข้าด้วยกัน —
   โค้ดกับ asset ของทั้งสองจอยังถูกแพ็กไปกับ .ipa อยู่

## 7. สกรีนช็อต — ถ่ายใหม่ทั้งชุดแล้ว 2026-08-22

ชุดเดิม (2026-08-19, 9 ใบ) **ใช้ไม่ได้แล้วทั้งชุด** แอปถูกออกแบบใหม่ระหว่างนั้น: หน้า Home
จากภาพวาดสว่างแท็บมีไอคอนคนวิ่ง เป็นภาพถ่ายมืดแท็บคนละชุด · หน้าบัตรจากป้ายห้อยขาว+บาร์โค้ด
เป็นการ์ดกระจก+QR · ส่งรูปที่ไม่ตรงกับแอปคือเดินเข้าไปเจอ **2.3.3 ข้อเดิมซ้ำ**

ชุดใหม่ 10 ใบ × 2 ขนาด ถ่ายจากโหมดเดโม่ทั้งหมด (`docs/appstore/README.md` มีตาราง launch args
ครบ) · สองใบเดิมที่หายไปคือ `02-surun` กับ `09-qr` เพราะจอนั้นเข้าไม่ถึงแล้ว (ดูข้อ 6.4)
แทนด้วย `05-activities` กับ `09-medical`

กับดักสามอย่างที่เจอตอนถ่าย และจดไว้ใน README แล้ว:

1. **กล่องขอสิทธิ์ค้างบัง 9 ใบจาก 10** — `Map3DLocation.start()` ไม่มีประตูกันโหมดเดโม่
   (รอยที่สองของรอยเดียวกับ `SOSLocator`) แก้แล้วพร้อมเทสกวาดทั้ง repo
2. **แบนเนอร์แจ้งเตือนของ iOS เองบังหัวจอ** ("พร้อมแล้วสำหรับ Apple Intelligence") ตอนถ่ายใบ 6.9
3. **`02-map` ต้องรอนานกว่าใบอื่นมาก** — `map.usdz` 9.4 MB · 6.5 ใช้ 40 วิ ส่วน 6.9 ต้องถึง 90 วิ
   ไม่งั้นได้ตัวหมุน "กำลังโหลดแผนที่" แทนโมเดล **(ตัวเลขนี้คือหลักฐานว่าข้อ 6.1 ยังจำเป็น —
   ซิมูเลเตอร์บนเครื่องนี้ยังใช้เวลาระดับนั้น เครื่องจริงยังไม่เคยวัด)**
