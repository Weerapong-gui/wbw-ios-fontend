# ทุกอย่างที่ App Store Connect ต้องการ แต่ repo นี้ให้ไม่ได้

ทรงเดียวกับ `docs/app-store/connect-checklist.md` ของโปรเจกต์ Club Fair —
รายการติ๊กของสิ่งที่ **โค้ดทำแทนไม่ได้** ต้องมีคนไปกดหรือไปตัดสิน

เขียน 2026-08-22 · อัปเดต 2026-08-25 สำหรับ **build 1.0 (13)**

สิ่งที่ตรวจแล้วในโค้ดอยู่ที่ `docs/appstore-1.0-10-verification.md` ไม่ซ้ำที่นี่

---

## ปลายทางคือ record เดิม `th.ac.mfu.wbwSwift` — สลับไป Club Fair แล้วกลับมา (2026-08-25)

**วันเดียวกันนี้เคยเปลี่ยน `PRODUCT_BUNDLE_IDENTIFIER` เป็น `th.ac.mfu.su.clubfair`
(Apple ID 6802118399) แล้ว *กลับมาที่รายการเดิม*** ตามที่เจ้าของงานสั่ง · ตอนนี้โปรเจกต์
เป็น `th.ac.mfu.wbwSwift` · **1.0 (13)** อีกครั้ง

เหตุผลที่กลับมา: ใบตีกลับ **5.1.1(iv)** ของ 1.0 (12) ค้างอยู่ที่รายการนี้ · จะตอบ
Resolution Center ของรายการไหนก็ต้องอัป build ใหม่เข้ารายการนั้น ข้ามไปตอบจากรายการอื่นไม่ได้

**เลข build ถัดไปคือ 13** — 12 อัปขึ้น ASC ไปแล้ว (ตัวที่โดนตีกลับ) ส่งซ้ำ ASC ปฏิเสธตั้งแต่
ตอนอัป · เลข 7 ที่เคยตั้งไว้เป็นของสายรายการ Club Fair ซึ่งนับแยกกัน — ถ้าวันหลังสลับกลับไป
รายการนั้นอีก ต้องกลับไปใช้สายเลขของมัน ไม่ใช่นับต่อจาก 13

### สิ่งที่ต้องทำนอก repo ก่อน archive รอบนี้

- [ ] **เอา `GoogleService-Info.plist` ของ bundle เดิมกลับมา** — ไฟล์ที่วางอยู่บนเครื่องตอนนี้
      เป็นของ `th.ac.mfu.su.clubfair` (ไฟล์นี้อยู่ใน `.gitignore` git ไม่ได้ย้อนให้)
      **BUNDLE_ID ไม่ตรงแล้ว push ตายเงียบ ไม่มี error ให้เห็นเลย** — โหลดใหม่จาก Firebase
      project `wbw-doi` ที่ iOS app ของ `th.ac.mfu.wbwSwift`
- [ ] **ทดสอบ push บนเครื่องจริงก่อน archive** — ASC ไม่มีทางบอกว่า push เสีย
- [ ] เมทาดาทาใน ASC ของรายการนี้ยังเป็นชุดที่แก้ไว้แล้วรอบ 1.0 (11)/(12) — ตรวจซ้ำเฉพาะ
      ลำดับอัปโหลดข้างล่าง ไม่ต้องรื้อใหม่ทั้งชุดเหมือนตอนจะย้ายไป Club Fair

**สิ่งที่ไม่ต้องทำแล้วเพราะกลับมารายการเดิม:** เปิด Push ให้ App ID `th.ac.mfu.su.clubfair`
ในพอร์ทัล (App ID เดิมมี entitlements ครบอยู่แล้ว) · รื้อเมทาดาทาทั้งชุดของ Club Fair
(ชื่อ/สกรีนช็อต/description/age rating/Privacy label/บัญชีรีวิว)

### ความเสี่ยงที่หายไปพร้อมกับการกลับมา

ตอนจะย้ายไป Club Fair มีข้อรับความเสี่ยงไว้ว่า คนที่ติดตั้ง Club Fair อยู่จะได้ WBW แทนตอน
อัปเดต และจะมีแอปเนื้อเดียวกันสองรายการในสโตร์พร้อมกัน (เป็นเหตุตีกลับที่กระทบทั้งสองรายการ)
· กลับมารายการเดิมแล้วทั้งสองข้อนี้หมดไป

---

## บันทึกของรายการนี้ `th.ac.mfu.wbwSwift`

รอบ 1.0 (7) โดนตีกลับ 2 ข้อเมื่อ 2026-08-09 (submission `30d177e2-a7d0-4be5-9050-5f47f2ba69f3`)
คือ Guideline 2.1 กับ 2.3.3 · เนื้อหาข้างล่างทั้งหมดเป็นของรายการนี้ ซึ่งเป็นปลายทางของ build
อีกครั้งตั้งแต่ 2026-08-25

## สถานะล่าสุด — **1.0 (12) โดนตีกลับ ข้อเดิมซ้ำรอบที่สอง**

**ตีกลับเมื่อ 2026-08-25 · Guideline 5.1.1(iv) อีกครั้ง** · ตรวจบน **iPad Air 11-inch (M3)
กับ iPhone 17 Pro Max** · submission ID `30d177e2-a7d0-4be5-9050-5f47f2ba69f3`

รอบ 1.0 (11) โดนเพราะ **คำบนปุ่ม** ("Allow Location") ซึ่งแก้เป็น `action_continue` ไปแล้ว ·
รอบนี้โดนเพราะ **ปุ่มที่สองบนจอเดียวกัน**:

> *"A custom message appears before the permission request, and the user can close the message
> and delay the permission request with the Not now button. The user should always proceed to
> the permission request after the message."*

**บทเรียนที่ต้องไม่ลืม:** ข้อความตอบรอบที่แล้ว (`review-reply-1.0-12.md`) เขียนเองว่า *"The
secondary button is unchanged and still reads 'Not now'"* — คือชี้จุดที่โดนตีกลับรอบนี้ให้ผู้ตรวจ
เห็นเอง · **จอที่คั่นก่อนกล่องขอสิทธิ์ต้องมีปุ่มเดียว และปิดได้ทางเดียวคือปุ่มนั้น**

- [x] **แก้แล้ว 2026-08-25 (ยังไม่ได้ archive)** — `LocationPrimerSheet` เหลือปุ่มเดียว
      (`action_continue` → กล่องของระบบ) · ปุ่ม "ไว้ทีหลัง" หายทั้งจากจอและจากชุดคีย์สองภาษา ·
      ปัดชีตลง/แตะนอกกรอบชีตบน iPad ปิดไม่ได้แล้ว (`interactiveDismissDisabled` + ซ่อนแถบลาก
      + detent เดียว `.large`) · ธง `LocationPrimer.dismissed` กลายเป็น `asked` **คนละคีย์ใน
      UserDefaults** เพื่อให้คนที่เคยกด "ไว้ทีหลัง" ถูกถามใหม่ · `WBWTests/PermissionCopyTests`
      สองเคสใหม่กันไม่ให้ปุ่ม/ทางปัดกลับมา · เทสทั้งชุด 651 ตัวผ่าน · ถ่ายจอยืนยันแล้วทั้ง
      iPhone 17 และ iPad Pro 13 (M4) ผ่าน `-uitestDemo 1 -uitestLocationPrimer 1`
- [x] **ตัดสินใจแล้ว 2026-08-25: ส่งขึ้นรายการเดิม** — โปรเจกต์กลับมาเป็น `th.ac.mfu.wbwSwift`
      · **1.0 (13)** ครบทั้งสามที่ (`project.yml` + สอง plist) เหลือ archive แล้วอัป
- [ ] **ตอบ Resolution Center ด้วย `docs/appstore/review-reply-1.0-13.md`** — หลังอัป build
      ใหม่เสร็จเท่านั้น (ลำดับข้อ 1 ก่อนข้อสุดท้ายเสมอ)

ระหว่างรอรีวิวรอบถัดไป **ห้ามทำสามอย่างนี้**:

1. **ห้ามลบหรือปิดบัญชีรีวิว** `6939999999` ออกจาก production — รอบ 1.0 (7) โดนตีกลับ
   เพราะบัญชีหายไประหว่างรอรีวิว
2. **ห้ามเอาหน้า `/privacy` กับ `/support` ลง** และห้ามแก้ให้ขัดกับ Nutrition Label
   (Apple เปิดสองหน้านี้ระหว่างตรวจ)
3. **ห้ามสลับ `Config.backend`** หรือปิด `api.studentunion.social`

จะส่ง build ใหม่ต้องกด "remove this version from review" ก่อน — ระหว่างนี้แก้เมทาดาทา
บางช่องได้ แต่เปลี่ยน build ไม่ได้

## บล็อกการส่ง — ต้องเสร็จก่อน ไม่มีทางลัด

- [x] **นโยบายขึ้นเว็บแล้ว** — https://walkbeyondthewild.studentunion.social/privacy
      ขึ้นเมื่อ 2026-08-22 · ไทย/อังกฤษสลับตามปุ่มภาษาของเว็บ ฉบับไทยเป็นฉบับอ้างอิง
      · โค้ดอยู่ repo `su-wbw-website` ที่ `app/privacy/` กับ `lib/i18n/dictionaries.ts`
      (คีย์ `privacy`) แก้แล้วต้อง build image ใหม่ นโยบายถึงเปลี่ยนบนเว็บ
      · **ต้องมีคนอ่าน `student-union@lamduan.mfu.ac.th` จริง** เพราะนโยบายสัญญา
      ว่าลบบัญชีให้ภายใน 7 วัน
- [x] **Support URL** — https://walkbeyondthewild.studentunion.social/support
      หน้าใหม่ ขึ้นเมื่อ 2026-08-23 · ไทย/อังกฤษ มีอีเมล ปุ่ม SOS ไม่ใช่ 1669
      สิทธิ์ตำแหน่ง วิธีขอลบบัญชี และ FAQ อีก 4 กลุ่ม · **ไม่ใช้ `/contact` แล้ว**
      เพราะหน้านั้นมีแค่ชื่อหน่วยงานกับไอคอนโซเชียล ไม่มีทางติดต่อที่ผู้ตรวจใช้ได้จริง
      (หน้า `/contact` ยังอยู่ และมีลิงก์ต่อมาที่หน้านี้)
- [x] **กรอก URL ใน ASC แล้ว 2026-08-23** — Support URL อยู่หน้าเวอร์ชัน (ของเดิมชี้ Instagram) ·
      Privacy Policy URL อยู่หน้า App Privacy (ของเดิมชี้ `/landing` ไม่ใช่หน้านโยบาย) ·
      ลบ User Privacy Choices URL ที่ชี้ `/landing` ทิ้งด้วย
- [ ] **บัญชีรีวิวยังใช้ได้** — **สำคัญกว่าเดิมตั้งแต่ 2026-08-25 ที่ปุ่ม "ดูตัวอย่างแอป (Demo)"
      ถูกถอดออกจากหน้าล็อกอิน: นี่คือทางเข้าแอปทางเดียวของผู้ตรวจแล้ว บัญชีล้ม = Guideline 2.1
      ทันที** (ทางแก้เร็วสุดคือเอาปุ่มกลับ ดูย่อหน้าใน `WBW/LoginView.swift`) ·
      ยิงซ้ำ 2026-08-25 ได้ **HTTP 200** ใน 0.35 วิ คืน `role: participant` ·
      ยิงซ้ำล่าสุดก่อนหน้า 2026-08-23 ได้ **HTTP 200** ใน 0.38 วิ
      คืน `role: participant` (ก่อนหน้านี้ยิงเมื่อ 2026-08-22 ก็ได้ 200)
      (`POST https://api.studentunion.social/wbw/auth/login`, `6939999999` /
      `WbwReview2026!`) · ยิงซ้ำอีกครั้งก่อนกด Submit จริง ไม่ใช่เชื่อบรรทัดนี้ —
      รอบ 1.0 (7) โดนตีกลับเพราะบัญชีหายไปจาก production ระหว่างรอรีวิว

## ลำดับการอัปโหลด — สลับลำดับแล้วโดนตีกลับซ้ำ

รอบที่แล้วตอบข้อความใน Resolution Center ตอนที่ ASC ยังมีแต่ build เก่า ผู้ตรวจกดทดสอบ
ตัวที่ยังไม่มีปุ่ม Demo แล้วตีกลับ 2.1 ซ้ำ · **ข้อ 1 ต้องเสร็จก่อนข้อสุดท้ายเสมอ**

- [ ] 1. Archive แล้วอัปโหลด **build 1.0 (13)** · `CURRENT_PROJECT_VERSION` กับ
      `CFBundleVersion` ทั้งสอง plist เป็น `13` แล้ว (12 อัปไปแล้วและโดนตีกลับ —
      เลขที่เคยอัปขึ้น ASC แล้วส่งซ้ำไม่ได้ ASC ปฏิเสธไฟล์ตั้งแต่ตอนอัป) ·
      `WBWTests/AppStoreConfigTests` ตรวจว่าเลขในสามที่ตรงกัน **และตรวจ bundle id ว่าชี้
      รายการนี้จริง** บั๊มไม่ครบหรือ bundle เพี้ยนเทสแดง
- [ ] 2. **เลือก build 13 ให้ submission** — ตอนนี้ยังผูก build 12 อยู่ (ตัวที่โดนตีกลับรอบนี้)
- [ ] 3. อัปสกรีนช็อต (ดูหัวข้อถัดไป) · **ถ่ายใหม่แล้วทั้ง 10 ใบ 2026-08-24 ทั้งสองขนาด**
      — นอกจาก `02-map` แล้วอีกสองใบที่เก่ากว่าแอปจริงคือ `10-sos` (บรรทัดเตือนว่าไม่ใช่
      หน่วยกู้ชีพเพิ่มมาเมื่อ 2026-08-23) กับ `08-feedback` (การ์ดขาวที่ตัวอักษรหายไปกับพื้น
      ในโหมดมืด — แก้ที่โค้ดแล้ว ไม่ใช่แค่ถ่ายใหม่) · รายละเอียดใบ `02-map`:
      (`screenshots/6.5/02-map.png` 1242x2688 · `screenshots/6.9/02-map.png` 1320x2868)
      เป็นจอแผนที่ 2 มิติพร้อมการ์ดฐาน "จุดปลูก" ที่เปิดค้างไว้ ตรงกับค่าเริ่มต้นของแท็บแผนที่
      ที่เปลี่ยนเมื่อ 2026-08-24 — ใบเดิมเป็นจอ 3 มิติ ซึ่งเป็นเหตุ **2.3.3** ตรง ๆ ·
      **เหลือแค่กดอัปใน Media Manager** ใบอื่นอีก 9 ใบยังใช้ของเดิมได้
      · หมายเหตุ: คำสั่ง `-uitestTab 1 -uitestMapPin 5` เพิ่งใช้ได้จริงหลังแก้บั๊กที่แฟลก
      ถูกกลืนในโหมด 2 มิติ (คอมมิตเดียวกับที่ถ่ายใบนี้)
- [ ] 4. กรอก Privacy Nutrition Label (ดูหัวข้อถัดไป)
- [ ] 5. ใส่ Privacy Policy URL กับ Support URL
- [ ] 6. เติมประโยคเรื่อง SOS ลงใน App description (ดูหัวข้อถัดไป)
- [ ] 7. **ต้องวาง Notes ชุดใหม่ `docs/appstore/review-notes-1.0-13.md`** — ชุดที่วางไว้
      2026-08-23 (ตัดจาก `review-reply-1.0-11.md`) **เล่าถึงปุ่ม "ดูตัวอย่างแอป (Demo)"
      ที่ถอดออกแล้ว 2026-08-25** ผู้ตรวจหาตามโน้ตแล้วไม่เจอ = โดนตีกลับเหมือนของหาย ·
      ชุดใหม่ 3,122 ตัวอักษร (เพดาน 4,000) · **ก่อนวาง: ล็อกอิน `6939999999` เปิดแท็บแชท
      แล้วดูว่ามีข้อความของคนอื่นให้กดบล็อก/รายงานจริงไหม** ไม่มีแล้ว Guideline 1.2
      จะกลายเป็น "ของมีแต่ผู้ตรวจหาไม่เจอ" เพราะทางเดโม่ที่เคยใช้สาธิตไม่มีแล้ว
- [ ] 8. **แล้วค่อย**ตอบข้อความใน Resolution Center ด้วยเนื้อหาไฟล์เดียวกัน

## Connect metadata

- [x] **Privacy Nutrition Label แก้แล้ว 2026-08-23** — ของเดิมประกาศ **14 ประเภท**
      ที่แอปไม่ได้เก็บเลย (Advertising Data, Photos or Videos, Crash/Performance Data,
      Hands, Head, Environment Scanning, Emails or Text Messages ฯลฯ) และ User ID ตั้งเป็น
      Product Personalization · ตอนนี้เหลือ 5 ประเภทตรงกับ manifest ทุกตัวเป็น
      App Functionality + Linked + ไม่ track
- [ ] (อ้างอิง) **Privacy Nutrition Label** — ตั้งให้ตรงกับ `WBW/PrivacyInfo.xcprivacy`
      **เป๊ะ ๆ** ตอนนี้ประกาศ 5 ประเภท ทุกตัวผูกกับตัวตน (`Linked = true`) ไม่มีตัวไหน
      ใช้เพื่อติดตาม (`Tracking = false`) และวัตถุประสงค์เป็น App Functionality ทั้งหมด:

      | ประเภทใน manifest | ตอบใน ASC ว่าคืออะไร |
      |---|---|
      | `UserID` | รหัสนักศึกษาที่ใช้เข้าสู่ระบบ |
      | `Name` | ชื่อบนบัตรผู้เข้าร่วมและในแชท |
      | `OtherUserContent` | ข้อความในแชทกลุ่ม + ความเห็นต่อฐาน |
      | `DeviceID` | device token สำหรับ push |
      | `PreciseLocation` | พิกัดตอนกดปุ่ม SOS เท่านั้น |

      `NSPrivacyTracking = false` และ `NSPrivacyTrackingDomains` ว่าง → ตอบ **No**
      ที่คำถามเรื่อง tracking · **ห้ามเพิ่มประเภทที่ manifest ไม่มี** และห้ามลดด้วย
      Apple เทียบสองอย่างนี้กับหน้าเว็บนโยบาย (ข้อ 9 ของนโยบาย) เอง

      **ข้อมูลสุขภาพไม่ต้องประกาศ** เพราะแอปไม่ได้เก็บ ผู้ใช้กรอกบนเว็บลงทะเบียน
      แอปแค่แสดง — ตรวจแล้วว่าไม่มี endpoint ไหนในแอปเขียนข้อมูลสุขภาพ
      (`WBW/APIClient.swift`) · ถ้าตัดสินใจตอบว่าเก็บ ต้องเพิ่มใน manifest ก่อน
      ซึ่งเป็นการแก้โค้ด ไม่ใช่แก้แค่แบบสอบถาม

- [x] **สกรีนช็อต — ส่งไปด้วย 7 ใบ** ของเก่า 6 ใบ (หน้าแรก / แชท / ตั้งค่า / ล็อกอิน / กลุ่ม /
      จอต้อนรับ) บวก `01-home.png` ที่อัปใหม่ · ตรวจภาพแล้วทุกใบเป็นจอที่มีจริงในแอป
      ไม่มีใบไหนโฆษณาฟีเจอร์ที่ถอดไปแล้ว จึงไม่เป็นความเสี่ยง 2.3.3
      · รอบหน้าถ้าจะเปลี่ยนเป็นชุด 10 ใบที่ถ่ายใหม่ ต้องกด Delete All เอง (ผู้ช่วยกดลบทั้งชุดไม่ได้)
      (1242×2688) และ `/6.9/` (1320×2868) ถ่ายใหม่ทั้งชุด 2026-08-22 จากโหมดเดโม่
      · **อัปโหลดต้องทำมือ** เมนู Previews and Screenshots → "View All Sizes in
      Media Manager" · โฟลเดอร์นี้อยู่ใน `.gitignore` ไฟล์จึงมีแค่บนเครื่องที่ถ่าย
      ถ้าย้ายเครื่องต้องถ่ายใหม่ตามขั้นตอนใน `docs/appstore/README.md`

- [x] **App description เขียนใหม่แล้ว 2026-08-23** — ของเดิมโฆษณาฟีเจอร์ที่ไม่มี
      (ติดตามระยะทาง/ความเร็ว · สะสมแต้ม-เหรียญตรา · สถิติย้อนหลัง) ซึ่งคือเหตุ 2.3.3 ที่โดนตีกลับ
      · ตัวใหม่เล่าเฉพาะจอที่มีจริงและมีประโยค SOS ครบ · **หมวดจริงใน ASC คือ Sports (รอง Travel)**
      ไม่ใช่ Health & Fitness อย่างที่เอกสารเคยเขียน แต่ผู้ตรวจอ่านการอ้างเรื่องฉุกเฉินละเอียดเหมือนกัน
- [x] **Age rating = 12+** — ตั้งใน ASC แล้ว (global 12+ · บางประเทศ 13+) เมื่อ 2026-08-23 · ไม่ใช่ 4+ เพราะแอปมีแชทกลุ่ม
      ซึ่งเป็น user-generated content · ตอบแบบสอบถามให้ตรงกับความจริงข้อนี้:
      มีข้อความระหว่างผู้ใช้ (กลุ่มปิด สมาชิกยืนยันตัวตนแล้ว) มีเครื่องมือรายงานและบล็อก
      ไม่มีเนื้อหาโป๊/รุนแรง/การพนัน/แอลกอฮอล์ · ดูหัวข้อ UGC ด้านล่างประกอบ

- [ ] **Demo Account** ในหน้าเวอร์ชัน — `6939999999` / `WbwReview2026!`
      ค่าเดิมที่ Apple ถืออยู่ใช้ได้แล้ว ไม่ต้องแก้ ถ้าไม่ได้เปลี่ยนรหัส

## UGC (Guideline 1.2) — ต้องชี้ให้ผู้ตรวจเห็น ไม่ใช่แค่มีในโค้ด

แชทกลุ่มคือ user-generated content · ทำครบสี่อย่างที่ Apple บังคับแล้วเมื่อ 2026-08-23:

- **บล็อก** — โปรไฟล์สมาชิกในกลุ่ม (แชท → แตะชื่อกลุ่ม → แตะสมาชิก) มีปุ่ม "บล็อกผู้ส่ง"
  และเมนูกดค้างบนฟองข้อความก็ทำได้เหมือนกัน
- **ปลดบล็อก** — ตั้งค่า → "ผู้ใช้ที่บล็อกไว้"
- **รายงาน** — กดค้างบนฟองข้อความ → "รายงานข้อความนี้" เปิดอีเมลที่กรอกข้อมูลไว้แล้ว
  (id ข้อความ ผู้ส่ง เวลา) ส่งไป `student-union@lamduan.mfu.ac.th`
- **ช่องทางติดต่อที่เผยแพร่** — หน้า /support กับ /privacy บนเว็บ

- [ ] **ย่อหน้า UGC ใน App Review Notes** — อยู่ใน `docs/appstore/review-reply-1.0-11.md`
      แล้ว (หัวข้อ "Group chat and user-generated content") · **ต้องวางไปด้วยจริง ๆ**
      ผู้ตรวจที่หาเมนูบล็อกไม่เจอจะตีกลับทั้งที่ของมีอยู่

## ข้อที่ต้องมีคนตัดสิน ไม่ใช่ช่องกรอก

- [ ] **ผู้เข้าร่วมอายุต่ำกว่า 20 ปี** — ตรวจ DB จริงแล้ว: ตาราง `consent` มีแค่
      `consent_health_data`, `consent_emergency_treatment`, `waiver_accepted`
      **ไม่มีคอลัมน์ความยินยอมของผู้ปกครองเลย** ทั้งที่ `date_of_birth` มีอยู่จึงรู้อายุ
      · ข้อ 8 ของนโยบายจึงเขียนว่าผู้จัดดำเนินการนอกระบบลงทะเบียน ซึ่งเป็นความจริง
      ตามที่ระบบเป็นอยู่ · **ถ้าจะเปลี่ยนไปเขียนว่าเว็บเก็บความยินยอมผู้ปกครอง
      ต้องมีขั้นตอนนั้นจริงและเก็บหลักฐานก่อน** ไม่ใช่แก้แค่ข้อความ

- [x] **ลิงก์นโยบายในหน้าตั้งค่าของแอป** — เพิ่มแล้ว 2026-08-23 · หมวด "ทั่วไป" มีสองแถว
      ออกเว็บต่อจากเครดิต: นโยบายความเป็นส่วนตัว กับ ติดต่อทีมงานบนเว็บ (`SettingsWebLink`
      ใน `WBW/SettingsView.swift`) เป็น `Link` ออกเว็บ **ไม่ใช่ `DocView` ที่ฝังข้อความ**
      · URL ตรงกับที่จะกรอกใน ASC และ `WBWTests/SettingsLinksTests.swift` ตรวจให้
      · **แถวนี้อยู่ในบิลด์ก็ต่อเมื่อ archive ใหม่** — build ที่อัปไปแล้วก่อนหน้านี้ไม่มี

## ตรวจบนเครื่องจริงก่อนส่ง (คนเดียว เครื่องเดียว)

ผ่านแล้วเมื่อ 2026-08-22 บน iPhone 13 — รายละเอียดที่
`docs/appstore-1.0-10-verification.md` §8 และ §9

- [x] แผนที่ 3D โหลดจริง (2.93–4.17 วิ) ไล่ครบทุกแท็บไม่ crash ไม่มี crash report
- [x] ตำแหน่ง: จออธิบายมาก่อนกล่องของระบบ · ความแม่น 2.2–2.6 ม.
- [ ] **กดปุ่ม SOS จริงหนึ่งครั้งบนเครื่อง** — ยังไม่เคยทำ เพราะจะสร้างเคสจริงใน
      backend production · ถ้าจะทำ ต้องยอมให้มีเคสหนึ่งใบแล้วให้เจ้าหน้าที่ปิดทีหลัง
      (เส้นทางที่เทสครอบไม่ถึงคือ APNs กับการที่เพื่อนในกลุ่มได้รับแจ้งเตือนจริง)
- [ ] **เดินจริงบนเส้นทางสักรอบ** — ที่วัดได้คือเครื่องวางนิ่งในอาคาร
- [ ] **ลองบนเครื่องที่เก่ากว่า iPhone 13** ถ้าหาได้ — iOS 18 รองรับถึง iPhone XR/XS
      (A12 / RAM 3 GB) ส่วน iPhone 13 คือ A15 / 4 GB · ผลที่วัดมาแปลว่า
      "ไม่ใช่ปัญหาบนเครื่องระดับกลาง" ไม่ใช่ "ปลอดภัยทุกเครื่อง" · ถ้าเครื่องเก่า
      รับไม่ไหว ปิดด้วยค่าเดียวคือ `Config.map3D = false`

## หลังรีวิวผ่าน

- [ ] คืนที่นั่งของบัญชีรีวิว — ขั้นตอน SQL อยู่ที่ `docs/appstore-1.0-8-verification.md`
      หัวข้อ "วิธีย้อนกลับหลังรีวิวผ่าน" · **ลำดับสำคัญ** ลบผู้ใช้ก่อนแล้วค่อยลดเพดาน
      ไม่งั้นชน CHECK constraint
